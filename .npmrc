# 不自動安裝 peer dependencies（ADR-0352）。
#
# pnpm 8 起預設 `auto-install-peers=true`。對本 repo 的實際後果是：`react-native-svg`
# 宣告了 `react-native` 這個 peer（且未標 optional），於是**整套 React Native 建置工具鏈**
# （metro bundler、@react-native/codegen、@babel/core…）被裝進相依圖——共 158 個套件。
#
# 而 `apps/mobile` 跑的是 **react-native-web（DOM）**：`vite.config.ts` 把 `react-native`
# 別名成 `react-native-web`、`react-native-svg` 指向它的 `.web.js` 實作，所以那套工具鏈
# **從來沒有被打包進去**。它只存在於相依圖裡，卻讓 `pnpm audit --prod` 這道「會出貨給
# 使用者的相依必須零弱點」的硬閘（ADR-0235 M2）掃到 metro/babel 的 advisory 而長紅——
# 其中 `image-size` 上游根本沒有修（`patched_versions: <0.0.0`），升級也解不掉。
#
# 關掉之後，`react-native-svg` 的 web 實作仍真正需要的那一個小套件
# （`@react-native/assets-registry`，本身零相依）改為 `apps/mobile` 的**明示**相依。
#
# ⚠ 代價：日後新增套件時，它的 peer 不會自動出現——缺了要自己在 package.json 補上。
# 這比較囉嗦，但也比較誠實：相依圖裡的東西都是有人明講要的。
auto-install-peers=false
