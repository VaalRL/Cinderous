name = "nostr-buddy-relay"
main = "src/worker.ts"
compatibility_date = "2024-12-01"

# 單一全域房間：以 Durable Object 在記憶體中保存連線並做 Ephemeral 扇出。
[[durable_objects.bindings]]
name = "RELAY_ROOM"
class_name = "RelayRoom"

[[migrations]]
tag = "v1"
new_classes = ["RelayRoom"]
