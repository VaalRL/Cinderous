# 架構決策紀錄（ADR）

本目錄記錄 Cinderous（原名 Cinder，ADR-0191 更名）的重大架構與設計決策（Architecture Decision Records）。

> 自此之後，任何架構/設計層級的決策（模組邊界、加密與協定選型、資料流、隱私取捨、外部依賴等）都必須新增一份 ADR。`PRD.md` 與 `ARCHITECTURE.md` 描述「規格現況」，ADR 描述「為何如此決策」。

## 規則

- 一個決策一份檔案，檔名格式：`NNNN-簡短標題.md`（四位流水號）。
- 狀態使用：`提議中 (Proposed)`、`已接受 (Accepted)`、`已棄用 (Deprecated)`、`被取代 (Superseded by NNNN)`。
- ADR 一旦 `已接受` 即不可竄改內容；若決策改變，新增一份 ADR 並把舊的標記為 `被取代`。
- 撰寫格式參考 `0000-template.md`。

## 索引

| 編號 | 標題 | 狀態 |
| --- | --- | --- |
| [0001](./0001-record-architecture-decisions.md) | 採用 ADR 記錄架構決策 | 已接受 |
| [0002](./0002-privacy-metadata-and-protocol-baseline.md) | 隱私元資料保護與 Nostr 協定基線（NIP-44/17/59/42/13） | 已接受 |
| [0003](./0003-monorepo-tooling-pnpm-workspace.md) | Monorepo 工具採用 pnpm workspace | 已接受 |
| [0004](./0004-crypto-primitives-secp256k1-noble-in-core.md) | 加密原語：secp256k1（@noble）收斂於 packages/core | 已接受 |
| [0005](./0005-relay-self-built-worker.md) | 中繼站：自建最小 Cloudflare Worker relay | 已接受 |
| [0006](./0006-heartbeat-capacity-and-free-tier.md) | 心跳容量估算與免費額度策略 | 已接受 |
| [0007](./0007-nip44-via-nostr-tools.md) | NIP-44 加密採用 nostr-tools，事件/簽章維持自有 core | 已接受 |
| [0008](./0008-webrtc-signaling-and-datachannel-protocol.md) | WebRTC 信令與資料通道協定（M3） | 已接受 |
| [0009](./0009-multidevice-sync.md) | 多設備同步：QR 配對、競速與收斂語意（M4） | 已接受 |
| [0010](./0010-line-inspired-feature-roadmap.md) | 借鏡 LINE 的功能路線圖與範疇界線（M6–M9） | 已接受 |
| [0011](./0011-message-reactions.md) | 訊息回應（Reactions，M6） | 已接受 |
| [0012](./0012-message-unsend.md) | 收回訊息（Unsend，M6） | 已接受 |
| [0013](./0013-disappearing-messages.md) | 限時訊息（Disappearing，M6） | 已接受 |
| [0014](./0014-contact-management.md) | 聯絡人管理：刪除與封鎖（Phase A3） | 已接受 |
| [0015](./0015-settings-identity-backup-notifications.md) | 設定面板：身分備份、桌面通知與未讀（Phase A5） | 已接受 |
| [0016](./0016-relay-reconnect-and-connection-status.md) | 中繼站自動重連與連線狀態（Phase A5） | 已接受 |
| [0017](./0017-webrtc-file-transfer.md) | WebRTC P2P 檔案傳輸整合（Phase A4） | 已接受 |
| [0018](./0018-tauri-desktop-shell.md) | Tauri 桌面殼與 IPC 契約（Phase B1/B2） | 已接受 |
| [0019](./0019-background-relay-connection.md) | 背景長連線：政策驅動器 + I/O 執行期（Phase B3） | **被取代 by 0105**（原生實作已退役；目標由系統匣達成，0106 實測無節流） |
| [0020](./0020-native-persistence-sqlite-sqlcipher.md) | 原生持久化：SQLite / SQLCipher（Phase B4） | **被取代 by 0054**（加密 blob）；死碼由 0105 移除 |
| [0021](./0021-stickers.md) | 貼圖（Stickers，M7） | 已接受 |
| [0022](./0022-voice-messages.md) | 語音訊息（Voice Messages，M7） | 已接受 |
| [0023](./0023-media-album.md) | 媒體相簿（Album，M7） | 已接受（⚠ 內文的「跨 session 需 B4 SQLite」已作廢 → 由 **0102** 以縮圖重新設計） |
| [0024](./0024-qr-add-friend.md) | QR 加好友：npub QR 產生（M9） | 已接受 |
| [0025](./0025-call-signaling.md) | 語音/視訊通話信令核心（M8） | 已接受（⚠ 內文的「尚未接 RTCPeerConnection/通話 UI」已過時——桌面早已實作，行動端見 **0101**） |
| [0026](./0026-call-runtime-ui.md) | 通話執行期與 UI（M8） | 已接受 |
| [0027](./0027-group-encryption.md) | 群組聊天加密方案（M9） | 已接受 |
| [0028](./0028-forward-secrecy.md) | 前向保密：維持靜態金鑰、FS/PCS 交由未來 MLS（F2） | 已接受 |
| [0029](./0029-datachannel-binary-framing.md) | 資料通道檔案分塊改用二進位框架（F3/C4） | 已接受 |
| [0030](./0030-presence-ux-idle-away-and-status-formatting.md) | 在線狀態 UX：閒置自動離開與狀態列表情/格式 | 已接受 |
| [0031](./0031-animated-stickers.md) | 動態貼圖：宣告式 SVG 動畫（CSS keyframes） | 已接受 |
| [0032](./0032-custom-stickers.md) | 自製貼圖：內容隨訊息、SVG 統一表示、點擊即擁有 | 已接受 |
| [0033](./0033-sticker-editor.md) | 貼圖編輯器：筆劃模型序列化為 SVG path（桌面優先） | 已接受 |
| [0034](./0034-multi-relay-routing.md) | 跨中繼通訊：客戶端 Relay Pool 與收件人路由 | 已接受 |
| [0035](./0035-relay-hint-learning.md) | Relay hint 自動學習：帶內加密 hint（否決 NIP-65） | 已接受（其「可取代事件未實作」已由 **0099** 收斂） |
| [0036](./0036-relay-hint-staleness.md) | Hint 陳舊偵測與離線回退；群訊 rumor 帶 hint | 已接受 |
| [0037](./0037-sticker-text-triggers.md) | 文字觸發貼圖：composer 尾端比對 + 建議列（Tab 送出） | 已接受 |
| [0038](./0038-url-hygiene.md) | 網址衛生：貼上清除追蹤參數 + 本地啟發式高風險警告 | 已接受 |
| [0039](./0039-hybrid-bootstrap-routing.md) | 混合式引導路由：錨點常數 + 簽章清單 + home 自動遞補 | 已接受 |
| [0040](./0040-group-local-labels.md) | 群組本地標籤與置頂（自訂標籤，純客戶端） | 已接受 |
| [0041](./0041-outbox-paced-delivery.md) | 可靠訊息節流外送匣：OK 感知重試 + 重連補送 | 已接受 |
| [0042](./0042-custom-sticker-limits.md) | 自製貼圖容量與規格限制（標籤上限 + 現有 SVG 上限盤點） | 已接受 |
| [0043](./0043-animated-sticker-norms.md) | 自製動態貼圖規範（借鏡 LINE：reduced-motion 護欄 + 維持 SVG/上限） | 已接受 |
| [0044](./0044-enterprise-closed-relay.md) | 企業模式：封閉 allowlist 中繼 + 自架單節點 | 已接受 |
| [0045](./0045-multi-identity-profiles.md) | 單一 App 多身分並存與切換（工作＋個人） | 已接受（⚠ 內文的「nsec 應改存 OS 金鑰庫」已由 **0053** 完成） |
| [0046](./0046-enterprise-membership-and-boundary.md) | 企業成員判定與對外通訊邊界（威脅模型） | 已接受 |
| [0047](./0047-enterprise-provisioning-roster.md) | 企業佈建與組織通訊錄（管理者簽章名冊） | 已接受 |
| [0048](./0048-enterprise-policy-and-turn.md) | 企業政策開關與強制 TURN（relay allowedKinds + 名冊政策） | 已接受 |
| [0049](./0049-org-groups-and-announcements.md) | 組織群組與公告（管理者佈建） | 已接受 |
| [0050](./0050-mentions.md) | @提及 Mention：NIP-01 p-tag 攜帶於加密 rumor 內層 | 已接受 |
| [0051](./0051-threads.md) | 對話串 Thread：NIP-10 reply e-tag + 右側面板（Slack 佈局） | 已接受 |
| [0052](./0052-enterprise-identity-rotation.md) | 企業工作身分輪替/重佈建（否決金鑰托管，無後門） | 已接受 |
| [0053](./0053-tauri-native-substrate-integration.md) | Tauri 原生整合：基質替換（重用 TS 引擎）＋ B5 金鑰庫 | 已接受 |
| [0054](./0054-encrypted-storage-substrate-aes-gcm.md) | 加密儲存基質：AES-256-GCM 加密 blob（純 Rust，免 SQLCipher/Perl） | 已接受 |
| [0055](./0055-block-cross-identity-friending.md) | 禁止跨身分互加好友（保護分身不可連結） | 已接受 |
| [0056](./0056-offline-store-durable-object-sqlite.md) | 離線留言持久層：Durable Object 內建 SQLite（非獨立 D1） | 已接受 |
| [0057](./0057-open-relay-nip42-auth.md) | 開放中繼 NIP-42 AUTH（讀取＋發布；企業維持 allowlist） | 已接受 |
| [0058](./0058-delivery-read-receipts.md) | 送達／已讀回條（Gift Wrap 回條，已讀 opt-in＋互惠） | 已接受 |
| [0059](./0059-relay-hibernation-heartbeat.md) | 中繼站 WebSocket 休眠化 + 心跳 30s（降免費層 duration/請求） | 已接受 |
| [0060](./0060-local-ollama-rewrite.md) | 本機 Ollama 訊息改寫（Rust IPC、localhost 限定、先預覽再採用） | 已接受 |
| [0061](./0061-encrypted-display-name.md) | 顯示名稱以加密個人檔廣播給聯絡人（非公開 kind 0） | 已接受 |
| [0062](./0062-online-llm-providers.md) | 桌面版接線上 LLM provider（OpenAI 相容、API key 存金鑰庫、localhost 守則把關） | 已接受 |
| [0063](./0063-phase-d-mobile-scaffold.md) | Phase D 行動端骨架：react-native-web 於此環境開發、重用 core/i18n | 已接受 |
| [0064](./0064-custom-accent-color.md) | 自訂主題色（本地儲存、單一 --accent 覆寫、吉祥物連動） | 已接受 |
| [0065](./0065-offline-store-ttl-cap.md) | 離線留言壽命上限（無標籤給預設 TTL、超長截斷——孤兒資料不可能） | 已接受 |
| [0066](./0066-home-relay-migration.md) | Home relay 搬家（個人檔廣播帶 hint ＋ 舊站排水） | 已接受 |
| [0067](./0067-local-password.md) | 本地密碼：密碼衍生金鑰包裹身分祕密（否決 nsec 日常登入） | 已接受 |
| [0068](./0068-group-snapshot-broadcast.md) | 管理員開機群組快照廣播（群組成員資格自癒） | 已接受 |
| [0069](./0069-auto-relay-assignment-migration.md) | Relay 自動分配與自動搬家（簽章清單驅動） | 已接受 |
| [0070](./0070-encrypted-backup-code.md) | 加密備份碼（NIP-49 混合式、使用者自持） | 已接受 |
| [0071](./0071-encrypted-cloud-snapshot.md) | 加密雲端快照（三檔模式、可尋址取代、opt-in） | 已接受 |
| [0072](./0072-desktop-pairing-clone.md) | 桌面配對克隆（D4a：一次性 P2P 全量搬家＋SAS 互認） | 已接受 |
| [0073](./0073-forgotten-password-nsec-rescue.md) | 忘記密碼救援：nsec 解鎖並重設密碼（雙重包裹） | 已接受 |
| [0074](./0074-frontend-extensibility-engine-package.md) | 社群自訂前端：三層封裝與 @cinder/engine 抽取 | 已接受 |
| [0075](./0075-containerized-relay-self-hosting.md) | 容器化中繼站自架（Dockerfile＋HTTP 健康端點） | 已接受 |
| [0076](./0076-desktop-native-notifications.md) | 桌面原生通知（外掛＋點擊回跳＋傳訊者/預覽/音效） | 已接受 |
| [0077](./0077-local-personalization.md) | 本地個人化（對話框縮放＋本地頭像＋每對話背景） | 已接受 |
| [0078](./0078-secondary-accent-color.md) | 副色（次要主題色：標題列＋頂部漸層） | 已接受 |
| [0079](./0079-three-column-layout-toggle.md) | 桌面三欄整合佈局＋經典/新版一鍵切換 | 已接受 |
| [0080](./0080-shared-theme-tokens-package.md) | 跨前端設計 token 套件 @cinder/theme（桌面/行動端共用主題色 SSOT） | 已接受 |
| [0081](./0081-mobile-signin-nsec-and-pairing.md) | 行動端登入：nsec 匯入＋配對匯入（同帳號跨裝置） | 已接受 |
| [0082](./0082-user-facing-naming-glossary.md) | 使用者可見名稱統一詞彙表（登入／同步／搬家／備份／配對） | 已接受 |
| [0083](./0083-drain-fully-internal.md) | 舊站排水完全內部化（移除所有 UI，機制不變） | 已接受 |
| [0084](./0084-code-identifier-naming.md) | 程式碼識別字命名：行動端 i18n 前綴一致化，其餘刻意保留 | 已接受 |
| [0085](./0085-mobile-app-shell-navigation.md) | 行動端 app 殼與導覽：聊天清單（最近互動排序）＋對話（LINE/Signal 風格） | 已接受 |
| [0086](./0086-mobile-real-relay-backend.md) | 行動端接真實 relay：RelayChatBackend＋加好友（npub） | 已接受（其列出的四項落差已由 **0100／0101** 全數補齊） |
| [0087](./0087-mobile-bottom-tabs.md) | 行動端底部分頁：聊天／聯絡人／設定 | 已接受 |
| [0088](./0088-presence-metadata-minimization-jitter-and-p2p-offload.md) | 元資料最小化（第一階段）：在線 jitter/隱身＋心跳 P2P 卸載 | 已接受 |
| [0089](./0089-relay-operator-donation-entry.md) | Relay 營運者贊助入口（NIP-11 擴充＋桌面角落卡＋純導流） | 已接受 |
| [0090](./0090-official-website-donation-and-fund-transparency.md) | 官方網站、官方捐款與資金透明度（靜態站＋簽章 JSON runway） | 已接受 |
| [0091](./0091-forward-secrecy-mls-upgrade-design.md) | 前向保密升級：統一採 MLS（含正常使用者可靠性護欄） | 已棄用（暫緩未採納，維持 0028） |
| [0092](./0092-node-membership-self-report-and-conformance-audit.md) | 節點成員自報＋一致性稽核＋簽章可驗證記錄（申請/審查/稽核介面化） | 已接受 |
| [0093](./0093-multi-device-delivery-and-file-persistence.md) | 多設備即時投遞與檔案接收：檔案 metadata 中繼化＋位元組 P2P＋收檔另存至使用者選定路徑 | 已接受 |
| [0094](./0094-retention-limit-and-plaintext-export.md) | 本地紀錄保留上限（可設定、預設無上限）與明文紀錄導出（TXT/MD/JSON、範圍可選） | 已接受 |
| [0095](./0095-group-receipts-and-message-status-icons.md) | 群組送達/已讀分級（≤5 名單／6–10 計數／>10 不記）＋failed 狀態與眼睛圖示語言＋行動端狀態同步 | 已接受 |
| [0096](./0096-mobile-vector-icons-react-native-svg.md) | 行動端向量圖示改採 react-native-svg＋web 端解析設定；補行動端群組已讀 UI（收斂 0095 後續） | 已接受 |
| [0097](./0097-inline-math-preview-and-calc-panel.md) | 對話算式預覽：composer 即時預覽＋右欄計算機（純本地純函式、禁用 eval、保守觸發） | 已接受 |
| [0098](./0098-cli-and-ai-mcp-boundaries.md) | 無頭 CLI（無狀態：不留私鑰/明文歷史）＋AI/MCP 接取邊界（致命三角、只提議不直送、雲端 LLM 預設關） | 已接受 |
| [0099](./0099-replaceable-events-and-kind-collision-fix.md) | 中繼站實作 NIP-01 可取代事件（收斂 0035：清單不再囤積）＋修正 kind 10038 撞號 | 已接受 |
| [0100](./0100-mobile-parity-anchors-cloud-files.md) | 行動端補齊：錨點/簽章清單、加密雲端備份、檔案傳輸（收斂 0086）＋引導設定移入 engine（SSOT） | 已接受 |
| [0101](./0101-mobile-calls.md) | 行動端通話（語音/視訊）：媒體平台縫（<video> 收斂於單一檔）＋react-native-webrtc 移植路徑 | 已接受 |
| [0102](./0102-image-thumbnails-and-relocate-original.md) | 圖片跨 session：持久化縮圖＋從 savedPath 讀回原圖＋原檔搬走可重新指定（收斂 0023） | 已接受 |
| [0103](./0103-native-file-picker-sender-path.md) | 送出端改走原生選檔對話框以取得原檔路徑（補完 0102：自己傳的圖也看得到原圖） | 已接受 |
| [0104](./0104-native-file-drop.md) | 原生檔案拖放（Tauri onDragDropEvent）＋修好「打包後拖放根本沒作用」；拖放也取得真實路徑 | 已接受 |
| [0105](./0105-retire-native-backend-dead-code.md) | 退役原生後端死碼（0019 背景連線／0020 SQLite）＋讓 cargo test 真的測到出貨的密碼學 | 已接受 |
| [0106](./0106-webview-throttling-measurement.md) | 實測：隱藏視窗的 webview 不會被節流（心跳全速）→ 推翻「引擎下沉 Rust」的主要理由，決策不下沉 | 已接受 |
| [0107](./0107-nip17-self-copy.md) | NIP-17 自封副本：自己發的訊息也包一份給自己 → 多裝置對話完整；訊息 id 統一為 rumor.id | 已接受 |
| [0108](./0108-read-watermark-local.md) | 已讀水位本機持久化（未讀不再重載歸零）＋訊息時間改用 rumor.created_at（修正順序錯亂） | 已接受 |
| [0109](./0109-relay-traffic-reduction.md) | 中繼流量削減約 90%：自適應心跳（閒置放慢）＋心跳自報節奏＋訂閱合併為單一 REQ＋收件箱增量抓取 | 已接受 |
| [0110](./0110-long-conversation-hot-paths.md) | 長對話熱路徑：id 索引（O(n²)→O(n)）、批次回條（3.5s→18ms）、增量未讀、分部位持久化 | 已接受 |
| [0111](./0111-message-archive.md) | 訊息封存（冷熱分離）：熱區 5,000 則、更舊者移入加密塊檔／OPFS；把 N 綁住，並修好 0094 的資料遺失 | 已接受 |
| [0112](./0112-web-at-rest-encryption.md) | 網頁/行動端靜態加密（DEK=HKDF(nsec)）＋**nsec 不再明文落盤**；推翻 0067 的「瀏覽器＝假安全感」 | 已接受 |
| [0113](./0113-local-launcher.md) | 本地啟動器 `cinder serve`：固定 port（origin ＝ 資料的身分）、撞埠不自動換、路徑穿越防護 | 已接受 |
| [0114](./0114-mobile-parity-messaging.md) | 行動端對齊（一）：修好群訊（點進群組送訊會爆）＋補上收回／emoji 回應／封鎖 | 已接受 |
| [0115](./0115-mobile-parity-groups.md) | 行動端對齊（二）：建立群組與成員管理、敲一下（可發）、上線狀態 | 已接受 |
| [0116](./0116-mobile-notifications.md) | 行動端通知（＋隱藏預覽的隱私開關）；Web Notification 基質收斂為單一來源；不做推播 | 已接受 |
| [0117](./0117-mobile-remember-me.md) | 行動端「記住我」：Argon2id 密碼包裹 nsec（絕不無密碼記住）；忘記密碼無救援但可改用 nsec | 已接受 |
| [0118](./0118-pairing-source-identity.md) | **修好桌面配對搬家**（捆包一直沒有 nsec）＋行動端補上送出端與真實的匯入 | 已接受 |
| [0119](./0119-health-check-fixes.md) | **全面健檢**：分部位寫入失敗＝靜默資料遺失、Rust 非原子寫入（斷電毀一整個對話）、**ADR-0112 在桌面瀏覽器路徑上整個是死碼**、瀏覽器「新增身分」造出打不開的身分、`stop()` 不關連線、群組回應／收回兩端皆爆 | 已接受 |
| [0120](./0120-seal-typing-nudge.md) | **typing／nudge 以 NIP-59 封裝**——明文的「正在輸入」是一條已簽章的社交圖譜邊，且能靠時間相關性**反推 Gift Wrap 的寄件人**；訂閱改為只靠 `#p`，並補上「只收聯絡人」的把關 | 已接受 |
| [0121](./0121-message-requests.md) | **訊息請求**：陌生人的訊息不再自動讓他變成聯絡人——在你接受前，他碰不到通知、未讀徽章、自動開窗、nudge、typing、上線狀態、個人檔、已讀回條 | 已接受 |
| [0122](./0122-browser-identity-persistence.md) | **瀏覽器版重載會把你換成另一個人**——身分不得被靜默替換（`expectPubkey` 守衛）＋接上一直是死碼的瀏覽器本地密碼 | 已接受 |
| [0123](./0123-relay-scoped-subscriptions.md) | **中繼訂閱必須具名**——`{kinds:[20000]}` 這種無 scope 的 filter 可一次收割全站線上名冊；順帶修好「一致性探測不做 AUTH → 每次 cron 都把自己的中繼判成不存活」 | 已接受 |
| [0124](./0124-group-file-transfer.md) | **群組傳檔**：metadata 扇給每位成員（共用 rumor 與 tid）、位元組各自 P2P；順帶修正 `onFileBytes` 回報 peer 而非對話鍵 | 已接受 |
| [0125](./0125-mobile-pair-import-apply-bundle.md) | **行動端配對搬家要套用捆包**——過去只還原身分，換手機後聯絡人與訊息全部沒搬過去（只搬空身分）；順帶補上行動端的 ADR-0122 身分守衛 | 已接受 |
| [0126](./0126-retention-cap-archives-not-deletes.md) | **保留上限＝封存而非刪除**——統一與 HOT_CAP 各說各話的兩個上限；使用者設上限不再靜默刪歷史，溢出移進封存（web/mobile 移出 localStorage 紓解配額） | 已接受 |
| [0127](./0127-message-request-flood-control.md) | **訊息請求防洪**：數量上限 100（FIFO 逐出，連訊息一起清）＋「全部刪除」一鍵出路；速率限制由硬上限上位替代 | 已接受 |
| [0128](./0128-file-ipc-hardening.md) | **檔案 IPC 縱深防禦**：收檔檔名消毒（遠端可控 → 乾淨 basename）＋ `read_saved_file` 路徑白名單（只讀原生對話框授權過的路徑，雜湊持久化） | 已接受 |
| [0129](./0129-sealed-presence-state.md) | **在線狀態內容改走封裝**：心跳降為無內容存活信標；狀態文字與音樂改以 NIP-59 封裝、只在改變/對方上線時發給「在線✕無P2P」的聯絡人——relay 再也讀不到你在聽什麼、狀態寫什麼 | 已接受 |
| [0130](./0130-jsdom-effect-tests.md) | **UI 測試補上會跑 `useEffect` 的環境**（jsdom 逐檔切、`createRoot`+`act`，不churn 既有 SSR 測試）——關掉「effect 從不執行」的測試盲區（ADR-0122 的 P0 就藏在那裡） | 已接受 |
| [0131](./0131-buffer-early-group-messages.md) | **早到的群訊不再被丟掉**——群訊比 group-create 先到（NIP-59 jitter 打亂順序）時緩存、加入後依時間重放；有界＋去重防假 `g` tag 撐爆記憶體 | 已接受 |
| [0132](./0132-image-share.md) | **圖片分享**：行動端走原生分享選單（Web Share API，與其他手機 app 同體驗，不支援退回下載）、桌面燈箱走快速複製（複製圖片轉 PNG／複製路徑，僅在已另存時）；掛在圖片訊息上、不綁相簿 | 已接受 |
| [0133](./0133-mobile-mentions.md) | **行動端補 @提及**：建議純函式（`suggestMentions`／`applyMention`）下沉 `@cinder/core`、兩端共用；行動端加建議列、送出解析成 `p` tag、被提及氣泡凸顯 | 已接受 |
| [0134](./0134-mobile-chat-background.md) | **行動端補對話背景**：背景 token（預設漸層／CSS 產生／儲存鍵）下沉 `@cinder/theme`、桌面 re-export 零改動；行動端加標題列入口＋預設漸層挑選，純本地不上雲（圖片上傳待原生） | 已接受 |
| [0135](./0135-mobile-identity-security-ui.md) | **行動端身分安全 UI**：改本地密碼（舊解新包）、產生加密備份碼（ADR-0070 core 早備）、登入/救援欄位同時吃 nsec 與備份碼；密碼學全在 core，行動端只接線 | 已接受 |
| [0136](./0136-mobile-inline-reply.md) | **行動端對話串/回覆**：內嵌引用（Signal/LINE 風），同 `replyTo` 資料、與桌面討論串面板各自呈現；長按→回覆→引用列→送出帶 NIP-10 reply e-tag；引用對已收回/檔案/引用不到皆安全處理 | 已接受 |
| [0137](./0137-mobile-stickers.md) | **行動端貼圖**：貼圖格式/內建資料/SVG 驗證下沉 `@cinder/core`、桌面 import 改指 core；行動端渲染收到的貼圖（含收端驗證惡意 SVG）＋內建貼圖挑選送出；編輯器留桌面 | 已接受 |
| [0138](./0138-mobile-multi-identity.md) | **行動端多身分**：採用 engine `profiles` 登錄、每身分 Argon2id 密碼包裹的 nsec（`nb.remembered.<pubkey>`）、舊單一身分無縫遷移；切換即解該身分密碼、新增、逐一登出；禁跨身分交友（ADR-0055） | 已接受 |
| [0139](./0139-unified-custom-dialog.md) | **統一自訂對話框**：Promise 介面的 confirm/alert/prompt（`useDialog`＋`DialogProvider`）取代瀏覽器內建；重用視窗樣式、主題感知、Esc/Enter/背景鍵盤操作、破壞性紅鈕；非元件走 `dialog()` 橋接、無 provider 回退 | 已接受 |
| [0140](./0140-fix-identity-switch-duplicate.md) | **修復切換身分→掉登入頁→建重複身分（帶舊聯絡人）**：Tauri 取不到金鑰改救援分流（`setNeedNsec`，與瀏覽器對稱）；`signIn` 命名空間隔離（純函式 `pickSignInNamespace`，非第一身分不重用 `""`）；救回即補寫金鑰庫治本 | 已接受 |
| [0141](./0141-unified-button-system.md) | **統一按鈕系統**：主要動作＝主色實心（隨主題色，取代所有綠色漸層）、中性＝描邊淺底、危險＝單一紅；裸 `<button>` 給中性預設不再落回 OS 灰；全域一致焦點環/停用態；語意色（接聽綠/掛斷紅）保留 | 已接受 |
| [0142](./0142-modern-layout-settings-and-status.md) | **三欄版改善**：設定入口移到上方 nav（idbar）不再擠頭像旁；左側欄補回自訂狀態文字＋正在聽（與經典版對齊）；設定改分頁（外觀/身分安全/連線備份/隱私通知/進階），只顯有內容分頁 | 已接受 |
| [0143](./0143-embedded-conversation-flush.md) | **三欄中欄內嵌對話整塊填滿**：去除外框/視窗底色/彩色標題列（不再像浮動視窗），標題列改扁平對話標頭；並修正把 JSX `//` 註解當文字渲染的 bug | 已接受 |
| [0144](./0144-change-display-name.md) | **更改顯示名稱（桌面＋行動）**：後端加 `setSelfName`（落地本機＋廣播加密 profile 給聯絡人 ADR-0061）；設定加改名欄，同步 self/登錄/記住的 blob；不換金鑰、不換身分 | 已接受 |
| [0145](./0145-add-identity-type-picker.md) | **新增身分先選類型（個人／組織）**：`AddIdentityModal` 改兩步——先以圖示＋說明選 👤 個人／🏢 組織（ADR-0047），再填表單；`mode` 取代 `enterprise` 布林、管理者 npub 欄只在組織出現、可回頭改選；`onAdd` 簽章不變 | 已接受 |
| [0146](./0146-signin-by-name-and-unique-names.md) | **登入以顯示名稱解析既有身分＋本機名稱唯一**：桌面登入打既有名字＝`setActive`+reload 進那個身分（重用解鎖/救援路徑，Fix First），不建重複；純函式 `resolveSignIn`／`nameTaken`（排除 hidden、trim 比對）下沉 engine；新增/改名兩端擋重名；名稱只是查找鍵非加密鍵，安全不變；行動端登入係 nsec 導向故只做名稱唯一 | 已接受 |
| [0147](./0147-self-hosted-web-app-separate-origin.md) | **自架網頁版部署——app 與官網分子網域**：瀏覽器版＝`apps/desktop` 的 web build（`isTauri()=false`，金鑰本機加密 ADR-0112/0122）；自架須把 app 與官網放**不同 origin**（延續 ADR-0090 隔離）、app origin HTTPS＋嚴格 CSP；relay 經 `?relay=`／`ANCHOR_RELAYS`／手動指定；官方 `apps/website` 維持硬隔離不變。附 `docs/self-hosting-web-app.md` 指南 | 已接受 |
| [0148](./0148-local-contact-alias.md) | **本地暱稱**：給聯絡人取**只有自己看得到**的顯示名（`Contact.alias`＋`setContactAlias`），恒優先於廣播名顯示（純函式 `contactLabel` 兩端共用）；**絕不廣播/上雲**，只隨自己的加密快照/搬家流動；不覆寫廣播名——對話標頭點名字可在暱稱↔廣播名切換、✎ 設定/清除（1:1、桌面＋行動） | 已接受 |
| [0149](./0149-notification-chime-presets-per-contact.md) | **通知音效自訂（桌面）**：6 種 Web Audio 合成預設集（`CHIME_PRESETS` 純資料配方，零音檔，未知 id 退回叮咚）；設定面板全域音效下拉＋試聽（`nb.notifyChime`）；依聯絡人覆寫 `Contact.notifySound`＋`setContactNotifySound`（比照 ADR-0148：**絕不廣播/上雲**，隨加密快照流動），對話標頭 🔔 指定；自訂音檔留待後續 ADR | 已接受 |
| [0150](./0150-custom-window-frame-configurable-controls.md) | **自繪視窗外框＋按鈕位置/順序可自訂（桌面）**：`decorations:false`＋前端自繪標題列（簡約扁平、吃主題 token）；`nb.titlebarControls`（左/右＋─□✕ 任意排，`parseTitlebarControls` 防呆永遠有效）；`WindowChrome` 包所有畫面（登入/解鎖也有外框）、瀏覽器版透傳；設定→外觀迷你預覽＋←→ 逐顆調；取捨：Win11 Snap Layouts 懸停選單消失。設定模型由 [0151](./0151-titlebar-settings-button-drag-autohide.md) 升級為雙帶 | 已接受 |
| [0151](./0151-titlebar-settings-button-drag-autohide.md) | **標題列 ⚙ 上移外框＋滑鼠拖曳排位＋平時隱藏**：設定模型升級雙帶 `{left[], right[], autoHide}`（v1 同鍵自動遷移、⚙ 補對側）；⚙ 成為第四顆可拖控制項（App 以 context 註冊開啟器、沒註冊不畫；Tauri 下 nav bar ⚙ 不重複畫）；設定頁改「編輯器即預覽」HTML5 拖放（拖到某顆前/帶尾，`placeControl` 純函式）；`autoHide`＝按鈕平時 opacity 0、hover 標題列才顯示。拖曳機制與 ⚙ 預設位置由 [0152](./0152-titlebar-drag-pointer-events-default-position.md) 修正 | 已接受 |
| [0152](./0152-titlebar-drag-pointer-events-default-position.md) | **拖曳改 pointer events＋⚙ 預設貼最小化左側（修 0151）**：Tauri `dragDropEnabled`（ADR-0104）在 WebView2 吞 HTML5 DnD → 改 `setPointerCapture`＋`elementFromPoint` 命中 `data-drop-side`/`data-piece` 自行實作（in-app 拖曳日後一律走 pointer events）；預設改 `{left:[], right:[⚙,─,□,✕]}`，v1 遷移 ⚙ 補帶最前、0151 舊預設視為未自訂自動轉新預設（autoHide 保留） | 已接受 |
| [0153](./0153-autohide-whole-titlebar.md) | **平時隱藏＝整條標題列滑出（修 0151 語意）**：autoHide 改為標題列 fixed 覆蓋層 `translateY(-100%)` 滑出、視窗頂端 6px 熱區或標題列 hover 才滑入（z-index 100/101 蓋過 app 內一切；reduced-motion 關動畫）；內容拿回全高（`--viewport-h` 回 100vh）；外框本體抽 `ChromeFrame` 供 SSR 測試；`TitleBar` 不再感知 autoHide | 已接受 |
| [0154](./0154-avatar-encrypted-broadcast.md) | **頭像加密廣播**：`wrapProfile` content 擴成 `{name, avatar?}`（128px data URI 縮圖；`""`＝移除；缺席＝無變更）；收端白名單 `data:image/*` ＋48KB 上限；**網址只當輸入方式**（本人裝置抓圖轉縮圖，不把 URL 發給對方＝不做追蹤信標）；雲端快照剝除 avatar（180KB 預算）；0077 本地頭像不自動廣播（`setSelfAvatar` 明確觸發）；顯示優先序 本地覆寫＞廣播＞生成；補三平台入口（DeckSidebar／行動端設定頁） | 已接受 |
| [0155](./0155-org-owner-identity-converged-roster-entry.md) | **企業主身分＋名冊入口收斂**：`Profile.orgOwner` 標記（一般身分＋名冊管理權，後端語意同個人）；新增身分第三類型 🗂 企業主（0145 兩步流程加一顆，表單無管理者欄——自己就是管理者）；建立後首次進入自動開名冊管理（`nb.rosterIntro.<pubkey>` 跨 reload）；idbar 🗂 從「人人可見」收緊為僅企業主顯示；idbar tooltips 進 i18n | 已接受 |
| [0156](./0156-org-invite-code-auto-join.md) | **入職邀請碼與自動入職**：`cinderinvite1…` 單一 token（relay＋管理者 pubkey＋核准權杖；可嵌任意文字中抽取）；成員三入口（登入畫面貼碼轉「加入組織」面板／企業成員表單貼碼自動填／開機重送入職請求直到入冊）；企業主自動核准（訂自己的可取代名冊找回狀態、驗權杖、自動併入重發、自動加聯絡人；首發前排隊）；入職請求為加密 rumor（`ORG_JOIN=42`，走離線信箱） | 已接受 |
| [0157](./0157-org-profile-welcome-work-hours.md) | **公司設定隨名冊分發**：`OrgRosterDoc` 擴 `welcome`（歡迎詞/基本規範，≤2000 字）與 `workHours`（HH:MM，支援跨夜）；成員端 `onOrgInfo` → 歡迎詞一次性彈窗（內容變更再彈）＋設定「組織資訊」區＋**下班自動靜音**組織通知（純函式判定；未讀照常）；企業主名冊視窗新增欄位＋ `currentRoster()` 預填（不再每次重打）；自動核准重發保留公司設定 | 已接受 |
| [0158](./0158-contact-labels-and-org-title.md) | **聯絡人標籤補全＋企業頭銜**：經典佈局補標籤入口（對話視窗對方頭像旁 chips＋🏷）；`forget()` 刪除/封鎖一併清標籤；**企業頭銜**自填隨加密個人檔廣播（`ProfileData.title` ≤24 字、`""`＝移除；工作身分聯絡人＝全組織同事 → 自然全員可見）；顯示以實心 `chip--role` 與私標 outline chip 色彩區隔；`selfTitle` 三態持久化隨快照/搬家；手機版標籤與同步留待後續 | 已接受 |
| [0159](./0159-off-hours-sender-hint.md) | **下班時間發訊提示**（0157 的發訊端另一半）：組織對話（成員 1:1/組織群組）在表定時間外，輸入區上方顯示非阻斷橫幅「對方通知已靜音，可能不會即時看到；訊息照常送達」；同一份名冊班表本地判定、每分鐘重算、不擋送出；明文確認靜音不影響投遞/入庫/未讀 | 已接受 |
| [0160](./0160-org-message-retention.md) | **企業訊息保留天數**：名冊政策 `messageTtlDays`（1–365，管理者設定、全組織一致）→ 發送端對聊天/檔案 metadata 蓋 `now+N天` 外層過期（閱後即焚不受影響）；中繼站 `MAX_TTL_DAYS` 環境變數放寬 TTL 上限（CF Worker＋node；未設＝7 天不變，站方上限恆為權威、超章截斷）；任何新舊組合都取兩者較小值、不會壞 | 已接受 |
| [0161](./0161-company-slot-queued-deposit.md) | **公司儲存槽**：員工**主動選擇**檔案存入（自主動作非監控）→ 本地佇列（存 savedPath 不存位元組）→ 企業主上線即背景 P2P 傳輸；`slot` rumor 標籤讓兩端不建聊天訊息；企業主端只收名冊成員、靜默落盤 `<槽>/<員工>/<日期>-<檔名>`＋index.jsonl（Rust 指令、基底外拒寫、逐段消毒）；未設槽目錄＝appData 預設；Tauri 桌面限定 | 已接受 |
| [0162](./0162-org-relay-file-transfer.md) | **組織檔案經 relay 暫存**（企業限定、預設關）：獨立外層 kind `FILE_WRAP=1060`（密文分塊 ≤48KB/塊，`FILE_CHUNK=43`）讓中繼站能整類拒收＋獨立配額桶（預設 4000 顆/收件人，**不與聊天 500 則互踐**）；`MAX_FILE_MB` 環境變數未設＝拒收（公共站零影響）；名冊政策 `relayFilesMaxMb`（1–16）啟用後名冊成員 1:1 小檔自動走 relay——離線收件人上線即收；大檔/群組維持 P2P | 已接受 |
| [0163](./0163-org-key-escrow-offboarding.md) | **入職金鑰託管＋離職接管**（公司帳號模型）：邀請碼 `escrow` 旗標（揭露、員工同意）→ 入職請求（0156 Gift Wrap E2E）順帶 nsec＝以管理者公鑰加密託管；企業主 `onOrgEscrow` 持久化 `nb.orgEscrow.<admin>`；離職＝名冊移除→`rosterAllowlist` 排除→舊金鑰連公司站都發不了（可執行的封鎖）；接管＝託管 nsec 匯入為 `orgOffboarded` 本機身分查看/刪除；ADR 明載「歷史＝relay 殘留＋同事副本、防不住在職自行外洩」的誠實極限 | 已接受 |
| [0164](./0164-local-presence-persistence.md) | **本機記住上線狀態與自訂狀態文字**：`nb.presence.<pubkey>` 存 `{status, statusMessage}`（純本機、依身分、不進 Nostr/雲端）；只記**手動**選擇（閒置自動 away 不落地）；上線還原（塞進 self 讓 idle 以此為基準＋`start()` 後同步後端廣播）；`nowPlaying` 維持 Ephemeral；**中繼站仍不持久化狀態**、鐵則不變 | 已接受 |
| [0165](./0165-usage-aware-heartbeat-shedding.md) | **依使用習慣的心跳時段降載**（研究記錄）：降載接入點單一（`beatInterval()`）、`hb` 自報已支援任意節奏、IDLE→ACTIVE 即時喚醒現成；方案 A 學習時段直方圖／方案 B 漸進閒置退避（推薦、不用學習）；最大浪費 ADR-0109 已消除、再降載為增量效益（省電/省額度）→ **暫緩**，待量測支撐先做方案 B | **暫緩（未實作）** |
| [0166](./0166-monetization-surface-and-trademark.md) | **營利面盤點與商標建議**（研究記錄）：**AGPL 不禁止收費**（自由非價格）——只擋閉源圈地（copyleft）＋要求 SaaS 揭露源碼；「作者不營利」已結構性達成（零站內金流＋簽章資金透明度）；「他人不營利」AGPL 做不到，盤點六種營利面（付費 relay/改名販售/fork 站內營利/賣元資料/賣支援/冒充官方）；唯一有效防線＝**註冊 Cinder 商標＋Logo＋商標政策**；非商業授權為代價大的備選 → **暫緩** | **暫緩（未實作）** |
| [0167](./0167-per-identity-appearance-and-titlebar-styles.md) | **外觀依身分覆寫、回退裝置層**：`identity-scoped` 儲存（`nb.<pubkey>.<suffix>` → `nb.<suffix>`）套用於**主色/佈局/標題列**；主題/語言維持全域（裝置偏好）；遷移零痛（舊全域值＝裝置層預設）；**標題列按鈕風格** `TitlebarControls.style`（flat/rounded/mac 交通燈/compact，隨身分層走）＋設定即時預覽 | 已接受 |
| [0168](./0168-mobile-parity-batch-1-presence.md) | **行動端功能對齊批次一**（引擎已就緒、只差 UI）：**自訂狀態文字**（`setStatus(status,msg)` 接上＋設定頁輸入）、**正在聽**（`setNowPlaying`＋對話副標題 ♪ 優先）、**上線狀態本機還原**（移植 `presence.ts`＝桌面 ADR-0164 同契約，`initialStatus` 塞進後端建構→首次心跳即正確、不洩漏舊狀態）、**敲一下收到即震動**（`navigator.vibrate`，不支援則靜默）；全純本機／依身分，中繼鐵則不動；輸入中提示、限時訊息、移除聯絡人等留待後續批次 | 已接受 |
| [0169](./0169-mobile-parity-batch-2-conversation.md) | **行動端功能對齊批次二**（對話／聯絡人層）：**輸入中提示**（送＝`onChangeText` 每 3 秒節流 `sendTyping`；收＝`onTyping` 6 秒易失、對話副標最優先顯示）、**限時訊息**（燒毀鈕循環 關/1m/1h/1d，`onSend` 帶 `ttlSeconds`→`sendMessage`，1:1 才有、群組扇出不帶 ttl）、**移除聯絡人**（長按「移除／封鎖」並排，`window.confirm` 後 `removeContact`＝清對話但不封鎖）、**連線狀態細條**（`onConnection`→非 online 顯示琥珀/紅細條，僅真實 relay）；SSR testID 斷言條件渲染 | 已接受 |
| [0297](./0297-device-bound-key-feasibility.md) | **裝置綁定金鑰的跨平台可行性**（研究記錄；補完 ADR-0296 §7-1 自列的「未做可行性評估」）。**先把兩個等級分開**——**A 祕密儲存**（Windows Credential Manager/DPAPI、macOS Keychain、Linux Secret Service：金鑰位元組會被取回，受 OS ACL 而非硬體保護）vs **B 不可匯出金鑰**（TPM 2.0／Secure Enclave／Android StrongBox：私鑰永不離晶片，只能請它 sign/decrypt/derive）；ADR-0296 要的是 B，**A 給不了**。**⚠ 更正對現況的理解：我們今天在 A 不在 B**——`keyvault.rs` 用 `keyring` crate ⇒ 三平台都是 A 級；Windows 那格尤其要講清楚，**Credential Manager 以 DPAPI 加密，而 DPAPI 是以使用者/機器祕密做對稱加密，與 TPM 支援的 NCrypt + Microsoft Platform Crypto Provider 是不同的東西** ⇒ **「已把 nsec 放進 OS 金鑰庫」不等於「複製磁碟也沒用」**；ADR-0053 給的是「不明文落盤」（真的、有價值）但**不是**裝置綁定，這個區別過去沒寫下來、容易被誤讀。**各平台**：Android 最好（Keystore/StrongBox **原生支援 AES ⇒ 直接**包 DEK，但需 API 28+ 且 `FEATURE_STRONGBOX_KEYSTORE`、不支援會丟 `StrongBoxUnavailableException` 故必須有 TEE 退路）；iOS 可行但**間接**（**SE 只支援 P-256、沒有對稱金鑰**，須以 ECIES `SecKeyCreateDecryptedData` 解封 DEK）；macOS 視 T2/Apple Silicon 而定（Intel 舊機無 SE）；Windows 需換到 CNG/PCP（**重寫金鑰庫層而非加參數**）；**Linux 沒有可攜的 B 級路徑**；**⚠ 瀏覽器擋的是另一個威脅**——WebCrypto `extractable:false` 擋得住 XSS 竊鑰位元組，但金鑰材料仍在瀏覽器 profile 目錄裡，**對「磁碟映像＋日後入侵」幾乎無效**。**⚠ 就算全做到 B 級**：擋的是離線攻擊（偷硬碟/映像/備份外洩），**擋不住以你的身分正在執行的惡意程式**（它可直接請 TPM/SE 解密）⇒ 文案只能說「磁碟被複製也解不開」，不能說「裝置被入侵也安全」。**設計形狀只能是分級**（L2 裝置綁定／L1 密碼包裹／L0 祕密儲存）＋**降級必須可見**、設定頁如實顯示本機等級。**未在任何平台實作或實測**；**最大未解問題：裝置綁定的反面是「裝置壞了資料就沒了」**（TPM 被清除/換主機板/SE 還原失效 ⇒ 金鑰永久遺失），而備份路徑又回到 ADR-0245 §2 的同一張力 | 研究記錄 |
| [0296](./0296-rotating-the-at-rest-key-does-not-buy-fs.md) | **評估：把輪替範圍擴到本機儲存金鑰＋預設開，能不能補齊 FS 缺口**（研究記錄）。**結論：不能，而且方向會反噬。** 提案指出的缺口是真的（`deriveStorageKey` ＝ `HKDF(nsec)`、**純由 nsec 決定無 salt 無輪替**，nsec 一洩過去所有本機歷史都可解；ADR-0245 自己就把這寫進殘餘風險）——**但問題不在有沒有看到缺口，在於輪替能不能補它**。**⚠ 核心是傳輸與本機的不對稱**：傳輸密文在對手手上、你不需要再讀它 ⇒ 扔掉金鑰零損失，FS 成立；本機密文在你手上、**你必須讀得到那就是你的歷史** ⇒ 刪金鑰＝你自己也讀不到。於是輪替 at-rest 金鑰落進二選一——**路徑 A 重新加密（歷史保得住）**：新金鑰必須被某物包住才載得回，那只能是 nsec 導出（與今天相同、**收益為零**）／OS 金鑰庫／使用者密碼，**後兩者的收益來自金鑰庫或密碼、不是來自輪替**；**路徑 B 不重新加密、舊鑰銷毀（真有 FS）**：那不是加密輪替、**是刪除資料**繞了一圈，而刪資料的機制（ADR-0126 保留上限／0013 限時／0234 無痕收回）本來就更直接。**⚠ 且路徑 B 會毀掉 ADR-0295 §3.2 剛查出、我們唯一在 FS 這格贏對手的地方**（0xchat 的 Secret Chat 換裝置就失去歷史、Marmot 新裝置解不開加入前 epoch，而我們 at-rest 與傳輸金鑰分離故兩者兼得）⇒ 走 B ＝ 主動把自己變成 0xchat，走 A ＝ 保住優勢但沒拿到 FS。**「預設開」另有三個新成本＋一個反噬**：裝回 ADR-0245 特地移除的 grace 失敗與背景 churn；**每次輪替要重新加密整個本機庫**（路徑 A 必然），從罕見的使用者動作變成每台裝置的週期性背景重寫、**中途失敗有新舊混雜風險**；**相容性懸崖**（TOFU 釘選會讓所有人上線即釘、舊版聯絡人一律觸發警告）；**反噬**——FS 現在卡的是外部審計（要錢），**擴大範圍等於把待審的東西變大變複雜、報價更高時程更長**，而 ADR-0290 §2 的建議正好相反（縮小審計範圍）。**對補齊 FS 缺口沒有一格是補齊的**（群組完全沒碰——ADR-0245 範圍是僅 1:1；vs Keychat 仍是 epoch 粗粒度且不輪替地址）。建議改走**裝置綁定金鑰**（OS 金鑰庫/Secure Enclave 的不可匯出金鑰包住 at-rest 金鑰，直接打中「複製磁碟≠拿到金鑰」）＋**真的刪資料**，兩者都比輪替直接；**但 §7-1 未做可行性評估，瀏覽器版可能根本做不到** | 研究記錄 |
| [0295](./0295-post-phase-p-gap-vs-0xchat-and-keychat.md) | **修完 Phase P 之後，我們與 0xchat／Keychat 還差什麼**（研究記錄；clone 兩家 repo 讀原文——ADR-0292 §5 曾明載「Keychat 完全未查證」）。**一句話結論：修完 Phase P 幾乎不會改變任何競爭差異**——P1–P4 修的是只有我們有的 bug，P5 是唯一動到競爭軸的且只補一半。**⚠ §0 更正 ADR-0289 的錯誤主張**：「0xchat 尚未有通話」是**錯的**（`- [x] P2P audio/video calling` 是打勾的，NIP-100 也列了，`contacts+calling.dart` 有 offer/answer/candidate 信令）；誤判來源是**網頁摘要把「planned features」標題當成整段未完成**——教訓：二手摘要不足以支撐競爭主張。**但同一份清單真正沒勾的是 `Desktop&Tablets versions` ⇒ 0xchat 沒有桌面版**，那才是真差異。**P5 只補一半**：ADR-0237 的洩漏在發布側（連線 AUTH 成我）與接收側（`#p` 穩定且收件匣必須 AUTH），P5 修前者、修不了後者；**Keychat 兩側都沒問題**——收件地址就是 Double Ratchet 的新 DH 公鑰、近乎每則訊息換一次（其代價寫在同段：要繼續監聽舊地址一段時間＝他們的 grace）。**⚠ 與 0xchat 的 FS 形狀幾乎一樣但他們付了我們刻意避開的代價**：其 `secretChat.md`（Beta）用 NIP-101 別名金鑰＋週期性更新＝粗粒度輪替，與我方 ADR-0245 同形；但明文寫 **「If the app is uninstalled or the device is changed, the secure chat history becomes irretrievable」**——而我們把 at-rest 金鑰（nsec 導出）與傳輸金鑰分開，**刪 EK 得 FS 的同時歷史仍還原得到** ⇒ 這一格設計上贏 0xchat 與 Marmot 兩家，**但我們沒上線（審計未過）、他們至少 Beta 可用；設計較好、交付較慢要一起講才誠實**。**Keychat 較強的一項**：Signal Double Ratchet ⇒ 每則一把金鑰、FS＋PCS 俱全，元資料輪替是加密機制的副產品；**但其協定草案全文沒出現 "device" 一字**（多裝置正是棘輪的殺手級問題）——**這是「文件未提」不是「不支援」，不可當競爭主張**。仍落後：群組 FS/PCS、1:1 FS 強度與元資料、FS 卡審計、無推播、零網路效應。仍領先/不同：E2E＋自架＋企業身分交集、**開了 FS 也不失去歷史**、桌面版（0xchat 無）、無公開社交圖譜/無金流。**未實機使用、未讀完整原始碼**；「Phase P 不影響競爭」只成立於「對手沒有更嚴重的同類問題」這個未驗證前提 | 研究記錄 |
| [0294](./0294-self-review-driven-by-competitor-teardown.md) | **以對手拆解為線索的自我審查：三個已證實的缺陷**（研究記錄＋特徵化測試；ADR-0293〈後續行動〉的執行）。**ADR-0288 的兩個疑慮從「靜態閱讀的推測」升級為「測試證實」**。⚠ **過程本身值得記：前兩次嘗試得到錯誤結論**——第一版測試「重開後沒重收」，我一度判斷 ADR-0288 是錯的，實際原因是測試環境兩個預設值（`createInMemoryRelayNetwork` **預設整類拒收檔案塊**、且**預設沒有離線留言庫**）⇒ **「測不出來」不等於「沒有問題」**；兩個預設值都是對的設計，但讓測試預設跑在**不存在於產線的組態**上。**缺陷 A（已證實）**：重開 App 會把 TTL 內收過的檔案再交付一次，對照組同時證實訊息路徑不會（有 `inboxSince` 水位）——13 組訂閱 filter 裡只有私訊帶水位，其餘要嘛 ephemeral 要嘛可取代/可尋址，**唯一「會累積的儲存型 kind」除私訊就是 FILE_WRAP 而它沒水位**；桌面 `onFileBytes` 內是無條件 `saveIncomingFile` ⇒ 每次重開 App 都會再跳一次「另存新檔」。**缺陷 B（已證實）**：缺一塊 → `onFileBytes` 從不觸發、**`onFileError` 也從不觸發**、殘骸 120 秒後靜默刪除 ⇒ 寄件者看到「已送出」、收件者什麼都沒有、雙方都不知道。**新增的 3 個測試是特徵化測試、斷言的是錯誤行為**，修好後會紅，屆時應把斷言翻正而非刪除。**ADR-0293 §2.2 的類比也成立**：桌面換身分走 `location.reload()`＝**結構性保證**，行動端是就地切換＋**手寫 reset 清單**＝靠人維護，而該清單目前漏了 `archived`（歷史入口的閘門——兩身分若都與同一 pubkey 對話會看到幽靈「歷史」入口）、`purged`、`calDraft`（**本 session 新增、當場就漏了**）⇒ 重點不是這三項的嚴重度，是**沒有任何機制保證下一個新增的 state 會被記得加**；建議把 per-identity 狀態關進以身分為 `key` 的子元件（reset 清單可整個刪掉），而非補三行。**改善優先序**：檔案塊補水位＋重組失敗要報錯（小）→ 把重取策略變成訂閱工作負載的屬性（治本，否則下一個累積型 kind 又重蹈覆轍）→ 行動端 per-identity 子元件 → 評估匿名發布 plane。**未修改任何產品程式碼**；§1.3 未真的產生 1024+ 顆事件（以「缺一塊」模擬）、「3 個滿版檔觸發」的門檻**未端到端驗證** | 研究記錄 |
| [0293](./0293-whitenoise-implementation-teardown.md) | **White Noise 實作拆解：他們踩過的坑，有幾個我們形狀相同**（研究記錄；補上 ADR-0292 自列的限制「只讀規格沒讀實作」——clone whitenoise-rs v0.2.1 讀其 docs 與 manifest）。**最有價值的不是他們做對什麼，是他們的兩份 re-architecture 文件寫著踩到的坑，而其中兩個我們形狀一模一樣、只是還沒痛到寫文件。****架構取捨**：他們**不自己實作 MLS**（用 marmot 的 `mdk-core`），核心是 Rust `cdylib` 走**原生 FFI 給 Flutter＝沒有網頁版** ⇒ ADR-0290 §3 說「MLS 成熟實作在 Rust」是對的但漏了下半句：**對手是靠放棄網頁版換到 Rust 的**，所以照抄他們的技術選擇不成立。**⚠ 坑一：一個共用 client 扮演五種角色**（discovery／group listener／authenticated inbox／**anonymous publisher**／transient query，「those workloads need different relay sets, connection lifetimes, authentication rules, retry policies」）——**我們一模一樣**：`this.client` 一條連線兼發布與訂閱、開機一次 `subscribe("all", …)` 裡有 **13 組 kinds**（presence／收件匣／快照／信令／檔案塊／名冊全混在一起）⇒ **ADR-0288 的「檔案塊 filter 沒水位而私訊 filter 有」不是孤立疏漏，是這個結構的必然症狀**；治本是把重取策略當成 plane 的屬性，不是每個 filter 各自記得加。**⚠ 坑二：帳號範圍是慣例不是型別保證**——他們已因此出過 bug（`message_delivery_status` 的 sender-local state 跨帳號滲漏），我們的 ADR-0045 命名空間前綴同樣是慣例（**本文只做類比、未查證我們是否有同類 bug**）。**⚠ 最可行動的發現：「匿名發布」應是獨立 plane**——他們明寫「Relay auth becomes global even though only some relay classes should auth」，這直接對上 ADR-0237 並指出洩漏發生在**發布側**：wrap 由一次性金鑰簽是匿名的，但**發布走 AUTH 成本人的那條連線**，而我們的中繼 `requireAuth` 時**連 EVENT 都要 AUTH**（`relay-core.ts:328`）⇒ 中繼知道「這顆匿名 wrap 是 Alice 送的」。拆開即可修掉一半（REQ 保留 AUTH 因收件匣必須只給本人、EVENT 不要求，反濫用退回既有 NIP-13 PoW＋速率）。**但有張力且說明這不是疏忽**：gift wrap 的 author 是一次性金鑰、對企業 allowlist（ADR-0044）完全沒用，**AUTH 身分很可能正是 allowlist 唯一的執行點** ⇒ 只能分部署（公共節點可拿掉換得寄件者不可連結；企業節點保留，且洩漏對象是自己的組織）——與 ADR-0290 §1 推播的分界線相同。**佐證**：他們的媒體層有 `BlossomClient` ⇒ 支持 ADR-0288 把 NIP-B7 升到第一。順帶：他們有 `SECURITY.md` 逐條記錄刻意忽略的 cargo audit advisory（component/justification/risk/mitigation），本專案無等價文件、企業銷售會被要 | 研究記錄 |
| [0292](./0292-how-competitors-pay-the-fs-convenience-cost.md) | **對手怎麼處理「FS vs 方便性」——他們沒有免費解掉，是把代價換了位置**（研究記錄；**直接讀 Marmot 規格原文**而非二手報導）：**同樣的代價他們明碼標價成可調參數**——「離線太久解不開」他們也有，只是以 epoch 計（`app_payload_past_epoch_limit = 5`，`retained-history.md` 明文「MUST expire」），群組越活躍該窗在時間上越短；而**我們的 grace 只在按下換鑰後才存在，他們的是常態運作的一部分**。**⚠ ADR-0091 的爆炸半徑顧慮沒有被推翻，反而被對手規格證實**——`convergence.md` 有一個具名狀態 **`Unrecoverable`**（retained state 缺失且落在 rollback horizon 內即進入，需 verified repair path），commit 本身也會 epoch-based 過期（`max_rewind_commits = 5`）。他們還**明說**保留量與 FS 是同一根拉桿（「Keeping less history … improves forward secrecy guarantees」）＝我們在 ADR-0245 §2 記過的同一張力，沒有人繞過它。**⚠ 兩項他們直接宣告不解**：`features/multi-device.md` 是 **branch draft**（「MUST NOT be implemented for interop yet」）且**「History synchronization is out of scope. A newly added device cannot decrypt epochs before it joined.」** ⇒ **這一格我們結構性領先**——ADR-0236 已釐清耐久歷史來自本機加密封存＋加密快照、at-rest 金鑰由 nsec 導出**與傳輸金鑰兩回事**，故我們刪 EK 得 FS 的同時新裝置仍還原完整歷史。**但兩項要誠實承認對手較強**：亂序/遲到有 `deferred input` 保留與續取（我們無等價物——而 ADR-0288 的檔案塊靜默截斷正是缺這個能力的症狀）、多設備用 per-device MLS leaf 結構上解掉我們「共用 nsec」的根因（但仍 draft 且無歷史）。**附帶發現：他們的推播設計比 ADR-0290 §1 我建議的形狀更好**——裝置把平台 token 加密給**通知伺服器的 Nostr 公鑰**、密文夾在一般加密群組訊息裡 gossip（kind 447/448/449），**中繼完全不必參與**，由寄件者觸發通知伺服器，該伺服器解得開 token 卻不必知道收件人的 Nostr 身分 ⇒ ADR-0290 §1 結論不變但做法應改。**Keychat 完全未查證**；只讀規格未讀 White Noise 實作；multi-device 與 push 兩份規格皆標 draft、引用有時效 | 研究記錄 |
| [0291](./0291-forward-secrecy-convenience-reexamined.md) | **重新審查「前向保密犧牲方便性」**（研究記錄）：**這個考量針對的是「自動連續 FS」（ADR-0238），而它已被 ADR-0245 處理掉了——那份 ADR 就是為回應它而寫的**，其〈後果〉自述「方便性顧慮化解」。原意見的四項具體代價逐項對照：離線>grace 訊息解不開（grace 縮到 7 天`FS_GRACE_MS`**且輪替改手動** ⇒ 只在你按下換鑰後才有這個窗，不再是背景風險）、背景金鑰 churn（**消除**，免 epoch 排程）、快照/備份持續老化（**消除**——備份碼身分-only、一次存終身，且順帶讓 FS 更強因為備份永不含 EK）、default-on 打擾（**消除**，預設關 opt-in）。**殘餘代價仍在但全被 opt-in 閘住**（grace 後送舊鑰的在途訊息接不到且「誰還沒學到」不可確知故只能靠時間兜底、全裝置遺失救不回未落地在途訊息、新裝置需一台活著的舊裝置同步 EK、舊版客戶端降級、換鑰要看一個誠實但嚇人的彈窗）——**每一條都要「開了 FS ＋ 按了換鑰」才發生，未啟用者成本為零** ⇒ 已不是「要不要做」而是「開了之後 UI 怎麼講」。**⚠ 但 MLS 的可靠性代價是另一回事且仍然成立**：ADR-0091 的第一顆砝碼不是「使用者覺得麻煩」而是**失效爆炸半徑**（靜態方案每則 Gift Wrap 自足、掉一則只掉一則；MLS 掉/亂序/過期一個 commit 可能讓後續整段對話解不開），而**本季新證據讓它更站得住**——ADR-0288 查出交付層現就有兩個未修尖角（檔案塊 1024 靜默截斷、訂閱無水位），**在會靜默掉東西的交付層上疊有狀態密碼學，風險比當初評估時更高**。**⚠ 我們的方便性代價天生比對手高是架構差異不是怕麻煩**：多設備共用同一把 nsec（ADR-0107），對手把多設備當 MLS 群組成員、per-device 身分內建；我們要 per-message FS 得先做 per-device 身分，那比密碼學本身更深。**⚠ 且方便性已不是綁住 FS 的那條繩子**——ADR-0245 Phase 0–2 已實作全測綠，卡的是外部審計（ADR-0290 §2），就算今天判定方便性完全不是問題也還是不能上線。**已知限制：「已化解」是設計層面結論，從未有真實使用者用過（預設關＋審計未過不得啟用），手動輪替夠不夠方便沒有任何實證** | 研究記錄 |
| [0290](./0290-competitive-gaps-and-catch-up-paths.md) | **相對他牌的落後項與追趕路徑**（研究記錄；展開 ADR-0289 §5，落差查實＋帶成本的選項）：**⚠ 最傷的是行動端完全沒有推播**（`native/notify.ts` 直接用 `webNotifier`＝App 關掉就收不到），而 White Noise 已出貨 iOS 推播（NSE 在裝置上解密，Apple 只看到「有一則投遞」）。**ADR-0116 的否決理由需要下修**——「洩漏給推播供應商」在種類上對、程度上被高估，且遺漏一個對照：**中繼站早就知道了**（ADR-0237 已自認每條連線都 AUTH ⇒ 接收端中繼能把「誰來拿信」連上身分），故**自架情境下推播與中繼是同一信任域、增量洩漏≈0**；ADR-0116 說「中繼要在不知道收件人是誰的前提下觸發推播（可能根本做不到）」也把一個既有事實（`#p` 本來就是收件匣的定位方式）當成新增洩漏 ⇒ 建議改為分情境（自架可做／公共需明示同意）。**真正的阻擋不是隱私爭議，是還沒移植到真 RN**（NSE 是原生元件，react-native-web 拿不到）。**1:1 FS 卡的是錢不是程式**：ADR-0245 Phase 0–2 已實作全測綠，只剩外部密碼學審計，而 ADR-0257 的結論是沒有營收面 ⇒ 已完工的競爭關鍵功能因非工程閘門無法上線，對手卻都能宣稱 FS；路徑是縮小審計範圍（只審 core 那幾百行）或申請開源資助。**群組無 FS/PCS 的真正阻擋是語言**——White Noise 把密碼學放 Rust（openmls 生態），我們的引擎是 TS，成熟 MLS 實作在 TS 沒有同級品；建議先把 ADR-0245 的輪替子鑰擴到群組（粗粒度 FS、**沒有 PCS，不可宣稱與 MLS 對等**），MLS 另案。**⚠ 網路效應：ADR-0061 的二分框架遮住了一條中間路**——查證發現我們的 1:1 訊息**就是標準 NIP-17**（kind 14 包 1059），與規格差異**只有一個 tag**（我們 `["to",pk]`、規格 `["p",pk]`），且陌生人來訊已有訊息請求區（ADR-0121）⇒「讓任何 Nostr 客戶端能寄私訊給我們」的協定成本約等於**收端多接受 `p` 作為 `to` 的別名**，且不違反 ADR-0002（DM 本來就加密）；真正刻意的不對稱在**出站可被發現性**（profile 是加密廣播給聯絡人，通用客戶端搜不到我們），那應維持。**元資料建議不追**（Keychat 地址輪替更強，但要追平得動到收件匣模型本身，連帶衝擊離線投遞/多裝置/補送/NIP-62；且自架幾乎讓此威脅消失）——改為主動標明適用範圍。所有成本皆為量級判斷、**未估工**；§5 未與任何真實客戶端實測 | 研究記錄 |
| [0289](./0289-nostr-native-competitor-landscape.md) | **Nostr 生態內的同類產品盤點**（研究記錄）：回答倉庫裡完全沒答過的問題——ADR-0254 的比較頁只比 Signal／LINE／WhatsApp，**沒有任何 Nostr 生態內的對手**，而懂 Nostr 的評估者第一個問的就是「你跟 0xchat／White Noise 差在哪」。四類：**0xchat**（最接近的同類，28 NIP＋NIP-17/EE，互通是它的賣點）、**White Noise/Marmot**（MLS 群組 E2EE，Rust＋Flutter，建構於 Nostr＋Blossom＋MLS）、**Keychat**（Signal/MLS 跑在 Nostr 傳輸上，**近乎每則訊息輪替地址**＋ecash 付費投遞）、**Buzz by Block**（2026-07-21 發布，自架 Nostr relay＋Tauri，對標 Slack＋GitHub，Apache-2.0，**但不是 E2E**）。差異分軸：**互通性是最大的結構差異且雙刃**——不互通才可能全部進 Gift Wrap（無公開 profile/社交圖譜），代價是**零生態網路效應**（對手換 App 還是同一身分同一批聯絡人，我們換 App 從零開始，必須誠實列）；**群組加密我們落後且對手已出貨**——成對扇出 O(N)、群組無 FS 無 PCS（ADR-0091 暫緩 MLS、0236 明載延後），White Noise 有 MLS 內建兩者；**元資料我們不是最強**——ADR-0237 已自認每條連線都 AUTH 讓接收端中繼能把「誰來拿信」連上身分、事件層被連線層打穿，Keychat 的地址輪替更強；**企業與自架是我們最有話講的一軸**——「E2E＋可自架＋管理者簽章名冊/邀請碼/離職接管」這個交集相當孤單（Buzz 佔了自架與企業但放掉 E2E，其餘三家有 E2E 但沒企業面）。**⚠ Buzz 更新了 ADR-0257 的競爭前提**（該 ADR 論證 B2B 是唯一毛利路徑時，沒把任何 Nostr 生態內企業級對手納入）：不推翻結論但差異化**必須壓在 E2E 上**，且**若 Buzz 補上已列為未來考慮的 NIP-44 E2E，該軸優勢就消失**。**⚠ White Noise ＝ 我們自己路線圖指向的規格的出貨實作**——ADR-0259 §1.3 查到 Marmot 時沒說的那半句是它有個在出貨的實作、而那是直接對手；好處是不必當第一個踩坑的，代價是「對手做到了」成為重評 MLS 時的新反證。**不建議放上官網**（替對手曝光，且落差在行銷語境會被斷章取義）——讀者是內部決策與企業客戶技術問答。全部為公開資料閱讀、**未實機驗證任何一款** | 研究記錄 |
| [0288](./0288-nip-survey-round-two.md) | **NIP 適用性盤點・第二輪**（研究記錄；補漏＋查證＋重新排序）：**修正 ADR-0259 自己的錯**——它自稱盤點「全部 NIP」但**漏了 11 個**（05/14/21/24/27/36/37/39/48/58/98），原因可還原：Tier 3 是**按主題成群**否決的（一列掃掉 15 個），成群處理時沒回頭對索引核帳——**分類做得越漂亮，越容易忘記驗證聯集等於全集**。補評全數否決，兩個例外值得記：**NIP-98 不是候選是相依**（NIP-86 與 Blossom 都以它為授權層，估工要含）；**NIP-05 否決但它指到的缺口是真的**——組織內身分有名冊擔保（ADR-0047 管理者簽章、客戶端可驗），**跨組織沒有**，此時的「企業自報頭銜」（ADR-0158/0170）如其名是自報的；否決 NIP-05 是因為它的代價是**公開員工名錄**，若要補缺口應比照 ADR-0035 走帶內加密的**單張跨組織憑證**（管理者簽、對方驗、無公開端點）。**查證 ADR-0259 後續行動 #4**（第一輪只提疑慮未查）：疑慮成立但形狀不同——`FILE_CHUNK_MAX_TOTAL=360` 表示**單檔永遠撞不到 `MAX_QUERY_ROWS=1024`**，且查詢是 `created_at DESC` 故**最新的檔案永遠完整**；真正條件是**離線期間收到 3 個滿版 16MB 檔**（1050>1024）→ 最舊那個缺塊 → `sweepChunkAsm` 120 秒後刪除、**無 `onFileError`、雙方都不知道**（寄件者看到「已送出」＝ADR-0041 只保證中繼收下）。**順手查到一個更容易踩的**：檔案塊訂閱**沒有 `since`**（同一個 subscribe 裡離線私訊那條有水位，只有它沒有）＋中繼不因送達而刪＋`seenEvt` 純記憶體不持久化＋桌面 `onFileBytes` 無條件呼叫 `saveIncomingFile` ⇒ **每次重開 App，7 天內收過的每個 relay 檔案都會重新下載、重組並再跳一次「另存新檔」**，日常就會發生。**⚠ 兩者都不是 NIP-67 能解的**——NIP-67 只讓你**知道**還有更多、不給你缺的那幾塊，且完全不觸及第二個問題；共同根因是**缺一個水位**，補 `since` 兩個一起解且零新協定面 ⇒ **NIP-67 從 Tier 2 之首降到末位**，**NIP-B7 Blossom 升為第一**（blob 以 SHA-256 定址、與 relay 解耦，配額/查詢上限/水位/重組逾時這四者的耦合根本不存在）。本文只查證不修改程式；兩個結論皆靜態閱讀得到、**未寫測試重現**，動手前應先寫會失敗的測試 | 研究記錄 |
| [0287](./0287-mobile-composer-no-horizontal-overflow.md) | **行動端對話畫面不得橫向溢出**：實機回報「對話開啟後畫面比螢幕寬，要往右滑才看得到送出鈕」。ADR-0284 修過安全區與鍵盤但那兩項不影響寬度；真兇在輸入列——5 顆圖示（📎📷🗄😊⏱，各約 34px＝170）＋6 個 `gap: 8`（48）＋容器 padding（16）＋送出鈕（約 60）＋輸入框（**約 173**）＝**約 467px**，而手機是 360–412px。關鍵在最後一項：`TextInput` 在 react-native-web 下就是 HTML `<input>`，**有約 173px 的內建寬度（`size` 屬性）且 `min-width: auto` 讓它拒絕收縮**——`flex: 1` 只給了「可以長大」，沒給「可以縮到內容以下」。修法三項缺一不可：`flexWrap: "wrap"`（塞不下就折行，不撐爆畫面）、輸入框 `minWidth: 160`（解除拒縮，同時保證折行後仍打得了字）、送出鈕 `flexShrink: 0`（被擠掉就沒有主要動作了）。標頭同類問題一併修：`headText` 加 `minWidth: 0`、對話名稱 `numberOfLines={1}`，否則長名字同樣撐寬。**不把圖示藏進「＋」選單**——那是更大的 UX 變動且現有測試對「提供 `onSendPhoto` → 📷 出現」有明確斷言，折行已足夠。測試用**探針法**：RNW 把版面編成 class（`r-flexWrap-…`）、hash 是內部實作寫死會假性失敗，故先渲染帶目標樣式的 `View` 取其 class 再比對；且斷言**指名道姓**到 testID——`flexWrap: "wrap"` 在該畫面另有 6 處使用，掃整份 HTML 會誤判通過。寫完把修正暫時拿掉跑過一次、確認三條斷言真的會紅。代價：窄螢幕上輸入列變兩行，佔掉一點訊息區高度（選了「全部功能可見」而非「單行緊湊」） | 已接受 |
| [0286](./0286-mobile-request-badge.md) | **行動端聯絡人分頁的好友請求徽章**：ADR-0285 修掉桌面三欄「完全沒有請求區」後被追問「APK 收得到邀請嗎？」。查下來與桌面**不同**——行動端接線完整：`MobileApp` 有 `requests` state 並訂了 `onRequests`，傳給 `ContactListScreen`（含 accept／decline／clear），該畫面確實渲染請求區且已有測試涵蓋。所以 APK **收得到也顯示得出來**。但有實用缺口：**請求在「聯絡人」分頁，而底部分頁列只有「聊天」分頁有徽章**——停在聊天分頁的人沒有任何提示，不會切過去看，結果與 ADR-0285 那個 bug 的使用者體感一模一樣（對方接受前 ADR-0121 不回送個人檔 → 雙方名字停在 `npub1abc…` → 像「加好友沒有用」）；差別只在桌面是**真的沒有 UI**，行動端是**有 UI 但沒人知道要去看**。決策：`BottomTabs` 徽章由「只有聊天分頁」擴為依分頁決定計數來源——聊天＝未讀總數、聯絡人＝待處理請求數、設定＝無；沿用既有紅色徽章與 `99+` 上限，不新增視覺語彙。桌面不需對應改動（請求區在側欄常駐可見）。代價：兩個紅點同時出現時視覺較吵，且徽章不區分請求與未讀的緊急程度 | 已接受 |
| [0285](./0285-deck-sidebar-contact-requests.md) | **三欄佈局補上訊息請求區與已封鎖名單**：實機回報「從行動端加對方好友後，**對方那側完全沒看到邀請**」。成因是 `App.tsx` 的漏接——`<DeckSidebar … {...addContactProps} />` 但 `<ContactListWindow … {...addContactProps} {...manageProps} />`，而 `requests`／`onAcceptRequest`／`onDeclineRequest`／`onOpenRequest`／`onClearRequests` **全在 `manageProps` 裡**；三欄版從未拿到，`DeckSidebar` 也就從來沒有請求區（`grep -c request` 為 **0**）。後果比「少一個區塊」嚴重：引擎確實收到、確實寫進請求區、確實 emit，但使用者在三欄佈局下看不到任何東西 → 永遠不會按接受 → ADR-0121 規定「接受之前不回送個人檔」→ 雙方顯示名稱都停在 `npub1abc…`，體感是「加好友沒有用」。這正是 ADR-0284 那份回報的另一半。修法：`DeckSidebar` 補請求區並讓 `App.tsx` 傳 `manageProps`；結構與 class 與經典版**完全一致**（共用 `msn.css`，不為三欄版另寫外觀），放在名冊**之前**（需要裁示的東西不該被埋在清單裡），四個操作對齊（預覽只開窗不送已讀回條／接受／拒絕／封鎖），兩筆以上才顯示「全部刪除」（ADR-0127 是防洪不是單筆操作）。**同源稽核**：既然成因是整包 props 沒傳，就把 `manageProps` 每個鍵都對一次——還漏了 `blocked`／`onUnblockContact`，代表三欄版**封鎖之後沒有任何地方解得開**，一併補上。殘留：這類「兩個佈局各接一半 props」型別擋不住（全選填），已補測試斷言，但 `addContactProps` 等其他物件未逐一稽核 | 已接受 |
| [0284](./0284-qr-self-reported-name-and-mobile-viewport.md) | **QR 帶自報顯示名稱，與行動端版面的安全區／鍵盤修正**：實機回報兩件事。其一，**加完好友只看到 `npub1abc…`**——這不是壞掉，是 ADR-0121 的既有設計且有測試寫死：A 加 B 後 B 的請求區會看到「Alice」，但 **B 在接受之前不回送自己的個人檔**（對還沒接受的陌生人回送＝向他確認「這把金鑰是活的」，正是垃圾訊息發送者要的回饋），所以 A 只有 npub 縮寫。這條紅線不動；但**當面掃 QR 是另一回事**——對方把 QR 亮給你就是自願給名字，寫進 QR 完全不需要協定層對陌生人回饋。格式擴充為 `npub…@wss://… #顯示名稱`（提示可省）：**名稱一律以 `#` 標記、不能只靠位置**——第一版寫成「第三段起算」，沒有中繼提示時 `npub… 小明` 只有兩段而把「小明」當成提示，測試當場抓到。`addContact` 以 `name` 取代 npub 佔位，對方接受後回送的個人檔仍覆寫它（ADR-0061 路徑不變）；**只進 QR 不進複製的純文字**（複製可能被貼到公開場合），桌面與行動端兩端 QR 都帶（同走 `formatContactInput`）。舊版相容：`split(sep,2)` 帶提示時丟掉 `#名字`、不帶提示時 `#名字` 落到 hint 但 `normalizeRelayUrl` 認不得而丟棄，都不污染路由。其二，**版面尺寸不合手機**——ADR-0277 加的 `viewport-fit=cover` 會讓版面延伸到瀏海與手勢列底下，**必須自己補 `env(safe-area-inset-*)` 留白**而當時沒補，標頭鑽進狀態列、底部分頁列被手勢列蓋住；另加 `interactive-widget=resizes-content`（Android WebView 軟鍵盤預設只覆蓋畫面不縮版面 → 輸入列被壓在鍵盤底下）與 `body { overflow: hidden }`（內層 ScrollView 自己捲，整頁不再雙層捲動）。殘留：自報名稱無法驗證（但與「接受後回送的個人檔」是同一種自報、並不更弱，且當面掃碼看得到人） | 已接受 |
| [0283](./0283-mobile-avatar-and-status-colors.md) | **狀態列可換頭像，狀態鈕改用各自的狀態色**：ADR-0278 加了聊天頁頂部狀態列後的兩個問題。其一，換頭像只有「設定」分頁能做，而狀態列已把頭像畫在最顯眼的位置卻**不能點**——反而讓人以為壞了或找不到入口；改為 `Pressable`：點擊→`pickAvatarImage()`（縮到 128px）→`onAvatar` 廣播，走**與設定分頁同一個** `changeAvatar` 不另開路徑；取消選圖（回 `null`）＝什麼都不做、**不清掉現有頭像**；未提供 `onAvatar`（示範後端無廣播對象）＝退化成純顯示並 `disabled`；沿用既有 `avatar_change` 作無障礙標籤。其二，狀態鈕選中時一律填**主色**（藍），但整個 App 對「狀態」的既有視覺語言是 `STATUS_COLORS`（聯絡人清單狀態點、桌面頭像圈都是綠／黃／紅）——狀態選擇器反而是唯一不用這組色的地方，使用者得讀文字才知道選了什麼；改為每顆鈕帶該狀態色圓點＋同色邊框，選中時以狀態色填底。**字色由對比決定而非一律白字**：「離開」的琥珀 `#f2b134` 上白字對比僅約 2.1:1，改用 ADR-0250 已引入的 `contrastRatio()` 在白與深墨間挑高的那個（既有工具的正當用途，不需新機制）；選中時色點改用字色，否則與底色糊成一塊。代價：「忙碌」選中是紅色實心鈕、視覺偏強可能被誤讀為錯誤狀態（以色點與文字並存緩解，是語義色用在互動元件上的固有代價）；鈕變寬、窄螢幕更易換行（`flexWrap` 已開，折行不截斷） | 已接受 |
| [0282](./0282-mobile-scan-icon-button.md) | **行動端掃描鈕改為圖示鈕，動作圖示幾何進 theme**：ADR-0280 的掃描入口是一顆整列寬的文字鈕，單獨佔一行擺在面板中段——但面板已有「複製」「顯示 QR」兩顆文字鈕，三顆長得一樣卻做的事差很多；且掃描其實是「**另一種輸入方式**」，跟下面「貼上 npub」的輸入欄是同一件事的兩條路，卻被拆到不同列。決策：(1) 掃描改為圖示鈕並**移到貼上欄同列**（`[貼上 npub…][⛶][新增]`）；圖示鈕沒有文字標籤，故**必須帶 `aria-label`**（用既有 `qr_scan` 文案）——沒有文字的按鈕對讀螢幕者等於一塊空白，有紅字測試鎖住；(2) 圖示幾何放 `@cinderous/theme` 的 `ACTION_ICONS`，比照 ADR-0095 `MSG_STATUS_ICONS` 的先例——**幾何與規格放 theme、渲染各前端自己做**（行動端 `ActionIcon` 用 react-native-svg，與 `MsgStatusIcon` 同一種分工），桌面日後要用不必再畫一次。圖案是**四個取景角＋中央掃描線**，刻意不畫成完整 QR 方格——那與旁邊「顯示我的 QR」看起來太像；取景框＋掃描線是相機取景的通用語彙。代價：圖示無文字，初次見到可能要按一下才知道是什麼（以 aria-label 與通用語彙緩解，但確是文字換空間的取捨）；掃描中預覽仍是獨立一區、版面會跳動一次（可接受，掃描是短暫模態） | 已接受 |
| [0281](./0281-shared-contact-input-parsing.md) | **加好友輸入的解析上移共用，與 i18n 佔位符漏傳修正**：ADR-0280 交付 APK 後的兩個實機回報。其一，**掃桌面版 QR 被回「掃到的內容不是 npub」**——桌面「我的 QR」編的不是裸 npub 而是 `selfShareUri`＝`npub…@wss://…`（帶 home relay，供 ADR-0034 多中繼路由）；後端 `addContact` 本來就吃這格式（自己 `split(/[@\s]+/, 2)`），但 ADR-0280 的掃描驗證另寫一份**只認裸 npub** 的 `npubDecode`，於是「後端加得進去的東西，掃描端擋在門外」——不是驗證太嚴，是同一輸入格式兩處各寫一套規則。修法：切分上移 `packages/core/src/contact-input.ts`（`parseContactInput` 只切分、`isContactInput` 供 UI 把關，裸 npub 與 `npub@relay` 都收），後端與行動端掃描**都改吃這一份**；行動端 QR 也改出示 `selfShareUri`（掃到的人才拿得到中繼提示，無 home relay 退回裸 npub）。其二，**解鎖畫面印出字面「歡迎回來，{name}」**——`unlock_title` 帶佔位符、插值靠 `translate` 第三引數，桌面有傳、行動端漏傳，而參數選填故 TS 擋不住；補傳後名字會與下方獨立一行重複，故移除該行（與桌面同一呈現）。另掃過全庫：45 個帶佔位符的鍵中，其餘只有桌面 `vanish_sent` 以手動 `.replace("{n}", …)` 處理（功能正確但繞過正規 API），一併改用 `t(key, params)` 免得被照抄。殘留：型別無法強制「帶佔位符的鍵必須傳參數」，目前靠一次性掃描與個別測試 | 已接受 |
| [0280](./0280-mobile-qr-add-contact.md) | **行動端以 QR 加好友（出示／複製／掃描），QR 產生上移 core**：行動端加好友只有「貼上 npub」一條路，而自己的 npub 在面板裡只是**一行不能點的文字**——要給朋友得手動選取 63 字 bech32。桌面早有 QR 顯示，`apps/desktop/src/qr.ts` 檔頭甚至寫明「掃描（相機解碼）在行動端（Phase D）」，行動端從未補上這半邊。決策：(1) `qr.ts`（`makeQr`/`qrSvg`/`qrDataUri`）與 `qrcode-generator` 相依自桌面**上移 `packages/core`**，桌面三個使用點改吃 `@cinderous/core`——複製第二份編碼器是 SSOT 的反面；(2) 加好友區塊抽成 `AddContactPanel`，含出示 QR（預設收合，一開啟就佔半畫面太吵）、一鍵複製 npub（走既有 ADR-0135 剪貼簿縫）、掃描對方 QR；(3) 掃描用 Chromium 的 **`BarcodeDetector`**（Shape Detection API）而非原生外掛——零第三方相依、零原生程式碼、零 APK 體積，與 ADR-0274「能用瀏覽器 API 就不拉原生依賴」一致（`@capacitor/camera` 當初正因硬吃 Java 21 被移除），相機權限也不必新增（`CAMERA` 早因 ADR-0101 WebRTC 視訊就在）；**誠實的限界**：`BarcodeDetector` 不保證存在（iOS Safari 無、部分 Android 需 Play 服務條碼模組），`qrScanSupported()` 據實回報、**不支援就不顯示掃描鈕**，貼上那條永遠在——刻意不做「假裝支援、按了才發現一直轉圈」的降級；(4) 兩條安全細節：**掃到的內容先驗證是 npub 才送出**（QR 可以是網址、Wi-Fi 設定、別人的名片，不驗就把垃圾灌進 `addContact`）、**相機串流用完一定關**（取消／卸載／解碼例外三路徑都走 `finally`，鏡頭燈不熄是嚴重隱私觀感問題，有紅字斷言）。最大未知：`BarcodeDetector` 實際覆蓋率無實測資料；若多數裝置沒有，替代方案是純 JS 解碼器（jsQR 約 13KB gzip），`native/qr-scan.ts` 是唯一碰解碼處、換掉是單檔改動 | 已接受 |
| [0279](./0279-android-disable-system-backup.md) | **Android 預設關閉系統自動備份與裝置間轉移**：`allowBackup="true"` 是 `cap add android` 的**樣板預設值**，效果是把 app 私有目錄（`localStorage`、OPFS 封存、SharedPreferences）上傳到使用者的 Google Drive。落地資料本身有加密（ADR-0112），故非明文外洩，但立場不對——專案預設是明文與私鑰不離開裝置，而這是一條我們既不控制也不可見的外流路徑，使用者從未被告知或同意；且我們已有**兩條**自家搬移路徑（ADR-0118 配對搬家 P2P＋SAS、ADR-0071 加密雲端備份），系統備份無增益、只多開暴露面。決策：`allowBackup="false"` **＋** `dataExtractionRules`——兩者缺一不可，因為 Android 12（API 31）後 `allowBackup="false"` **只擋雲端備份、管不到 D2D 裝置間轉移**（換新機的「複製資料」），而本專案 targetSdk 35 一定走到該路徑，故 `res/xml/data_extraction_rules.xml` 的 `<cloud-backup>` 與 `<device-transfer>` 兩段都排除 `root`（涵蓋 file/database/sharedpref 與 WebView 存放處）與 `external`（不在 root 底下需各別排）。已驗證合併後 manifest 兩屬性未被外掛覆蓋。代價：**換新機時系統不會幫忙搬資料**（刻意取捨，但行動端目前沒有任何地方告知使用者這件事——待辦）；卸載重裝＝資料全失。`cap add android` 重跑會還原樣板預設，同 ADR-0274 類問題 | 已接受 |
| [0278](./0278-mobile-self-status-bar-and-brand-icon.md) | **行動端聊天頁的自己狀態列，與 Android 品牌啟動圖示**：ADR-0277 讓 APK 首開變成真 App 後實機看到兩件不對的事。其一，上線狀態與自訂狀態文字**只在設定分頁**，但改狀態是高頻操作——桌面版一直把它擺在聯絡人清單正上方（`DeckSidebar` 的 `dsb__me`）。新增 `SelfStatusBar`（頭像＋狀態點／名稱／狀態三選一／狀態文字）置於 `ChatsListScreen` 標題列之上，以 `self` prop 設閘（示範後端無 presence 可廣播）；狀態控制項抽成 `StatusSegments` 與 `SettingsScreen` **共用**——否則同一個開關會有兩套樣式與 testID 要各自維護（Fix-First 要避免的平行路徑）。**隱身時狀態點畫成離線**並加註：隱身（ADR-0164）＝對外完全不廣播，若照 `status` 畫成綠色「線上」等於 UI 在說謊，有紅字測試鎖住。狀態文字沿用 ADR-0171 節流語意，只是換位置顯示、不另開廣播路徑。其二，整套 `ic_launcher*` 都是 `cap add android` 的**樣板預設**（綠色機器人），而專案早有品牌圖 `cinderous-ember.svg`（深藍 `#0f1f3a` ＋橘色漸層火焰）在桌面版用。改為**全向量**：`drawable-v24/`（含漸層）＋`drawable/`（純色，漸層需 API 24）＋`mipmap-anydpi-v26/`（自適應）＋`mipmap/`（API 23–25 完整圖，`minSdkVersion` 是 23 而自適應要 26），刪掉各密度 PNG。前景 viewport 直接用原稿 1024 讓路徑原封搬移；安全區驗算：火焰下緣圓心 (512,640) 半徑 190 → 距中心 318 < 338（66% 安全圈），尖端距中心 312 < 338，任何遮罩都裁不到。代價：清單頂部變高少約一列、API 23 無漸層、`cap add android` 重跑會還原 `res/`（同 ADR-0274 類問題，資源表即重建依據）。另記已發現但未處理：manifest 的 `android:allowBackup="true"` 是樣板預設，與隱私預設立場不一致，另立 ADR | 已接受 |
| [0277](./0277-mobile-first-run-entry.md) | **行動端正式進入點與「建立身分」為預設登入路徑**：兩個疊在一起、使用者一開 App 就撞到的問題。其一，`index.html` 載入的是 ADR-0085 的**開發預覽台** `preview.tsx`（手機外框、「示範／真實 relay」切換且預設示範、可複製的「示範 nsec」、機器人小幫手說明），而 ADR-0274 的 Capacitor `webDir: "dist"` 打的就是這份產物——**APK 開起來就是預覽台**：手機裡再畫一支手機、要先複製 nsec 才進得去、預設連記憶體後端（訊息根本不出裝置）。其二，`auth.ts` 只有 `identityFromNsec`／`identityFromSecret`／`identityFromPairBundle`——**全部要求已有金鑰**，標題直白寫「用私鑰登入」、提示要人去桌面版複製 nsec，等於**手機是第一個裝置的人沒有入口**（還得把私鑰過剪貼簿與傳輸通道）；桌面早在 ADR-0251 就以「填名字＝建新身分」為主、行動端從未跟上。決策：(1) **進入點與預覽台分家**——`index.html`→新 `src/main.tsx`（滿版、`relayUrl=DEFAULT_RELAY`、零示範字樣、只留 `100dvh` 高度鏈不放配色以免與 `@cinderous/theme` 打架），預覽台移到 `preview.html` 且 `build.rollupOptions.input` 明確只釘 `index.html` → `vite dev` 照舊服務、但**不進 `dist/` 就不可能被打進 APK**；(2) 新增 `createIdentity(name)` 本機生成金鑰，登入畫面預設只留**顯示名稱＋記住我密碼**、主鈕「建立新身分」，nsec／備份碼欄收合於次要連結「已經有身分？用 nsec 登入」之後（展開後主鈕變「登入」並可折返），文案改為說明本機生成金鑰、不需手機號碼或電子郵件。紅線不動：密碼留空＝不記住（ADR-0112），新路徑走 ADR-0117 同一條 Argon2id 包裹。代價：多一個 HTML 進入點要維護（靠顯式 `input` 與本 ADR 表格防回歸）；使用者更容易不慎建出多個身分（同名有 ADR-0146 擋，不同名與桌面現況相同暫不收斂） | 已接受 |
| [0276](./0276-out-of-band-fallback-relays.md) | **錨點全失聯的自動備援：帶外簽章清單＋確定性收斂（不靠通知對方）**（提議中）：現行拓樸有個**在最需要時失效**的環節——錨點硬編碼（2 座且**都在 Cloudflare**，ADR-0189 已記單點），動態調整靠**帶內** kind 10037，**中繼全掛就拿不到**。否決「出事後通知對方換 relay」——**通知不可能送達**（訊息/離線信箱/WebRTC 信令全經中繼，正是剛失去的通道；雞生蛋）。決策＝**收斂而非通知**：雙方各自跑同一演算法、吃同一份輸入即自動相遇，零協調訊息；且**不要求精準收斂**——沿用 `REDUNDANT_K` 各自連前 K 座、**有交集即通**（比要求兩邊選一模一樣穩健）。安全核心：清單走**帶外**（官網靜態檔，比照 `releases.json`／`threat-intel.json` 的 `siteFile()`，零新增基礎設施）但**必須以硬編碼 `MAINTAINER_PUBKEY` 驗簽**（既有 `verifyRelayList`）——不驗簽的話「上網抓 relay 清單」等於**誰控制清單就能把所有使用者導向他的中繼**，那是主動攻擊、遠比被動元資料洩漏嚴重；有簽章則來源被入侵也改寫不了（同 ADR-0239 立場）。緊急選座**只驗 liveness**（完整探測每項 8 秒且會寫入事件；斷線時速度就是可用性），行為健檢背景補跑只影響徽章與下次排序。觸發＝Tier 1 全失聯 ≥5 分鐘**且使用者事先同意**（降級絕不靜默）＋企業身分不適用＋降級期間 UI 常駐標示；**換回錨點天然收斂**（硬編碼、雙方一致、各自重試）。殘餘：審查封鎖連官網也擋則失效——故與**第三座換平台錨點（0189）互補而非替代**、降級期僅剩 Gift Wrap 一層保護、清單陳舊＝備援失效 | 提議中 |
| [0275](./0275-client-side-relay-health-check.md) | **中繼健檢下放客戶端：三層分級、首次探測一次、只提示不阻擋**：公共 relay 研究揭露缺口——客戶端只做 liveness/重連/hint 陳舊/清單簽章四項，**行為稽核（Ephemeral 語意/NIP-40 過期/AUTH）只在 CI 每小時排程跑**；且不對稱：CI 測的是 relays.json 候選站，**使用者自填的 relay 從未被任何一層檢查過行為**。否決「每次連線都檢查」（探測**會寫入事件**，每次開 App 寫兩顆測試事件不划算）與「只手動」（沒人會點＝警告等於不存在）。決策：(1) **三層依成本定頻率**——A 被動觀察 AUTH 挑戰（**零成本**，`authSigner` 收到即記 true、連上 3 秒未見即記 false）／B NIP-11（待接）／C 主動探測（**首次＋30 天 TTL**）；A 層最划算，因「不要求認證＝任何人都能看到你何時收到訊息」正是最大風險；(2) **官方錨點不做客戶端探測**（CI 每小時已稽核，`shouldProbe` 排除）；(3) 探測本體**上移 `@cinderous/core`**（relay/engine 都依賴 core 且不成環——engine 對 relay 是 devDep，反向會成環；core 本就有 WebSocket I/O），relay/bootstrap 轉引、CI 行為不變；**探測一律用臨時金鑰**（下放的前提：不洩漏使用者身分）；(4) **只提示不阻擋**，徽章列出具體原因，未知不顯示（沒資訊不製造噪音）——使用者有權選擇連哪座中繼。殘餘：首次寫入兩顆測試事件、3 秒判定偏保守（誤報優於漏報）、B 層待接、行動端未接。core/engine 406＋desktop 619 綠 | 已接受 |
| [0274](./0274-capacitor-android-and-camera.md) | **Android 打包採 Capacitor ＋對話內「拍照直接傳」**：行動端至今只有 react-native-web、無原生 app。(1) **打包＝Capacitor**（研究已量化：121 處瀏覽器 API 零改動、WebView 儲存 app-private）——`appId=app.cinder.mobile`（沿桌面反向網域慣例）、`webDir=dist`、**刻意不設 `server.url`**（那等於線上 webapp 的殼；要的是離線可用、資產隨版本固定）、`allowMixedContent:false`（自架 `ws://` 需使用者自行放行，不預設放寬傳輸安全）；(2) **權限最小集**＝INTERNET／CAMERA(+`required=false`)／RECORD_AUDIO＋MODIFY_AUDIO_SETTINGS（通話 0101）／POST_NOTIFICATIONS（13+），**刻意不要相簿讀取**（系統選擇器只給單一檔案，不需「讀取全部照片」）；(3) **版號納入 ADR-0227 SSOT**——`versionName` 進 targets，`versionCode` 以 `major*10000+minor*100+patch` 決定性推導（Play 要求單調遞增整數；同版號恆同 code、重建不跳號），CI `version:check` 自動涵蓋 Android；(4) **拍照直傳**＝平台縫加 `takePhoto()`，以 `<input capture="environment">` 實作（行動瀏覽器與 WebView 皆直接開相機→**零額外相依**、可在瀏覽器預覽先驗證，把「相機」與「Capacitor 整合」解耦），位元組同樣經 `sanitizeImage`（ADR-0273——剛拍的照片 GPS 最準，此路徑尤其不能漏），UI 加 📷 與 📎 並列。殘餘：**背景收訊仍未解**（ADR-0272 前台服務/FCM 待實作）、APK 為 debug 簽章未上架、`<input capture>` 取全解析度（解碼瞬間仍需完整影像，極低階裝置 OOM 時改用已裝好的 `@capacitor/camera` 原生層縮圖）、`android/` 進版控增加維護面 | 已接受 |
| [0273](./0273-strip-image-metadata-on-send.md) | **送出圖片一律去除 EXIF 等中繼資料（canvas 重編碼）**：全 repo 搜 `exif` **零命中**——送檔是原檔位元組直送，傳照片會連 **GPS 座標/拍攝時間/裝置型號**一併送出，與藏元資料的整體立場（Gift Wrap/presence jitter/威脅情報純本地）直接衝突；`makeThumbnail` 只清了**縮圖**的 EXIF，原檔照送＝現況並無保護。洞已存在於已發布的 v0.0.13，且規劃中的「拍照直接傳」會讓它從潛在洞變成主要管道。決策：engine 新增 `sanitizeImage()`——canvas 重編碼**只保留像素**（EXIF/XMP/IPTC/私有 MakerNote 全消，不必信任某個 EXIF 解析器有無漏欄位），限 `SEND_MAX_EDGE=2048`／`SEND_QUALITY=0.85`（常數與 ADR-0102 縮圖政策同置 SSOT）；**排除 GIF**（重編碼會摧毀動畫，ADR-0222）與 SVG（可執行標記）；**失敗即原樣送出**（清不掉不該讓人送不出檔案）；桌面 `sendFile`/`attachFile` 與行動端 `pickFile` 三個入口共用，Tauri 路徑重編碼後**不再帶 path**（避免重載讀回未清除的原檔）；`sanitizedFileName` 同步改副檔名。**v1 不做「傳送原檔」開關**（隱私預設、少一個腳槍）。順帶壓縮解決大圖記憶體/頻寬（Capacitor 相機路徑的已知障礙）。殘餘：畫質損失且非原檔、GIF 不處理、失敗時仍送原檔、v0.0.13 仍有此洞需下版修復。engine 394 綠 | 已接受 |
| [0272](./0272-contentless-push-wake.md) | **背景推播：FCM／APNs「無內容喚醒」＋明示同意、可關閉**（設計決策，待行動端原生化後實作）：ADR-0116 當年劃線「不做推播」並設條件「中繼須在**不知道收件人**下觸發」——本 ADR **誠實修正該前提**：中繼本來就靠 Gift Wrap 外層 `#p` 定址投遞離線留言，**推播並未新增中繼端知識**；真正新增的是 (1) Google/Apple 得知裝置喚醒時序、(2) 中繼須存 `pubkey→token`、(3) 裝置連結性。決策＝採無內容喚醒並以四道設計壓低代價：①payload 零內容/零寄件者（通知文字一律本機生成）；②**合併＋抖動**（每 token 60s 最多一次＋0–30s 隨機，借 ADR-0088 jitter 思路→第三方只看到粗粒度活動窗、連續 50 則只喚醒 1 次，同時省電省配額）；③token 帶 **TTL 30 天**＋輪替（比照 ADR-0065，中繼不因此變永久狀態，並納入 NIP-62 清除）；④**預設關、明示同意**且依身分獨立——**Android 預設走前台服務（零第三方）**、推播僅為省電替代，**iOS 則是推播或無背景通知二選一**，同意畫面須逐條列出洩漏。企業政策可全域停用。**§5.1 payload 形態**：訊息預覽與 payload 是兩件事——通知文字**永遠本機解密後生成**，兩形態皆可顯示完整預覽（要不要顯示沿用既有 `notifyHidePreview`，ADR-0076）；真正差異在可靠性且平台不對稱：**Android＝(a) 空喚醒**（FCM 高優先足夠、零額外洩漏）、**iOS＝(b) 一般推播＋Notification Service Extension**（iOS 靜默推播會被系統節流→通知形同虛設；NSE 於裝置解密後改寫通知文字，Apple 仍看不到內容，代價是多洩漏**密文大小級距**，因 NIP-44 padding 已粗化；APNs 4KB 上限→payload 帶事件 id 由 NSE 取回解密）。殘餘：開啟者 pubkey↔裝置在 TTL 內於中繼有連結、第三方取得粗粒度時序（不可完全消除，靠 opt-in＋自架＋輪替緩解）；前置＝行動端目前無原生 app | 已接受 |
| [0270](./0270-right-panel-tab-style.md) | **右欄分頁樣式：固定高度、選中改填底（去除底線指示器）**（補建檔）：使用者回報切換分頁時高度會跳、底線指示器「AI 味重」。根因＝選中態 `font-weight:600` 讓文字變寬→窄欄換行→該分頁變高。決策：固定 `height:34px`＋`nowrap`＋flex 置中、**選中不變字重**；選中指示改**填底**（surface2＋主色字）、移除主色 `border-bottom`（Material 語彙與懷舊即時通風格不合，且 2px 底線在六分頁擠一排時辨識度低）；`msn-layout.test` 讀 CSS 鎖住不回歸。殘餘：分頁名過長會裁切而非換行（現六個實測可容） | 已接受 |
| [0271](./0271-per-speaker-name-color.md) | **群組發言者名字色：pubkey 雜湊的穩定色（否決「每次隨機」）**：現況名字只分兩態（自己 --accent／對方 --in-name 單一色）→ **群組所有人同色**，認人只能靠頭像。否決 (a) 每次渲染真隨機（顏色的價值在「這色＝這人」，每次變＝雜訊＋破壞 SSR/測試決定性）、(b) 直接用 avatarColor 的 HSL 當文字色（黃/青綠在白底約 2:1，違反 ADR-0253）。決策：`@cinderous/theme` 新增 `nameColor(seed, theme, contrast)`——hue 與 avatarColor **同一把雜湊**（名字/頭像同源），S/L 依主題×對比選安全值，**全 360 hue × 四組合測試迴圈驗證**（一般 ≥AA 4.5、高對比 ≥AAA 7；最差 hue 亮色 60 黃 4.87、深色 240 藍 5.04）；**只在群組套用**（1:1 維持 --in-name、自己維持 --accent）；新增唯讀 `useThemeMode`／`useContrastMode`（缺 Provider 回預設不拋錯）。殘餘：大群仍可能相近 hue（未做 per-群去重，那會犧牲跨群一致性）、行動端待接。theme 24＋desktop 618 綠  | 已接受 |
| [0269](./0269-calendar-views-and-deck-avatar.md) | **行程分頁多視圖（月/週/日/清單，預設本月月曆）＋三欄側欄頭圖放大**：行程 v1 只有清單卡片。決策：(1) `calendar-view.ts` 純函式（`monthGrid` 含補格湊整週/標 today、`weekGrid`/`dayCell`/`addDays/addMonths` 月底夾限、**本機時區、跨日行程落起訖兩天**，7 測）＋`CalendarPanel` 整合——`CalView=month|week|day|list` **預設 month**、視圖切換 tablist＋期間導覽（‹今天›，清單無）；月/週＝日格格陣（每格 ≤3 標題＋＋N、點日跳日視圖）、日/清單＝完整卡片複用既有 `renderItem`（RSVP/編輯/提醒零改動）；`initialView` prop 供深連結/測試（既有互動測試改 list）；(2) DeckSidebar 頭圖去 `sm`（30→44px）與經典對齊。殘餘：格子只顯標題+數量（點進看細節）、週起日固定週日（參數已備 UI 未暴露）、行動端多視圖後續。desktop 616 test 綠 | 已接受 |
| [0267](./0267-ai-rewrite-environment-gate.md) | **AI 改寫依環境設閘：公網瀏覽器版整面隱藏**（編號躍遷：0257–0266 被平行分支預訂）：部署版 webapp 偵測不到 Ollama 的根因＝瀏覽器路徑直接 fetch localhost——(1) CORS：Ollama 預設 `OLLAMA_ORIGINS` 只放行 localhost 系 origin；(2) Chrome 本地網路管制（PNA/LNA）。死路 UI 比沒有更糟，但一刀切全禁會誤傷 **localhost origin**（開發/自架——Ollama 預設就放行，開箱即用）。決策＝`aiRewriteSupported(origin)`（純函式可注入測試）：Tauri 恆可、瀏覽器僅 localhost origin；App 四處設閘（設定 AI 區塊/🧠 摘要×2/composer 改寫），`enabled` 偏好照存（換回桌面即恢復）。殘餘：自行設好 OLLAMA_ORIGINS 的專家在公網版也看不到入口（刻意——可用桌面/localhost 版）。desktop 561 綠 | 已接受 |
| [0266](./0266-calendar-reminders.md) | **行程提醒：本機計時器、零中繼成本**（ADR-0263 §1.4 唯一那條紅線；行事曆自此無已知功能缺口）：提醒不是「排程一則未來事件」而是**到期判定**——沒有伺服器、沒有推播、沒有排程佇列（那些都是流量或第三方），`dueReminders()` 純函式，答案完全由本機資料決定。**⚠ 用週期 tick 而非每筆行程一個 `setTimeout`**：後者對本專案的主要情境是壞的——`setTimeout` 延遲是 32 位元毫秒（上限約 24.8 天），超過就**溢位成立刻觸發**，於是「三個月後的婚禮」在你設定的那一秒就叫你；而行事曆的價值正好在遠期行程。30 秒一次（粒度是分鐘，每秒掃只是白費電）＋開機補掃一次。**⚠ 提醒設定不進 rumor**：行程 id ＝ rumor 雜湊，塞進去會改變它（ADR-0264 §8 補送踩過的同一顆地雷）；且「我要提前多久被叫」本來就是自己的事，沒理由讓群組知道——`calendarRemind()` 刻意一個網路動作都不做（測試**攔在 `client.publish`** 確認一顆事件都沒多）。連帶：`upsertCalendarEvent` 必須保留既有 `remindLead`，否則**主揪一改時間就把全體的提醒設定清掉**。**⚠ 「已提醒過」記的是 `start` 不是布林**：改時間時提醒必須重新武裝，以 `start` 為鍵就自動成立、不需要任何清除邏輯；且**必須落盤**，只留記憶體的話每次重開 App 都會被同一個提醒轟一次。**寬限窗 5 分鐘**：沒有它「準時提醒」（lead=0）**永遠不會觸發**（提醒時刻恰等於 start，而 tick 是離散的）；同一個窗也涵蓋「關著錯過、開起來稍遲」，更晚就只是噪音。已回覆「不參加」的不提醒（只看自己那格）。**⚠ 提醒不走 `shouldNotify` 的 `windowHidden` 閘**：ADR-0217 的「視窗看得到就別跳」在這裡是錯的——你正盯著 App 不代表你知道十分鐘後有會；提醒只受總開關管，不受每對話靜音/下班靜音（那些是「別人打擾你」的閘門，提醒是你自己的鬧鐘）。UI：**沒選過就是不提醒**（預設值只在主動打開時當起始選擇），過去的行程不顯示提醒選項，**非主揪也能設自己的提醒**（提醒與主揪權威無關）。core 10＋engine 6＋desktop 5＋mobile 5 測試；全工作區綠（core 523／engine 386／desktop 578／mobile 216）。**已知缺口（刻意）**：App 沒開就不會提醒——要在關著時提醒就得有推播服務，那既是流量也是第三方，還會洩漏「你有一個行程」給供應商 | 已接受 |
| [0265](./0265-mobile-calendar-ui.md) | **行動端行程 UI**（ADR-0264〈後續行動〉的最後一塊；引擎層本就共用，缺的只有畫面）：行程分頁掛在既有輔助面板（ADR-0183）內與媒體/對話串/便條並列＝桌面「右欄多一個分頁」在小螢幕上的樣子；**不新增底部分頁**——行程是「某個對話的」不是全域的，放進 `BottomTabs` 會暗示錯誤的資料歸屬。無 `onCalendarPublish` ＝分頁不出現且日期完全不偵測。**⚠ 時間輸入：桌面的 `datetime-local` 在這裡不存在**（本 repo 的 react-native-web ambient 宣告為日後上真 RN 刻意收斂、真 RN 更沒有 `type`），改為**相對調整**（天/時/分三組 ▲▼ ＋長度 chip）；附帶好處是**存「長度」而非絕對結束時間**——改開始時間時結束跟著走，不會留下桌面得靠送出檢查才擋得住的「結束早於開始」壞狀態，而且**最常見的路徑根本不用調**（從對話點日期進來時開始時間已經是對的）。**⚠ 預填狀態放呼叫端不放元件內**：行動端測試環境是純 SSR（`environment: "node"`＋`renderToStaticMarkup`）——**點不了任何東西**，第一版做成內部 state 的「點日期→開面板→切分頁→預填」整串**一行都測不到**（與 ADR-0264 §7 的 useEffect 是同一類錯誤換個樣子再出現：**「測不到」在這個專案等於「沒做」**）；改為 `onPickDate` 往上、`calendarDraft` 往下由 `MobileApp` 持有，與桌面同形且 SSR 測得到，分頁切換一樣用 render 期間調整 state。`calendarDraft` **帶 `convo`**——畫面以 `key={activeId}` 重掛但 App 層 state 不會，不記對話就會冒出上一個對話的預填表單。**權威與桌面逐條對齊**：非主揪連編輯/取消鈕都不出現、RSVP 名單不列缺席者、過去的留著但退到背景、取消**先出確認鈕再按才送**（ADR-0234 的行動端形態，手機沒有桌面的對話框元件）。**`calendarNameFor` 與 `nameFor` 分開**：後者只在群組提供，共用會讓 1:1 的參與者名字全變成公鑰片段；同理 `selfPubkey` 改為一律傳入——只在群組傳的話**自己在 1:1 開的行程會被判成別人的，變成「改不動自己的行程」**。行動端 14 測試；全工作區綠（mobile 197→211）。修掉自己寫的兩個弱斷言：整份 HTML 比對「我」等於沒測（改比對 `cal-going` 節點）、`opacity:0.5` 斷言不到（**RN-web 把樣式編成雜湊 class 名**，改讓過去的項目帶自己的 testID） | 已接受 |
| [0264](./0264-shared-calendar-implementation.md) | **共享行程實作**（承接 ADR-0263 的設計；階段一～四＋補送＋保留上限已實作）：**kind 46/47** 接續既有 app 內部 rumor kind 40–45，**欄位照抄 NIP-52 的 tag 名**（title/start/end/location）但走既有 1059 管線＝零新協定面、零新加密原語，**中繼端零改動**（ADR-0263 §1.3 的性質保住）。**行程 id ＝ `rumor.id` 不自造**——ADR-0095 為群訊回條釘死的「rumor 對每位收件人相同→雜湊相同」性質，讓 RSVP 需要的「所有人都同意的 id」免費取得。**RSVP 存在事件內**（rsvps: pubkey→{status,at}）而非另立集合：由本人簽章故無多人衝突；兩個靠測試才釘住的細節——`setCalendarRsvp` 只往前推進（亂序的舊回覆不覆蓋新的）、`upsertCalendarEvent` **保留既有 rsvps**（主揪改時間那則 rumor 不帶回覆，直接覆寫會把大家的回覆清光）。**權威**：收發共用同一條 `applyCalendarChange`（兩邊分歧是 bug 溫床），非主揪的 update/cancel 一律忽略**即使 created_at 更新**；**明確不用 ADR-0242 的 CRDT 機具**——那是為同一身分的多裝置設計的，推到多使用者會帶出它從不必解的問題：**LWW ＝時鐘最快的人贏，而 created_at 是自報的**。**RSVP 廣播閘 10**（≤10 送全體、>10 只送主揪＝每輪 O(N²)→O(N)），沿用 ADR-0095 的 knee 值。**UI 權威三層一致**：非主揪連編輯按鈕都不出現；RSVP 名單只列要來與也許（不列缺席者——會把「明說不來」與「還沒回覆」混在一起）；過去的行程退到背景不刪。**已知缺口**：無（行動端 UI 由 ADR-0265、提醒由 ADR-0266 補齊）。**階段四日期偵測**：`core/date-detect.ts` 純函式（比照 calc.ts／sticker-triggers.ts），`detectDates` 掃全文、`detectDateAtEnd` 只看尾端（同 ADR-0037 語意）；**取捨一律偏向少報**——過去的不報（差點漏掉 `上週三`／`last friday`：前綴表沒列它們的話，比對會從第二個字開始，**把過去的事報成未來的約定**）、今天但時刻已過不報、裸數字與 `2026/3/15`／`v1/2/3` 不誤判、重疊時長的優先。UI 兩處皆為**提示**：氣泡下方可點標記（已收回/過期訊息不掃）＋composer 尾端建議列（**不劫持 Enter**）；點擊才切分頁並預填＝**使用者觸發的導覽，非 §1.6 否決的自動切換**；未提供 `onPickDate` ＝完全不偵測。分頁切換用 **render 期間調整 state** 而非 `useEffect`——effect 在 `renderToStaticMarkup` 下不執行，用 effect 等於這段行為沒有測試覆蓋。**§9 補送**（解掉 §1.5 的靜默分歧：中繼只存 7 天，「三個月後的事、對方離線 8 天」＝他永遠收不到，而群組以為講定了；Outbox/ADR-0041 不覆蓋——它保證「中繼收下了」不是「對方拿到了」）：**送達回條沿用既有 RECEIPT kind**（零新協定面），主揪記進 `delivered`，觸發點為**對方上線**（ADR-0109 訊號）與**新成員入群**（無伺服器端房間＝沒有歷史重播），只補「未來、未送達、未 RSVP」＋6 小時節流窗（純記憶體——持久化反而會讓「換裝置就永遠不補」）；**⚠ 補送必須重建逐位元組相同的 rumor**——id ＝ rumor 雜湊而 tags 含 `g`/`to`，第一版用 1:1 形狀重包群組行程，收件人拿到的是**新 id 的重複行程、原 RSVP 全對不回去**（測試比對 id 抓到，故 `wrapCalendarEvent` 加 `groupId`、`created_at` 還原成 `updatedAt`）。**§10 保留上限**＝**ADR-0263 §1.5 那個「過去一個月」政策，放在它真正做得到的那一層**：中繼看不出是行事曆、更讀不到 `start`（做得到就等於放棄 §1.3 的零知情），**但本機看得到日期**，且本機才是真實來源。過去 30 天窗（`CALENDAR_PAST_RETENTION_DAYS`）＋500 筆硬上限防呆；**未來側刻意不設窗**——§1.5 的「未來兩個月」是中繼儲存成本參數，本機沒這成本，清掉「三個月後的婚禮」是純資料遺失。**⚠ 超量時不是「按 start 排序砍尾」**——那在「一堆未來行程」時會砍掉**最近**的，正好砍反；改依相關性排（未來優先於過去、同側取較近）。**⚠ `pruneCalendar` 放進 `AppStorage` 介面而非鴨子型別探測**：第一版用 `typeof === "function"` 探測，結果只有 `MemoryStorage` 有，**桌面實際用的 `LocalStorage`/`TauriStorage` 開機完全不清**——而那正是唯一會累積三個月的環境；且清完必須寫回磁碟，否則重開機整筆復活。core 50＋engine 12＋engine 補送 7＋desktop 22＋LocalStorage 1 測試；全工作區綠（core 513／engine 380／desktop 573） | 已接受 |
| [0263](./0263-calendar-and-podcast-feasibility.md) | **行事曆與 Podcast 擴充的可行性**（研究記錄，不改變產品行為）：把 ADR-0259 Tier 3 的兩項拉出來細評，**修正其中一半的結論**——兩者被放在同一格，實際上卡住的地方完全不同。**行事曆（NIP-52）**：原樣搬進來有三個硬阻礙——(1) 全公開，且公開的正好是我們在藏的東西（`p`＝參與者、`location`/`g`＝地點、**RSVP kind 31925 就是一條公開的社交圖譜邊**）；(2) `ADDRESSABLE_MAX_PER_AUTHOR = 5` ＝一個人最多 5 個行程（那個 5 是為 ADR-0071 的 5 台裝置設的）；(3) `ADDRESSABLE_TTL_SECONDS` 30 天＝三個月後的行程會先從 relay 消失。②③ 可改常數、①改不了（是規格語意本身）。**可行形態＝借形狀不借傳輸**：把 NIP-52 的欄位當 app 內部 rumor kind 包進既有 Gift Wrap 管線（同 `GROUP_CONTROL=40`/`RECEIPT=41`），三個阻礙一次消失、本機為真實來源。**§1.3 誠實界定中繼看得到什麼**：內層 kind 與行程欄位讀不到（前提是沿用外層 1059——檔案塊當初刻意給了獨立 kind 1060 的反例在此），**但送出邊「你→B」看得到**——依 ADR-0237，每條連線都以真實金鑰 AUTH、發布走同一條連線，中繼靠 `authState` 就能把寄件人接回來，**即使外層作者是一次性臨時金鑰**；故 Gift Wrap 在此買到的是「藏內容與藏事件類型」而非「藏送出邊」，而**送出邊本來就對中繼可見，加不加行事曆都一樣＝不新增洩漏類別**。**唯一實質新代價：行事曆結構上無法卸載到 P2P**（ADR-0237 列為最有效槓桿，但邀請本質非同步、對方可能離線，必須經 relay）。**§1.4 成本（前一版誤記為 O(N)，已更正）**：依 ADR-0109 計費，1:1 邀請＋回覆 ≈ 6 顆事件 ≈ **2 則訊息**（便宜）；但**群組 RSVP 的形狀不是群訊、是「群組回條」**——每位成員回應同一則事件，若要全體看得到「誰要來」就是每人各扇出 N 份＝**每輪 O(N²)**（10 人群 ~100 顆 ≈ 33 則訊息；50 人組織公告 ~2,500 顆 ≈ 830 則）。ADR-0095 已量測過同一形狀（全網回條 ≈ r·N²）並以**依成員數分級**（≤5 名單／6–10 計數／>10 不送）解決，RSVP 直接沿用即可，但**必須在實作 ADR 明確指定**。另補兩項前版漏算：**行事曆有生命週期**（改時間/改地點/取消＝每次一次完整扇出，訊息送出即結束）、**提醒必須是本機計時器（零中繼成本）**。**§1.5 已評估並否決「讓中繼依行程日期決定保存範圍（過去 1 月＋未來 2 月）」**——中繼**做不到**（看不出是行事曆、也讀不到 `start`，兩者都在密文內層），要做得到只有 (a) 給獨立外層 kind＝中繼知道你在傳行事曆，或 (b) 把行程日期放進明文 tag＝**中繼得到你未來兩個月的行程時間表**，(b) 是嚴重退步。而提案想要的效果**現況已達成且更嚴格**（1059 吃站方 7 天 TTL，`min(客戶端標的, now+站方上限)` 硬夾，標 90 天也夾回 7 天），「本地保存」則本來就是設計本身。**但提案指向一個真問題：投遞窗不是保存期**——約三個月後的事、對方離線 8 天→wrap 過期→**永遠收不到邀請**；行事曆與訊息的真正差異是**資料有效期比傳輸 TTL 長**，而 Outbox（ADR-0041）只保證「中繼收下了」不保證「對方拿到了」。**解法是重送不是延長保存**（看到對方上線時補送未送達的邀請，訊號現成，零中繼改動零隱私代價）。**§1.7 群組行程是「建立一次、扇出全體」不是每人各建一份**——機制同群訊，且 ADR-0095 已保證「rumor 對每位收件人完全相同→雜湊相同→`rumor.id` 跨成員一致可驗證」（該 ADR 修的正是「群訊在每台裝置 id 不同→回條/回應/引用對不回發訊者」，RSVP 有完全相同的需求故直接繼承）；但**三種情況會讓成員手上沒有這筆**（離線超過 7 天、事後才加入群組＝無伺服器端房間無歷史重播、移除後再加回），而行事曆比聊天糟——**群組以為講定了但某人的行事曆就是沒有，且無訊號顯示他少了東西＝靜默分歧**。**權威 v1 採主揪權威**（只有建立者能改/取消，他人是複本，編輯＝指向原 id 的新 rumor）；**否決「任何人可編輯」**——ADR-0242 的 CRDT 機具是為**同一身分的多裝置**設計的，推到多使用者會帶出它從不必解的問題：**LWW ＝時鐘最快的人贏，而 `created_at` 是自報的**，對多裝置良性、對多使用者是真攻擊面。**§1.6 評估「對話提到日期時右欄自動切到行事曆分頁」**：技術可行（分頁只是一個 `useState`，且目前**無任何自動切換行為**），**但「自動切換」應否決**——違反專案立過兩次的原則（ADR-0037「不劫持 Enter」、ADR-0097「不要偷改正在打的訊息」，兩者皆為「偵測→提供選項，不代替使用者行動」）；三個問題：搶走使用者剛選的脈絡、中文日期樣式的偽陽性是常態（「我昨天看到」「3/15」）、**⚠ 對方的訊息是攻擊者可控輸入**（ADR-0121/0127 陌生人可進訊息請求區且該區可被灌爆 → 任何能傳訊給你的人都能操控你的 UI 狀態＝UI redressing 輕量版）。**改為偵測→提示（設計已定）**：氣泡內日期加低調可點標記＋composer 建議列（同 sticker trigger 語意、**不劫持 Enter**）＋全本機零外送；**分頁未處理日期 badge 不做**（維護者決定——已知取捨：badge 是三者中唯一持久且聚合的提示，拿掉後使用者當下沒注意到標記就不會再被提醒，屬刻意的低噪音選擇）。**真正工作量在日期解析而非 UI**（中文相對日期需基準時間/週起始/時區、zh-Hant+en 兩套，應為 core 純函式＋測試），**且該成本在提示形態就要付，自動切換省不了工**。若仍要自動：opt-in 預設關＋只對自己輸入的文字＋只在停留於預設分頁時才切、群組扇出成本同 ADR-0027、UI 的家已存在（右欄便條 ADR-0182/0183 的天然兄弟）、企業共用行事曆是 ADR-0157 上下班時間的延伸兼 B2B 賣點。**定位判斷**：PRD §12 排除的是「會把產品拉成社群平台」的公開大群/動態牆，而「跟三個朋友約週六幾點」是私密群組協調，與「connect buddies」直接相關 → **從「與 IM 無關」改判為值得做的候選**（但須誠實標示不與其他 Nostr 客戶端互通）。**Podcast（NIP-F4）維持否決，理由改記為三點**：(1) 內容不在我們的網裡——客戶端只連 cinder relay，要聽就得連通用公共 relay，而 `authors:[節目 pubkey]` 就是「這個 IP 在追這些節目」的完整紀錄＝**全新的元資料洩漏面，完全在現行威脅模型之外**；(2) `audio` 是第三方 HTTP URL，播放＝自動交出 IP，與 ADR-0231/0038 對 URL 的謹慎正面衝突；(3) 把私密通訊工具變成內容消費 App。而唯一與 IM 相關的切片「分享某一集」**現在貼連結就能做**，零 NIP-F4。兩者皆不進 ROADMAP，直到有人明確要求 | 研究記錄 |
| [0262](./0262-sponsor-corner-card.md) | **「贊助此節點」角落卡**（ADR-0089 決策 2 落地，其三項後續行動至此全部完成）：ADR-0260 讓 relay 會回 NIP-11 了，但沒人讀＝營運者填了 `cinder_donations` 也看不到；而 ADR-0257 算過公共 relay 結構上不可能盈利、只能靠自願贊助。**engine `relay-info.ts`**：比照 `turn-fetch.ts` 形狀（可注入 fetch、任何失敗一律回 undefined——贊助卡是純加分，不能影響通訊也不該留錯誤痕跡）；**重點是 scheme 白名單**——文件由被視為對手的 relay 提供，而這些字串最後會變成可點的 href，故三個平台**只收 `https:`（刻意不收 `http:`**，贊助頁會輸入付款資訊）、`lightning` 正規化為 deep link 且限保守字元集、`javascript:`/`data:`/`file:`/`vbscript:` 逐一測試釘死、長度有界、順序固定（不讓營運者用鍵順序搶眼球）。**桌面卡片**：右下角 z-index 40（**刻意低於對話框/通話/貼圖編輯器 55–70**——畫面上最不重要的東西不該蓋住互動）、外連 `noopener noreferrer`、`lightning:` 交外部錢包；**dismiss 依 relay 記不依身分記**（「我對這一座的態度」，換身分不該又冒出來）＋URL 正規化，走裝置層 localStorage **刻意不進加密基質**故不會混進配對捆包/快照；localStorage 讀不到時**當作沒關過**（寧可多顯示也不永久隱藏）。**只對 home 座顯示**：對照 pool 組成，錨點（0039）與 hint 學習（0035）的座使用者從沒選過，ADR-0089 明訂不對它們跳卡；桌面也無手動加座 UI，故 home 即唯一。企業身分整個跳過（無「贊助雇主 relay」語意）；**行動端不做**（0089 決策 3）。**措辭三句（自報非官方背書／完全自願／永不自動付款）由測試釘死——這些字是安全機制不是文案**。殘餘：地址仍為自報、未驗證營運者身分、付款隱私在 App 之外。engine 361＋desktop 551 全綠 | 已接受 |
| [0261](./0261-nip86-relay-management-api-plan.md) | **NIP-86 Relay 管理 API 規劃**（提議中）：現況成員／政策**只有部署期入口**（`allowedAuthors`／`allowedKinds`／env 由佈建注入），改一個人要一次部署——正好打在 ADR-0257「人力才是天花板」的痛點上。核心張力：本專案的成員權威是**管理者簽章名冊**（ADR-0047，客戶端可驗），NIP-86 的 `allowpubkey`／`banpubkey` 則讓 **relay 營運者**直接改＝**兩個真實來源**且會與名冊佈建靜默互相覆蓋。決策：**採 NIP-86 子集作為「名冊→relay」的執行管道與唯讀觀測面，不讓它成為獨立權威**——成員類只實作 `list*`（唯讀），寫入改走新增的 `cinder_applyroster`（吃簽章名冊、驗簽＋較新才取代後更新 allowlist）；政策類（`allowkind`／`changerelayname`）可寫（本就是站方權責）；**明確不實作** role 全家（無角色概念）、事件審核全家（中繼只看得到密文，提供等於暗示讀得懂內容）、IP 封鎖全家（relay 開始持有連線層可識別資料，與 ADR-0237 反向）。授權用 NIP-98，但**管理金鑰必須與名冊金鑰分離**——否則把運維外包＝把組織的成員權威交出去。維持提議中的主因：**分片（ADR-0241）下設定如何傳播到所有 DO 尚未解**。 | 提議中 |
| [0260](./0260-nip11-document-and-nip62-vanish.md) | **補齊 NIP-11 文件與 NIP-62 清除請求，並更新 MLS 規格指標**（承接 ADR-0259 Tier 1 三項）：**(1) NIP-11 是還債**——ADR-0089 贊助入口與 0092 節點自報都建立在它之上，但兩座宿主對 HTTP GET 只回純文字。新增 `nip11.ts` 為兩宿主 SSOT（比照 host-config 的 H1 教訓）；只有 `Accept: application/nostr+json` 拿 JSON、其餘維持純文字 200（健康檢查契約）；`supported_nips` **只列 relay 這層真的強制的** `[1,11,13,40,42,62]`（NIP-17/25/09/59 是客戶端語意，列上去＝謊報能力，測試釘死）；未設欄位一律不出現而非空字串佔位；`limitation`／`retention` 取自實際生效常數不另抄；承載 `cinder_donations`／`cinder_node`。**(2) NIP-62 補「刪除我的資料」缺口**——core `vanish.ts`＋relay 三道閘：`requireAuth` 時認證身分＝作者（擋**重放**，簽章已證明作者故這條是為重放而設）、`relay` tag 指向本站、刪除他發的＋**寄給他的（`p` tag——Gift Wrap 外層是一次性金鑰，唯一能定位收件匣的鍵）**＋可尋址快照；**目標比對 fail-closed**（缺 tag 或不知本站主機→只認 `ALL_RELAYS`，與 NIP-42「不給就不強制」刻意相反：不可逆的動作不對模糊輸入寬容）；**是命令不是留言→不寫庫、不扇出**（廣播「某人要求清除」等於用公開事件宣告其動作與時間點）；新增 `connHost` 把主機資訊與 AUTH 狀態分離（否則無 AUTH 自架站只認 ALL_RELAYS）；engine `requestVanish()` 逐座各簽一顆帶該座 URL、回傳**已送出**非「已刪除」；desktop 隱私分頁二次確認，措辭不做做不到的保證。**明確不做永久墓碑**（npub＝身分，永久封鎖會讓清除過的人無法再用；黑名單本身是「誰清除過」的元資料），殘留由重放窗＋TTL 有界化。**(3) MLS 指標**：ROADMAP F1/F2 改指向 Marmot（NIP-EE 已 `unrecommended`），只改方向不改行程。core 463＋relay 197＋engine 344＋desktop 543 全綠、`pnpm -r typecheck` 綠 | 已接受 |
| [0259](./0259-other-nips-applicability-survey.md) | **其他 NIP 的適用性盤點**（研究記錄，不改變產品行為）：對照生態全部 NIP 與本專案既有決策，先從既有決策導出五條篩選準則（自足生態刻意不互通／無公開內容／瓶頸是 requests 非頻寬／非金融 App／relay 視為對手），再分三層。**Tier 1 建議做**：(1) **NIP-11 是還債不是新功能**——ADR-0089 贊助入口與 0092 節點自報都建立在它之上，但 `worker.ts` 至今對 HTTP GET 只回純文字，做完可一次解鎖贊助入口＋`cinder_node`＋`supported_nips`＋`limitation` 四項已決議事；(2) **NIP-62 Request to Vanish** 補上「請刪掉我的資料」缺口（現況個人只能等 7 天 TTL、企業離職的 allowlist 只擋未來發布、已存殘留照躺；NIP-62 明訂須刪 `#p` 提及的 Gift Wrap＝正對本專案收件匣，且需本人私鑰簽＝天然安全）；(3) **NIP-EE（MLS）已標 `unrecommended`、被 Marmot Protocol（adopted）取代** → ADR-0027/0236 與 ROADMAP F1/F2 的「未來走 MLS」指標需更新（只改方向、不改行程，0091 暫緩理由仍成立）。**Tier 2 待評估**：NIP-67（順帶查出 `MAX_QUERY_ROWS=1024` vs 檔案塊配額 4000、且客戶端 FILE_WRAP 訂閱無分頁的不對稱）、Blossom（ADR-0244 階段 B 的自架友善替代，但其 kind:10063 公開清單須比照 ADR-0035 改帶內加密）、NIP-86（託管服務的執行期管理面，須先解「兩個真實來源」與簽章名冊的張力）、NIP-07（網頁版金鑰減壓閥，代價是每則訊息/每次解密都要 round-trip 擴充）。**Tier 3 明確否決並記錄理由**：NIP-77（省的是免費的 egress、瓶頸卻是 requests）、NIP-70（要求 AUTH pubkey＝作者，與 Gift Wrap 一次性金鑰簽名結構性互斥）、NIP-43/65/66（信任根比簽章名冊弱／已否決公開 relay 清單）、NIP-29（破 E2E）、支付類全數（PRD §12）、公開社群類全數、生態已 Deprecated 者 | 研究記錄 |
| [0258](./0258-desktop-escrow-encrypted-at-rest.md) | **桌面託管清單加密落盤＋舊明文就地遷移**（補上 ADR-0163 決策 #1 白紙黑字承諾、卻未兌現的「加密儲存落地」；由 ADR-0257 §6.1 查核發現）：`apps/desktop/src/ui/org-escrow.ts` 原以 `localStorage.setItem(JSON)` **明文**存**一整家公司所有公司帳號的 nsec**，繞過 ADR-0112 的加密基質——**web 版任一 XSS 即可一次撈走全公司私鑰**（ADR-0179 已為此加密行動端，桌面未跟進；原註解所謂「Tauri 有 OS 層保護」在 web 版根本不存在，且 ADR-0112 §3 早論證 OS 帳號邊界不足以當不加密的理由）。決策：(1) 比照行動端以 `deriveStorageKey(ownerSk)`＋`sealValue` 封裝，磁碟只留 `c1:` 密文、金鑰不對回空不拋；(2) **舊明文就地遷移**——`openValue` 對無前綴值 pass-through 故舊資料仍可讀，但 `loadEscrow` 讀到未加密值即**當場重寫為密文**（光能讀不夠，磁碟明文必須消失），已是密文則不重寫；(3) **無 `selfNsec` ＝放棄寫入而非退回明文**；(4) 載入改依 `backend`（原只看 `profilesState.active`，那時沒有解密金鑰）；(5) 寫入改「以磁碟為準的讀-改-寫」，**順手修掉託管在清單載入前到達會整批蓋掉既有條目的競態**。否決改走引擎 `AppStorage` 基質（會把全公司私鑰帶進 PairBundle／快照＝新風險面）。殘餘：只擋靜態（解鎖後記憶體中的明文同 ADR-0112 結論）、企業主換裝置未帶身分＝失去接管能力、遷移需該裝置「開過一次」。desktop 539 綠＋typecheck 綠 | 已接受 |
| [0257](./0257-relay-profitability-and-enterprise-service-model.md) | **Relay 營運的盈利可能性與企業服務模式**（研究記錄，**不改變產品行為、不新增站內金流**）：ADR-0166「架付費 relay／企業套件可賣 B2B」兩列的數字化展開。**成本**——以 ADR-0109 量測換算，每位在線使用者邊際成本 ≈ **$0.007/月**（1,000 人 ≈ $12/月；requests 才是天花板、egress 免費），會花錢的只有被刻意上鎖的 TURN（頻寬，0243）與公共檔案後備（儲存，0244）。**收入**——查核發現 ADR-0089 的 NIP-11 `cinder_donations` 與桌面角落卡**尚未實作**（`worker.ts` 對 HTTP GET 仍只回純文字），現況收入面為零。**公共 relay 結構上不可盈利**：pool/hint/home 遞補＝零轉換成本、進池免費＝競爭者價格恆為 0、只轉發密文＝無加值面、全網僅 2 座＝市場尚不存在 → 定位為貢獻非生意。**唯一有毛利的路徑＝企業導入＋託管＋支援＋客製**（軟體 AGPL 且自架文件齊全＝賣不了；AGPL §13 亦禁絕閉源護城河 → 賣的是營運責任）；50 人客戶基礎設施 <$6/月、報價 $150–300/月，天花板在人力不在機器；G5（SSO／元資料稽核，未實作）為首個付費客製標的。**成交前必揭露**：法遵/eDiscovery/DLP 結構上不支援、員工工作身分不託管（0052）、allowlist 只擋發布、接管只看得到 relay 殘留。**查核發現**：桌面 `org-escrow.ts` 託管員工 nsec **明文** localStorage（繞過 ADR-0112 加密基質；ADR-0163 文字稱加密、與實作不符，ADR-0179 已為行動端修正）→ 建議承接企業客戶前先修 | 研究記錄 |
| [0256](./0256-message-search.md) | **歷史訊息搜尋（四階段）**：盤點＝三欄側欄只能過濾對話清單、經典零搜尋、對話內不存在、封存（熱區 5,000 則外）是盲區、行動端全無。①**對話內搜尋＋跳轉**：標頭 🔍、Enter＝較舊/Shift+Enter＝較新/Esc、`msg-search.ts` 純函式（比對**可見文字**剝 ADR-0220 manifest、檔名可搜、收回/過期不參與）、跳轉配合訊息視窗化擴窗＋高亮（`.log` 子元素索引對映）；②**經典佈局**：roster 搜尋框，`nameOrMessagesMatch` 抽出共用；③**封存搜尋**：HistoryWindow 由新到舊**逐塊解密掃描、即掃即棄**——否決持久明文索引（會在加密儲存旁放可搜明文，掏空 ADR-0054），進度即時＋世代計數可中止；④**行動端**：聊天清單搜尋，比對邏輯上移引擎（thread-util 前例）。殘餘：封存掃描 O(全部塊)（過慢再議加密索引）；側欄比對原始 text 維持既有行為。engine 134＋desktop 557＋mobile 198 綠 | 已接受 |
| [0255](./0255-enterprise-category-comparison.md) | **企業版「與常見選擇的差異」類別比較段（否決完整企業對比頁）**：企業競品分三類（雲端套件 Slack/Teams/LINE WORKS、自架開源 Mattermost、E2E 自架 Element/Matrix）。否決逐產品完整頁——**類別錯位**（逐功能比協作套件＝一整欄 ✗；合規稽核匯出**設計上做不到**：零知識連企業主都看不到明文）、**成熟度時機**（無 SSO/SCIM，對 Element 現在輸多贏少）、**維護成本**。採輕量：企業版頁內五列（預設 E2E/管理者可否讀訊息/組織內元資料/資料所在/離職金鑰託管）× 三類別欄，開頭直說「只比隱私主權，功能廣度不是主場也不假裝是」；**誠實限制段測試鎖住**（無法匯出明文＋受監管行業可能不適合＋SSO/SCIM 未提供——誠實過濾客戶）；企業頁 ↔ 比較頁雙向 locale-aware 互連。完整對比頁等 SSO/審計後再議。website 59 綠 | 已接受 |
| [0254](./0254-website-comparison-page.md) | **官網比較頁：與 Signal／LINE／WhatsApp 的誠實比較表**：獨立 `compare` view（照 0246 enterprise 模式——自有 title/meta/canonical，吃「X vs Y」高意圖搜尋；雙語預渲染 12→14 頁自動生效）；十列＝手機號碼/預設 E2E/元資料/開源/伺服器架構/訊息存放/營利模式/平台停權/**前向保密**/**功能成熟度**，文案進 `copy.ts`（`cp_*` SSOT）。**保留對我方不利的列**：FS 格＝「開發中——外部審計前不宣稱」（承認沒有≠宣稱有，符合 0245 硬閘、**測試鎖住不得為 ✓**、Signal 誠實給 ✓）；成熟度自承開發中——誠實表比全勝表可信。防呆：只用文字名稱無第三方 logo（商標）、表尾商標聲明＋資料時間（2026-07）、措辭不絕對化（「大幅隱藏」）、LINE E2E 標「部分（Letter Sealing）」、我方欄主色淡染＋`th scope`（無障礙）。殘餘：競品資訊需年度複查。website 55 綠＋build 14 頁 | 已接受 |
| [0253](./0253-accessibility-high-contrast-and-ui-scale.md) | **無障礙設定：高對比模式＋UI 尺寸五檔**：現況零高對比支援且亮色 `--muted` 對白底僅 **4.18 < WCAG AA**；CSS 全 px 無 rem→rem 重構不划算。決策：(1) **高對比＝獨立 `data-contrast` 軸**與亮/暗正交（四組合自然成立）——token 覆寫進 `@cinderous/theme`（`HIGH_CONTRAST` SSOT＋`resolveTheme({contrast})`），`msn.css` 區塊與之對齊（測試讀 CSS 比對）；**AA 以測試鎖住**（`contrastRatio` 純函式，文字 ≥4.5/邊框 ≥3，實際 5.6–19.2）；焦點環 2→3px＋連結強制底線；未手動設定時跟隨系統 `prefers-contrast: more`（無障礙需求非審美，與 0250 不矛盾）；(2) **色覺友善色票** `ACCENT_PRESETS_CB`（Okabe-Ito 取向 5 色，白字全 ≥4.5 測試鎖住）；(3) **UI 尺寸五檔**（90–150%，`nb.uiScale` 裝置層）＝Tauri 原生 `webview.setZoom()`（capability 補 `allow-set-webview-zoom`，座標零校正）＋瀏覽器退 CSS `zoom`（浮動視窗/縮放把手位移除以 `cssZoomFactor()`，三處）；(4) 外觀分頁「無障礙」區（aria-pressed、選中主色描邊非只靠顏色）。範圍桌面先行；行動端 `resolveTheme` 已支援待接 UI。theme 22＋desktop 549＋全 workspace 綠 | 已接受 |
| [0252](./0252-now-playing-auto-detect.md) | **正在聽自動偵測：♪ 切換、GSMTC 本機查詢、封裝節流**：原純手動輸入；研究釐清 (1) 偵測範圍＝Tauri 桌面（Windows GSMTC；瀏覽器 `mediaSession` 只能發布不能讀系統→做不到；行動端不可行）、(2) 額度＝偵測零網路，廣播端天真實作聽歌時段 ≈ +80 則/hr 會侵蝕「改變稀疏」前提。決策：**引擎分層節流**——`setNowPlaying` 同文去重（重複 set＝no-op）＋封裝壓每 5 分鐘最多一次（窗內掛尾端補發最新值；P2P 直送即時零 relay 成本；`setStatus`/catch-up 不受影響）→ 增量上限 ≈12×K/hr（+2~5%）；**Rust `now_playing_detect`**（GSMTC「播放中」→「演出者 — 曲名」，`windows 0.61` 共版、cfg(windows) 限定）；**♪ 按鈕化**（使用者指定 UX）：點擊切換自動/手動、開啟主色高亮＋唯讀偵測顯示、**opt-in 預設關**（`nb.npAuto` 裝置層，自動廣播聽歌＝新隱私面）、關閉即廣播清空；前端 15s 本機輪詢變更才進後端；瀏覽器/行動端維持純手動。殘餘：非 P2P 聯絡人最多晚 5 分鐘；macOS/Linux 偵測待補（MPRIS）；CI 不編 cfg(windows)→本機 check/clippy 驗。engine 131＋desktop 535 綠 | 已接受 |
| [0251](./0251-unlock-screen-secondary-actions-demotion.md) | **解鎖畫面逃生口降級為細字連結（否決 icon-only＋懸停文字）**：三顆按鈕視覺等重的病因＝逃生口掛的 `settings__reveal` 是**幽靈 class**（10+ 處引用、零 CSS 規則）→ 在 `.signin` 內 fallback 成主色實心，與「解鎖」搶視覺。否決 icon-only：「忘記密碼？」是**焦慮情境逃生口**（不能藏、無無歧義 icon、業界一律文字連結）、低頻動作 icon 學習成本攤提不回、**觸控裝置無 hover**（webapp 平板上說明永不出現）、原生 title ~1s 延遲。決策：逃生口＋救援「返回」改掛既有 `signin__relaytoggle`（＋2 行 `signin__escape`＝成行置中）、「解鎖」獨佔主色；`unlock_forgot` 縮至「忘記密碼？」（細節本在 `rescue_hint`，行動端同鍵連帶受益）；SignIn `nsec-open` 改 `signin__secondary` 與兄弟 `pair-import` 同語言；`settings__reveal` 於登入/解鎖範疇退場（測試鎖住）。殘餘：SettingsPanel 等其餘幽靈 class 引用留待盤點。desktop 534＋mobile 197 綠 | 已接受 |
| [0250](./0250-light-default-all-versions-and-brand-links-to-site.md) | **全版本首次啟動預設淺色＋品牌字樣「Cinderous」外連官網**：(1) 桌面/webapp `initialTheme()` **移除 `prefers-color-scheme` 分支**→首次一律 `light`（已存偏好仍優先），至此三版本首開皆淺色、深色改手動（收斂官網 ADR-0246 立場；行動端本已 `initialTheme="light"`）；(2) 抽 `BrandWordmark`（SSOT）＝ app 名稱包成外連 `SITE_BASE`（新分頁、`rel=noopener`，比照 SettingsPanel 既有外連），用於 **SignIn／UnlockScreen／ContactListWindow** 標題列（登入/解鎖與經典佈局皆現、且非拖曳把手）；**刻意不動原生 Tauri 標題列**（OS 拖曳把手→連結會與拖曳衝突/誤觸）；官網 nav 品牌已連首頁（不動）、行動端無 in-app 字樣可接；新增 i18n `brand_visitSite`。殘餘：Tauri 三欄登入後無可點品牌字樣（登入畫面已有）。desktop typecheck＋532 test＋build 綠 | 已接受 |
| [0249](./0249-theme-toggle-in-three-column-layout.md) | **三欄佈局補明/暗切換鈕（抽 `ThemeToggleButton` 共用元件）**：全 app 唯一的明暗切換 UI 原僅在經典佈局 `ContactListWindow` 標題列＋登入/解鎖畫面；三欄（modern）左欄是 `DeckSidebar` → 登入後**三欄完全沒有明暗鈕**：瀏覽器三欄（idbar 只有身分/⚙、設定面板只有主題色 accent、瀏覽器不畫自繪標題列）使用者**無途徑切換**（回報問題），Tauri 三欄（idbar 依 0206 不畫、原生標題列也沒有）同缺。決策：(1) 抽 `ThemeToggleButton`（SSOT，`className/testId/tabIndex` 貼合各處），`TitleControls` 改用它（Fix-First 不另造）；(2) 瀏覽器三欄放 **idbar**，gate on `layout==="modern"`（idbar 於三欄只在瀏覽器渲染→自然只瀏覽器三欄顯示、不與經典重複）；(3) Tauri 三欄放**原生 `TitleBar`**，gate on `identityControls`（僅三欄＋Tauri 註冊，0206）；(4) 經典佈局不動避免重複。殘餘：mac 交通燈風格未替 `--theme` 上專屬色（中性鈕、仍可用）。desktop typecheck＋530 test 綠 | 已接受 |
| [0248](./0248-threat-intel-multi-feed-union-and-per-feed-memory.md) | **威脅情報多-feed 聯集＋逐-feed 記憶（止住變動量護欄反覆誤觸）**：排程反覆變紅的根因＝URLhaus 兩 feed 分歧（abuse.ch 直連 ~592、GitHub 鏡像 ~368，且 abuse.ch 部分環境不可達），舊「誰先通用誰」每次接到不同來源→網域數 368↔592 跳動→變動量護欄（0.5）把「換源」誤判為投毒/暴跌 `exit 1`。決策：(1) **聯集**——一個來源的所有 feed 全抓去重合併（`unionWithMemory` 純函式，順序不影響）；(2) **逐-feed 記憶**——每 feed 最後成功內容寫 `docs/threat-intel.feeds.json`（committed、不在 vite 白名單＝不送 app），某 feed 掛掉沿用記憶→聯集不縮水→護欄回到只在真.大量增減才響（**門檻不放寬、投毒防護不打折**）；單-feed 的 StevenBlack 不進快取；(3) **`--reseed`** 逃生口＋`workflow_dispatch` 輸入，供刻意變更來源組成時重設基準（手動化＝穩態護欄永在線、不開投毒窗）；(4) 純核心匯出＋`node --test scripts/*.test.mjs` 進 ci.yml（root scripts/ 不在 pnpm workspace）。取代前一版「鏡像優先」band-aid（那會丟 abuse.ch 獨有活躍威脅）。殘餘：首建聯集會跳一次→需在 CI 手動 dispatch `reseed` 一次建立 ~700 基準 | 已接受 |
| [0247](./0247-mascot-shared-brand-package-three-column-website.md) | **吉祥物 CinderMascot 抽共用套件＋上三欄與官網**（已接受）：吉祥物原僅存在桌面經典佈局 `ContactListWindow` 的 me 列；三欄（`DeckSidebar`）與官網皆缺。決策：(1) 新增 `@cinderous/brand`（React-DOM 元件套件、as-source 消費）輸出 `CinderMascot`，桌面 `Brand.tsx` 改 **re-export**（`CinderMark` 維持本地、桌面專屬款）——不採 theme 幾何-only 模式（那是為 React＋RN 兩 renderer；吉祥物是單體 SVG、目前只上兩個 React-DOM 面）、不複製；(2) **三欄**放**中欄空狀態**（`deckcenter__empty`，size 104＋呼吸浮動，reduced-motion 停）；(3) **官網**放 **footer**（size 34）＋新增 **404 頁**（`renderNotFound`→`dist/404.html`，size 132、熄滅營火文案、回首頁）；身體吃站台 `--accent`（官網橘）——元件本讀 `var(--accent)`，同顆元件各環境自動上色、免 prop。404 預渲染時**剝 app JS**→純靜態不 hydrate（否則 SPA unknown→home 覆蓋 404）、noindex、不進 sitemap。首個 ship JSX 的 workspace 套件（tsc＋vite 皆驗證）；RN 埠與 Hero 副角留待後續 | 已接受 |
| [0246](./0246-website-default-locale-theme-and-enterprise-page.md) | **官網預設語言改英文、預設淺色、新增企業版頁、技術原理擴充**（已接受）：官網（`apps/website/`）預渲染雙語站——(1) `DEFAULT_LOCALE` 由 `zh-Hant` **翻轉為 `en`**（英文走根路徑、繁中改 `/zh-Hant/`；`routeSlug/href/url/alternates/sitemap/canonical/x-default` 全自動翻轉）；(2) `initialTheme()` 改 `light` 並**移除 `prefers-color-scheme` 自動切深色**（CSS `:root` 本為淺色→**消除既有 hydration 閃爍**、深色改純手動）；(3) 新增獨立 `enterprise` 頁（納入 VIEWS/nav/`seo.byView`＋自有 meta/JSON-LD＋自動進 sitemap/hreflang），六卡＝封閉自架節點(allowlist)／組織名冊邀請碼／離職接管(無金鑰託管,0052)／公司政策(保留天數·強制TURN)／資料主權／開源不鎖定；(4) 技術原理新增「底層機制（進階）」四卡＝中繼分片(0241)／多裝置 CRDT 同步(0242/0071)／通話 NAT 穿透(0243)／換機備援，**排除 FS**（ADR-0245 硬閘：審計前文案不得宣稱 FS，並以測試 `not.toContain("前向保密"/"forward secrecy")` 鎖住）。文案雙語進 `copy.ts`(SSOT)。殘餘＝既有網址語意一次性位移（繁中根→`/zh-Hant/`）；pre-release 幾無外連→不設 301 | 已接受 |
| [0245](./0245-manual-opt-in-forward-secrecy.md) | **手動觸發、opt-in 的前向保密：把 0238 的輪替降階為使用者控制**（已實作 Phase 0–2、待審計）：沿用 ADR-0238 的加密機制（IK 不動＋隨機輪替加密子鑰 EK＋retarget Gift Wrap 到收件人 EK＋kind 10040/rumor 內嵌發現＋刪 EK 得 FS 不弄丟本機歷史），但把**輪替模型從「自動每週 epoch」降階為「手動、opt-in、可當緊急按鈕」**（免 epoch 排程、預設關、不打擾既有使用者）。定案討論釐清點：**備份碼＝身分-only 不含 EK**（FS 與持久備份的根本張力——凡進備份的鑰不 FS；身分-only 更方便＋更 FS，代價＝全裝置遺失時少數未落地在途訊息救不回）；**10040 訂閱搭 presence 便車**（同 `authors:[聯絡人]`、隨真隱身同進退→零新增隱私面）；**「誰學到新 EK」＝「已確認」可觀測（對方用新鑰回你）、「未學到」不可知（沉默/無回執）→ 安全刪舊鑰靠 grace 時間非點名**；換鑰**文字警告**（在途/備份/其他裝置影響）。仍需 grace（換完不立刻刪）、降級保護（capability＋TOFU）、刪除紀律、僅 1:1、上線前外部審計＋文案不得宣稱 FS。分 4 phase：core 純函式→引擎 opt-in→UI→審計 | 已接受（待審計） |
| [0244](./0244-public-relay-file-fallback.md) | **公共檔案後備：離線送檔的成本、上限與後端選擇**（提議中）：檔案內容預設**只走 P2P**（需兩人同時在線＋打得穿 NAT）；公共站 `MAX_FILE_MB` 未設＝**整類拒收檔案塊**→一般使用者**無離線送檔退路**（收件人離線/P2P 失敗則位元組送不出，與無 TURN 的通話同構）。ADR-0162 的企業後備預設寬（500MB/收件人、7 天、存單一全域 DO SQLite）**不能直接套公共**。**成本結構與 TURN 相反**：Cloudflare **不收 egress 流量費**、成本主體是**會累積的儲存**、且檔案**常需 relay**（傳給離線者是日常 ~30–60%，非 TURN 的 ~10–20%）→ 不能套「trivial」，靠 **TTL＋配額**封頂。估算：務實 @1 萬使用者近乎免費（在途 ~0.85GB < 5GB 免費）；天花板（人人塞滿）5TB＝DO SQLite ~$1000/月（撞單 DO 上限）vs **R2 ~$75/月（零 egress、blob 正解）**。決策＝**有條件開放、分階段**：階段 A（DO SQLite＋**收緊公共上限**：~50MB/人、TTL 2–3 天、單檔 ≤8MB、**後備非取代**——只有離線/P2P 失敗才落 relay），階段 B（擴充硬閘＝blob 遷 R2，就緒前不放寬配額）。不做：用企業預設開公共（選項 1）、blob 長期硬塞 DO SQLite。元資料不惡化（同聊天）；殘餘＝超上限仍失敗（明確標示＋UI 提示） | 提議中 |
| [0243](./0243-webrtc-turn-fallback.md) | **WebRTC TURN 保底：缺口、影響界定與明確標示**（已接受）：WebRTC 直連靠 STUN（有，`stun.cloudflare.com`）＋ICE；**對稱 NAT／嚴格防火牆**下 STUN 打不穿→直連失敗。**現況無公共 TURN**（程式碼支援、企業可配 `turnServers`＋`forceTurn` ADR-0048）。**影響界定**：唯**通話（M8）無退路**（即時影音只能 P2P/TURN）；**檔案有 relay 退路（0162）、文字/nudge 本走 relay，不受影響**。**成本/必要性評估**：業界 ~8–20% 連線需 TURN、只失敗通話用、語音頻寬小→實際流量很小→現規模近乎免費（Coturn ~$5/月固定 / Cloudflare TURN 小帳單）。**修正結論：延後理由不成立，上一個「有上限」的公共 TURN（Cloudflare TURN 首選、可封頂防視訊燒頻寬）。**並明確標示於 ARCHITECTURE §7／SECURITY 殘餘＋後續通話失敗 UI 提示。TURN 用時見兩端 IP 不見內容（自架可避） | 已接受 |
| [0242](./0242-multi-device-crdt-mutable-state.md) | **多設備可變狀態的 CRDT 化：把臨時合併規則系統化**（已接受，**三階段＋墓碑 GC 皆已實作**）：多設備同步的合併目前是**臨時、add-biased**（`mergeSnapshotContent`：聯絡人/群組**補缺不覆蓋**、封鎖**聯集**）→ 缺口：**聯絡人/群組刪除會復活**（一台移除、另一台補回）、**解封不傳播**（聯集只增不減）、**欄位編輯不傳播**（暱稱/標籤改了、對方已有此聯絡人就跳過）、**設定/偏好根本不跨裝置**（每對話靜音/狀態文字/主題各台一份）。已對的：訊息 id 去重（G-Set）、已讀水位 LWW（ADR-0108）、資產 LWW＋墓碑（ADR-0224）。決策＝**輕量系統化**（非重框架 Automerge/Yjs——撞快照 180KB 預算）：OR-Set＋墓碑用於聯絡人/群組/封鎖（刪除傳播、重加復活、解封生效，複用 ADR-0224 模式）、per-field LWW 用於暱稱/標籤/設定。分階段逐坑觸發：①刪除/解封 ②欄位 ③設定。墓碑要 GC（保留窗 ≥ 最長離線期）。殘餘：metadata 成本、非因果一致（毫秒級並發同欄位 LWW 取一） | 已接受 |
| [0241](./0241-relay-sharding-durable-object.md) | **中繼分片：從單一全域 Durable Object 到按收件人 pubkey 分片**（已實作＋預設開）：產線把全網流量路由到**單一** DO（`idFromName("global")`）→ 兩個結構性問題：(1) **爆炸半徑**——單點故障、一崩全崩（ADR-0235 C1 因此 critical）；(2) **擴充天花板**——單執行緒/~128MB/有限連線。決策：**按收件人 pubkey 前綴分片**（重用 outbox ADR-0034，非重寫；分片由 pubkey 可算＝免 hint）。客戶端連 `wss://relay/s/<自己pubkey前綴>`（解「AUTH 早於選 DO」的雞生蛋，因為 shard 從自己 npub 算）；送給 B → 算 shard(B) 連該片發布；收件匣天然同片。**血條收益無條件**（一崩 1/N、與 presence 連線無關）；**擴充收益取決於 presence 處理。**presence 拆成獨立層**（訊息一對一切得乾淨、presence 多對多切不乾淨→不共用分片；client 對 presence 只需一條連線 `authors:[聯絡人]`、其失效只影響綠點不影響訊息）→ 訊息片的擴充/血條收益因此**無條件**成立。presence 層先用單一/少數 DO（廣播型、輕），成長後單獨切；與真隱身（ADR-0240）/P2P 協同降負載。與 ADR-0237 Tier 2 多連線綁定。**shard 數＝16、遷移＝切換＋舊留言自然過期（不搬資料、過渡期雙讀＋最低版本閘）皆已定**。另記**選項 1b 一人一片（per-user DO）**：血條最好、免片數/遷移，但送信 AUTH churn 與百萬冷 DO 喚醒/計費需 prototype 實測，現在先 16 不上 | 已接受 |
| [0240](./0240-true-invisible-mode.md) | **真隱身：隱身時也停止訂閱聯絡人心跳**（已接受，ADR-0237 Tier 1 落地）：既有「隱身」（ADR-0088d）只關**發布側**（不廣播），**訂閱側照舊**——仍送 `{kinds:[20000], authors:[聯絡人]}` 把聯絡人集合以真名交給中繼（ADR-0237 核心洩漏）。故舊隱身**名不符實**：對聯絡人隱身、對中繼沒有。決策：補完成真隱身——`subscribeOn` 在 invisible 時**移除心跳 filter**（聯絡人不上中繼）、`setInvisible` 重訂閱＋清 presence＋重繪（聯絡人立即變離線，不等逾時）；收件匣與訊息收發不受影響；仍收 P2P presence（正在對話者）。折進既有隱身而非新增並存模式。取捨：失去「顯示離線但仍偷看」（那本就是假隱私、仍洩漏聯絡人），清單變灰；UI hint 已更新說明。純客戶端、relay 不可見（看你像「沒有聯絡人的人」）、**IP 關聯拆不掉**（送更少）。殘餘：收件匣訂閱與發訊連線仍綁真名（連線歸屬 ADR-0237 不消失），完整元資料隱私仍需自架 | 已接受 |
| [0239](./0239-supply-chain-trust-offline-signing.md) | **供應鏈信任根：relay 清單離線簽發；threat-intel 不在 CI 簽章的取捨**（已接受）：ADR-0235 C5 的核心殘餘——`MAINTAINER_NSEC`（客戶端釘死的簽章 relay 清單信任根 kind 10037）仍在 CI，簽章步驟又在 runner 上跑整棵相依。決策：**relay 清單改維護者本機離線簽發**（CI 只探測＋更新明文 relays.json，不再持有金鑰、不簽不發；新增 `bootstrap:sign`＝`--sign-only` 對已提交清單本機簽 kind 10037＋帶內發佈；relay 集合低頻變動才簽）。明文清單被竄改也無法冒充（客戶端只採信釘死公鑰簽章）。**threat-intel 不比照 funds.json 簽章**：它是 CI 每日自動產生（無法離線手簽 2 萬筆），CI 內簽章金鑰與被服務檔在同一 GitHub 信任域→一次淪陷同取兩者→簽章擋不住真實威脅，卻重引入正要移除的「金鑰進 CI」反模式；真正威脅（上游投毒）由 H5 絕不封鎖清單＋變動量護欄處理。殘餘：簽章清單新鮮度依賴手動簽發（緩解＝明文健康清單仍每時更新＋錨點＋rumor hint）；若 MAINTAINER_NSEC 曾進 CI 應輪替一次 | 已接受 |
| [0238](./0238-rotating-encryption-subkey-fs-design.md) | **輪替加密子鑰前向保密：設計與規格**（已接受，ADR-0236 選項 3 的實作設計）：把「輪替簽章加密子鑰」落成可實作規格。**epoch＝固定 UTC 週、每週輪換、grace 固定 7 天且獨立於企業 TTL**（不隨 MAX_TTL_DAYS 放大以免自廢 FS，代價＝離線 >14 天的訊息可能收不到）。EK＝隨機、每身分一把（非 nsec 導出、非每對話）。**生成＝自由生成＋可取代事件 LWW 權威＋全 EK 私鑰同步到每台裝置（取代選 leader，免單點/開窗）**，殘餘＝私鑰未及同步的訊息重試。**發佈＝混合**：公開可取代 kind 10040（IK 簽章、帶當前＋預發下一週 EK）供 discovery ＋ 加密 rumor 內嵌（比照 ADR-0035 hint）免元資料即時更新。新裝置帶「當前＋grace 內舊 EK 私鑰」。加密＝retarget wrap（及 seal，縱深）到收件人當前 EK、#p 仍真名路由、沿用一次性臨時金鑰得 sender 側 FS。**降級保護＝簽章 capability 旗標 `fs:ek-v1`（在加密個人檔，MITM 不可偽造）＋TOFU 釘選＋已釘選卻無 EK 時拒絕靜默退回靜態、改警告/重試**。刪除紀律延伸到快照/配對老化；安全刪除 best-effort。僅 1:1、群組與 PCS 另議；不追溯。需外部審計後上線 | 已接受 |
| [0237](./0237-relay-metadata-linkability-connection-attribution.md) | **中繼層元資料可連結性：連線歸屬打穿事件層隱藏**（已接受）：根因是反爬蟲的 NIP-42 AUTH 讓**每條連線都綁在真實 npub 上**（`relay-backend` authSigner 用 this.sk），且發布與訂閱走同一條已認證連線。一個把「連線認證身分 ↔ 收發」關聯的中繼（零成本，它本就握有 authState）看得到:(1) presence 訂閱 `authors:[聯絡人]` → 你的**聯絡人集合**;(2) 你發布的 Gift Wrap（外層 #p=收件人真名）→ 你的**送出邊**——即使外層作者是一次性臨時金鑰。事件層隱藏被連線層歸屬打穿。關鍵結論:crypto/連線層混淆（臨時身分連線、輪替金鑰 presence）**在傳輸匿名到位前被 IP 關聯抹平**，對託管中繼幾乎零效益;輪替金鑰 presence 另撞 N× 信標容量牆。決策分層:Tier 0 強化 P2P presence 卸載（唯一真正把中繼移出 presence 的機制）;**Tier 1** opt-in「嚴格 presence 隱私」（關掉 authors:[聯絡人] 訂閱、presence 只走 P2P，低成本不被 IP 關聯打穿）;**自架＝元資料隱私的官方解答**;Tier 2/3（臨時身分連線＋傳輸匿名）與 DO 分片一起延後。附帶修補 ADR-0235 H1 缺口:Gift Wrap 洪水繞過速率限制（臨時作者），改以認證身分計數 | 已接受 |
| [0236](./0236-forward-secrecy-staged-ratchet.md) | **前向保密：分階段路徑（2026-07-23 修訂）**（已接受）：現行靜態金鑰無 FS/PCS——NIP-44 對話金鑰是 `ECDH(靜態,靜態)` 恆為同一把；Gift Wrap 臨時金鑰只藏寄件人**元資料**、不給內容 FS（兩層 ECDH 對手方都是收件人靜態金鑰）。故日後 nsec 失竊即可解所有側錄歷史密文。**修訂重點**：(1) 更正——**純對稱棘輪對 nsec 威脅無效**（root 由靜態 ECDH 導出者，nsec 一失即重算整條鏈；刪除舊金鑰擋不住從 nsec 重算）；「定期換金鑰」若換的是靜態導出金鑰同樣無效。**真實 FS 必須含輪替/一次性、事後刪除、且不可從 IK 導出的私鑰材料**。(2) **推薦近期路徑改為「輪替簽章加密子鑰」（粗粒度 FS）**：IK/npub 不動、另生每 epoch 一把加密子鑰 EK_e（IK 簽章、發佈為可取代事件），寄件人以一次性臨時金鑰對收件人當前 EK 加密（retarget 現有 gift wrap），epoch+grace 後在所有裝置與快照刪 priv(EK_e)→舊密文再也解不開。**關鍵：無 per-message 鏈 → 消掉棘輪的多設備 counter 碰撞、跳過金鑰窗 DoS、狀態失步**；粒度 ≈1–2 週、不給 PCS。(3) per-message 棘輪延後；完整 PCS/群組走 **MLS、不自捲**（需 Nostr 補有序 epoch delivery）。止血：限時訊息+at-rest 加固+誠實文件。FS 上線前不得宣稱前向保密 | 已接受 |
| [0235](./0235-security-audit-hardening-and-website-seo.md) | **對外面向資安加固與官網 SEO／GEO 重整**：中繼——事件結構驗證＋`RelayCore.handle` 例外圍籬（畸形 `tags` 可通過驗簽後拋 TypeError 打掛單一全域 DO＝全站斷線）、查詢條件下推 SQL＋硬 `LIMIT`（`{"authors":[]}` 原本會全表掃描到 OOM）、事件大小／tag／`p` tag 上限（一則可放大成 15,000 列 INSERT）、**啟用**per-pubkey 速率限制與非對稱時鐘窗（過去窗必須 > NIP-59 的 2 天抖動，否則擋掉所有 Gift Wrap）、NIP-42 補驗 `relay` tag＋新鮮度（擋中間人轉發挑戰竊取收件匣讀取權）、`filter.limit` 實作；桌面——補上 CSP（`csp: null` ＋ `key_get` 回傳 nsec 明文＝XSS 即私鑰外洩）、AI API key **綁定端點主機**（原本 `ai_generate("openai", 任意網址)` 就會把 key 當 Bearer 送出）、切線上 provider 不再自動解除「僅本機」；供應鏈——action 釘 SHA＋`--ignore-scripts`＋最小 `permissions`＋`pnpm audit`/`cargo audit`/clippy、threat-intel 絕不封鎖清單與變動量護欄、透明度佔位金鑰絆線；官網——**每頁真實 URL＋建置時預渲染**（原本整站一個 URL、導覽是 `<button>`、純 CSR 讓不執行 JS 的答案引擎只看到空 div）、雙語 `/en/` 路徑＋hreflang、canonical/OG/Twitter/JSON-LD/robots/sitemap/OG 圖、H1 加平述句。PoW 刻意不啟用（客戶端無挖礦實作） | 已接受 |
| [0234](./0234-unsend-confirm-and-traceless.md) | **收回確認彈窗與無痕收回**：收回無二次確認（hover 誤點即收）、且一律留「訊息已收回」佔位。決策：**確認彈窗**（桌面 modal／行動端長按確認列，含「無痕收回」勾選、每次開啟重置）；**無痕契約**＝kind 5 加 `["purge"]` tag（加密內層、舊客戶端退回佔位＝無害降級）；儲存分流 `markPurged`/`loadPurged`（與 deleted 分集合、snapshot 容忍舊快照缺欄）——purged＝顯示層**整行剔除**（列表/串/相簿/回放/匯出一致）、deleted＝佔位；驗證沿用 ADR-0233（僅原寄件人、等待區帶旗標）；`unsendMessage`/`onUnsend` 加 traceless 參數，1:1/群組/多裝置自封皆支援。限制：僅顯示層無痕（對方可能已讀/截圖、密文仍在中繼至過期） | 已接受 |
| [0233](./0233-unsend-ownership-verification.md) | **收回訊息擁有者驗證**：接收端收到 kind 5 不驗身分就標記（NIP-09 慣例漏做）→ 1:1 對端可讓**你的**訊息「被收回」、**群組任何成員可刪任何人的訊息**（UI 只擋按鈕、協定層無防線）。決策：接收端 `applyDeletion` 以 `storage.findMessage`（新增介面，memory/local/tauri 三實作）判定作者（outgoing＝自己／群訊 `sender`／1:1 `contact`），**作者≠收回寄件人→靜默拒收**（寄件人由 Gift Wrap seal 驗證、不可偽造）；目標未抵達→`pendingDeletes` 有界暫存（比照 pendingReceipts），三個訊息落地點驗證後補套用；本機自發收回不設閘（自己裝置隱藏＝自己的選擇）。1:1 偽造拒收＋群組非作者拒收/作者生效測試；engine 296 綠 | 已接受 |
| [0232](./0232-message-interaction-copy-thread-alsomain.md) | **訊息互動強化（複製／點文字開串／串回覆同傳主對話）**：複製要手動反白、開串命中面積小、串內結論無法回流主對話。決策：**📋 一鍵複製**（hover 浮現、複製可見文字不含資產 manifest）；**點訊息文字開串**（`openThread(rootIdOf(m))`，連結/按鈕優先＋反白選取不觸發、🧵 鈕保留）；**串回覆「同時傳到主對話」勾選**（仿 Slack）——rumor 內層加 `["also-main"]` tag（reply e-tag 照舊＝NIP-10 相容、加密內層中繼不可見），`ChatMessage.alsoMain`＋`mainMessages` 保留 alsoMain 回覆（桌面/行動共用 thread-util 同時生效）、🧵 徽章標示。否決客端雙發（id 分裂、回條對不上）。舊客戶端無害降級；行動端勾選列後續 | 已接受 |
| [0231](./0231-url-threat-intel-mask.md) | **URL 威脅情報比對與訊息遮罩**：啟發式 `assessUrl`（ADR-0038）對「長得正常的已知惡意網域」無能為力；雲端查詢 API 會送出 URL＝違背隱私底線（否決）。決策（本地清單比對）：開源來源（URLhaus CC0＋StevenBlack MIT）build 成 registrable-domain snapshot **分來源保留**；core 純函式 `matchThreat`（子網域命中母網域）＋`assessUrl` 注入 matcher→`known-malicious`（danger）＋`sources` 出處；**渲染端遮罩**（顯示來源、一般可展開/嚴格企業不可展開）＋**送出端警示**（可關；嚴格模式阻止送出）＋設定四項（啟用/送出警示/嚴格/自訂清單）；snapshot 官網部署＋定期重建（複用 ADR-0228 管道，opt-in 失敗靜默）。**比對純本地、URL 絕不離開裝置**。P1 core＋assessUrl／P2 snapshot 管道（每日排程＋官網部署＋desktop 拉取快取）／P3 遮罩＋送出警示＋嚴格＋設定四項／P4 官網介紹皆落地；desktop 513＋core 411＋website 10 綠 | 已接受 |
| [0230](./0230-website-mobile-usability.md) | **官網手機可用性強化**：觸控目標偏小（nav ~33px）、hero h1 斷點硬切易溢出、長字串可能撐出水平捲動。決策（最小 CSS 修正、零 JS）：**觸控目標 ≥44px**（≤640px 的 nav link/toggle/cta＋`.btn` `min-height:44`；icon 鈕由 0229 處理）；**hero h1 `clamp(36px,9vw,64px)`** 取代固定值＋斷點硬切；**`html,body{overflow-x:clip}`**（clip 不建捲動容器、比 hidden 乾淨）＋`body{overflow-wrap:break-word}`；**nav 維持 flex-wrap 不做漢堡**（項目少、兩行可容納）。否決：漢堡選單重構（改動大＋JS 狀態）。website 9 測試＋build 綠 | 已接受 |
| [0229](./0229-website-hero-icon-actions.md) | **官網 hero 改品牌 icon 按鈕列（分平台下載）**：原文字按鈕不分平台——macOS 使用者進 releases 只找得到 Windows 檔、行動版未推出無從得知。決策：hero 改 **SVG 品牌 icon 按鈕列**（`icons.tsx`：Windows／Apple／手機／地球／GitHub，`currentColor` 隨主題、零外部資源）；**下載分平台**——Windows 可用（primary、連 releases）、macOS／行動版 **disabled**（灰階＋`aria-disabled`＋tooltip「即將推出」）；**自訂 CSS tooltip**（hover／聚焦顯示）；「看技術原理」保留文字連結；**手機 ≤640px icon＋可見標籤**（觸控無 hover、目標 ≥44px，協同 0230）；i18n zh/en＋`aria-label`＋鍵盤可聚焦；nav 下載鈕不動。website 9 測試＋build 綠 | 已接受 |
| [0228](./0228-update-detection-and-prompt.md) | **桌面／行動版更新偵測與提醒**：桌面走 GitHub release（未走商店）→ 使用者不被告知有新版；行動版尚無原生發布管道。決策（採「僅偵測＋提醒」）：**最新版來源＝官網部署的 `releases.json`**（GitHub Pages，發版時才部署＝已發布權威、避免 hold 草稿誤報；endpoint 可設定供自架）；**core 純函式 `latestNewerThan`**（semver 比對 `APP_VERSION`，fetch 平台注入、desktop/mobile 共用）；開機查一次＋localStorage 節流＋失敗靜默；**關於區「可更新 vX.Y.Z」徽章＋前往下載**（不自動安裝）；**隱私 opt-in 可關、只查版本號、不送使用者資料**、CSP 放行 endpoint。否決：Tauri updater（需簽章金鑰，列後續）、直連 GitHub API（rate limit）。P1 core（`newerRelease`）／P2 官網發佈／P3 desktop（`update-check.ts`＋開機查詢＋關於區徽章＋opt-in 開關）皆落地 | 已接受 |
| [0227](./0227-unified-version-and-release-notes.md) | **統一版號、runtime 版號顯示與雙語 release note**：版號四層分歧（tag 統一 v0.0.12／各 app package.json 不同步 desktop0.0.12·cli/mobile0.0.11·web0.0.0／runtime 無版號／協定各帶 v:1）。決策：**SSOT＝root package.json version**＋`version:sync` 腳本（對齊四端 app＋desktop 三處 package/tauri.conf/Cargo）＋CI `--check`；**runtime 經 vite `define __APP_VERSION__`** 注入（源自 SSOT，不混 tauri getVersion）；**release note＝單一 `docs/releases.json`（雙語結構化 zh/en）**，app 依 locale 顯示、`release-notes.mjs` 生成 GitHub release 雙語 body；**desktop 設定「關於/版本」區** 顯示版號＋更新記錄。P1 SSOT/sync（83ec161）／P2 runtime 注入（6b4e0b9）／P3 releases.json＋腳本（eba4e70）／P4 desktop 關於區皆已落地；四端統一 0.0.13、i18n 8／desktop 484 綠；仍未發版。已接受 | 已接受 |
| [0226](./0226-raster-size-bounds.md) | **Raster 尺寸把關（blob 位元組上限＋GIF 像素上限）**：ADR-0223 blob 傳輸有上限（64×48000≈3MB）但**產生端不限**→超大 GIF 本機存/送、對端 chunk total>64 全丟棄＝**本機成功、對端靜默失敗**；且 GIF 保留原位元組、**像素寬高不限**（非 GIF 匯入已 canvas 正規化）。決策：`BLOB_MAX_BYTES=ASSET_CHUNK_CHARS×ASSET_CHUNK_MAX_TOTAL`（產生端 addRaster 超過即拒收、送端 sendAssetBlob/push 超過不送）；`RASTER_MAX_EDGE`＋`gifDimensions`（讀 GIF Logical Screen Descriptor byte6–9，沿用 0225 firstBytes）＋`rasterWithinPixelBounds`（僅 GIF、非 GIF 回 true），匯入/收端行內/收端 blob 皆擋超大像素。純函式無新依賴。已接受 | 已接受 |
| [0225](./0225-raster-magic-byte-sniffing.md) | **Raster 資產 magic-byte 內容嗅探**：匯入/收端對 raster 只驗「宣告 MIME＋data URI 格式」（`isValidRasterDataUri` regex），不讀位元組→偽裝副檔名/MIME 可入庫（與 SVG 內容層 `validateStickerSvg` 不對稱）。決策：新增 `rasterMagicOk`（手寫極簡 base64 前綴解碼取前 12 bytes，比對 GIF/PNG/WEBP/JPEG magic、要求宣告與實際一致），**Fix First 串進 `isValidRasterDataUri`**→匯入 addRaster/addSticker 與收端 parseAssetManifest 一次全升級；不用 atob/Buffer（純函式）。blob 走 contentHash 不經此檢查。raster 無腳本面＝健壯性/一致性強化。已接受 | 已接受 |
| [0224](./0224-cross-device-asset-sync.md) | **跨裝置自訂資產同步（emoji／貼圖庫與 blob）**：`customAssets`/`assetBlobs` 每裝置本機→他裝置無該 emoji（打不出、blob 無法渲染）。決策（採 B）：**庫走加密雲端快照**（ADR-0071，`CloudSnapshotContent.customAssets`，小圖帶行內 svg、大圖只帶 ref，180KB 預算截斷、交換律合併＋mine/最愛保護）；**blob 排除於快照、改「向自己 backfill」**（擴 ADR-0223：ASSET_REQUEST 放行自我定址、sendAssetBlob 回應自己）；觸發＝開機合併＋庫變更 debounce 重發、遇「有 ref 無 blob」向 self 索取。**刪除傳播＝墓碑（LWW 交換律，`assetTombstones`＋每筆 `at`，重匯自動復活，僅自機間生效）**；**無檔案/來源離線＝佔位優雅降級**（顯示 pending、背景索取、上線即補，收送不中斷）。實作 M1 core（aa390aa）／M2 engine 自我 backfill（9025159）／M3 desktop 佈線（0340251）皆已落地，core 383／engine 291／desktop 480 綠；桌面互動待實機驗收。已接受 | 已接受 |
| [0223](./0223-custom-emoji-content-addressed-blobs.md) | **自訂 emoji 內容定址 blob 傳遞（Model B，ADR-0222 Phase 2 細部）**：大動畫 GIF 塞不進 65535 明文→圖與訊息拆開、送一次、訊息只帶參照。決策（採「首次推播＋backfill 後援」）：`nb-assets:v1` entry 加 `ref`（blob 的 contentHash、無 svg，向後相容）；新增加密 `assetBlobs` 內容定址快取（獨立於 32 策展庫、LRU）；傳遞複用 `sendFile`(P2P)/`FILE_CHUNK`+`FILE_WRAP`(relay 信箱、離線)＋群組送一次（解 ADR-0042）；缺圖以新 `KIND.ASSET_REQUEST`(44) 向寄件者 backfill；收端驗 `contentHash` 整合性；渲染狀態機 resolved/pending/failed。分 P2a（快取+ref+渲染+1:1 backfill）／P2b（推播集合+離線+群組+跨裝置）。提議中 | 已接受 |
| [0222](./0222-animated-gif-custom-emoji.md) | **自訂 emoji 動畫 GIF 支援（raster 渲染＋內容定址傳遞 Model B）**：slackmojis 主打動畫 GIF，現況匯入以 `createImageBitmap`＋canvas 只取第一幀→靜態、且渲染走 SVG-in-`<img>` secure static mode 不會動、傳遞受 65535 明文上限。決策：`CustomAsset.format:"svg"｜"raster"`，raster 存原 `data:image/gif` 直接 `<img>` 渲染（會動）、驗證改 magic-byte＋尺寸；傳遞兩層——**Phase 1** 行內小 raster（≤48KiB）、**Phase 2** 內容定址 blob 走 ADR-0093 P2P 送一次＋`nb-assets:v2:{shortcode:hash}` 參照＋快取回填（同解 ADR-0042 群組扇出）。轉碼縮小無原生 API（否決）。提議中、待實作 | 已接受 |
| [0221](./0221-custom-emoji-review-fixes.md) | **ADR-0220 自訂 emoji／貼圖庫 審查修正**：對抗式審查（正確性/並發＋安全/加密）後修——**C1 遷移↔自動收藏競態**、**C2 多對話窗 lost-update**（同根因：對共享儲存做陳舊 read-modify-write→一律 `loadLib()` 讀最新再寫）；**H1 遷移後刪舊明文**（`clearLegacyLibrary`，多身分取捨：舊全域庫歸第一個身分）；**H2 內容雜湊碰撞保留本地短碼**；**H3 行內 emoji 渲染上限 50**（防對端 DoS）；M1 LRU 保護自建（`mine`）；M2 短碼正規化小寫；L1 收端總位元組上限；L3 送出端筆數上限；L4 收緊 `validateStickerSvg`（`@import`／事件處理器）；L2 對齊註解。XSS/RCE 主防線經確認穩固（皆 `<img data:svg>`）。core＋engine＋desktop 綠 | 已接受 |
| [0220](./0220-unified-custom-emoji-and-sticker-assets.md) | **統一自訂資產（emoji＋貼圖）：共用模型＋收到自動收藏＋加密本地庫**：emoji 與貼圖在資產層同源→升 `CustomSticker` 為 `CustomAsset{id:contentHash,label,svg,kind,shortcode?}`，匯入(`wrapRasterAsSvg`+`validateStickerSvg`+32KiB)／去重／庫／收藏全共用；差異只在用法（貼圖＝整則 `nb-sticker:v2:` 大圖／emoji＝`:shortcode:` 行內小圖，可 `kind:"both"`）。去中心化無中央目錄→**Model A 行內內嵌**：訊息尾附 `nb-assets:v1:{shortcode:{label,svg}}`（短碼為鍵、`contentHash` 作儲存 id）只帶本則引用資產、解析序 本則清單→本機庫→保留原字，emoji 降至 ≤64px＋每則 ≤48KiB 預算落在 NIP-44 65535 內（B 內容定址／C org 共用集後續）。**收到自動收藏**（僅信任來源、LRU 淘汰非最愛、企業 `disableStickers` 為閘、設定可關）。**加密本地庫**：桌面接 ADR-0054 加密 AppStorage、web／RN 走 0219 `getKv()`、遷移舊 `nb.stickers.custom`；可見檔案另做匯出。**全 6 步落地**（送/渲染/收藏/挑選器分頁/`:`補全/設定/加密遷移）；因桌面 JS 無金鑰→庫存進 AppStorage 故變每身分獨立。core 352＋engine 278＋desktop 464 綠 | 已接受 |
| [0219](./0219-swappable-sync-kv-store.md) | **可換式同步鍵值儲存（行動端就緒的 KV 抽象）**：盤點 RN 原生化抽象需求——多數邊界已抽好（Notifier/AppStorage/MessageArchive/connector/rtcConfig，加密純 @noble、零 crypto.subtle 實際呼叫）。唯一硬綁的是 `profiles.ts`/`device-id.ts` **直接呼叫同步 localStorage**（RN 的 AsyncStorage 是非同步、換不進來）。新增 `engine/kv.ts`：`KvStore` 介面＋單一可換基質（比照 getNotifier），預設 localStorage（不可用優雅失敗），`setKvBackend` 供 RN 注入同步 MMKV。profiles/device-id 改走 getKv()。web 零變、有測試。YAGNI：已抽象的與 desktop-only 鍵不碰。engine 277＋desktop 447 綠 | 已接受 |
| [0218](./0218-signin-create-identity-hint.md) | **登入畫面：新名稱明示「將建立新身分」**：ADR-0146 以名稱統一建立/登入，但呈現不對稱——命中既有有提示＋按鈕「登入既有身分」，新名稱卻無提示、按鈕仍「登入」（實為建立）。補對稱訊號：名稱是新的且已輸入 → 顯示 `signin-create-new`「這個名稱是新的，將建立新身分（本機生成金鑰）」＋按鈕改「建立新身分」（`signIn_createButton`）。純顯示層、不動 submit/lookupName。desktop 447 綠 | 已接受 |
| [0217](./0217-notification-event-toggles-and-per-conversation-mute.md) | **通知事件細分開關＋每對話靜音**：原本只有「收到訊息」會跳通知。抽純函式 `shouldNotify(prefs, ctx)`（engine，SSOT）收斂所有閘門：總開關→視窗未聚焦→非下班靜音→對話未靜音→事件開關（群訊 `group \|\| (mention && mentionsMe)`）。`NotifyPrefs` 七開關（request/reaction 預設關）。接線 onMessage/onNudge/onCallState(incoming)/onRequests(opt-in，防洪)；reaction 已建模但未接線（onReaction 缺對話情境）。每對話靜音＝`GroupPrefs.muted`（復用於群組/聯絡人）＋ConversationWindow 🔕。SettingsPanel「要通知哪些事件」子區。engine 273＋desktop 442 綠 | 已接受 |
| [0216](./0216-classic-floating-conversation-windows.md) | **經典佈局：左固定 MSN 主視窗＋右浮動對話窗**：左＝完整 `ContactListWindow`（固定、控制鈕收斂入 idbar，⚙ 從標題列移除）；右＝`.desktop__canvas`（grid，position:relative）內每個對話為絕對定位浮動窗（拖標題列移位、拖右下縮放、點擊置頂、每窗記 position+size+z 於 localStorage）。純幾何 `floating-window.ts`（clamp/cascade/nextZ）＋`useFloatingWindows` hook（單一 hook 管全部、`[data-floatwin]` 定位、放開才落盤）。autoHide（ADR-0205）只套最外框、內部標題列常駐。窄螢幕（≤720px）退回單欄檢視切換（主視窗↔對話滿版＋返回鈕，不浮動）。三欄 modern 不受影響。desktop 440 綠 | 已接受 |
| [0215](./0215-classic-contact-grouping-and-collapsible-sections.md) | **經典佈局 MSN 擬真：自訂分組排序切換＋可收合標頭**：對照真 MSN 兩大落差——聯絡人只能依狀態分區(不能自訂分組/切換排序)、標頭不可收合且無「線上/總數」。決策沿用**既有標籤當分組維度**(可多屬、零新資料模型)。純函式 `groupContacts(contacts, mode, labels)`：狀態(現有)/分組(每標籤一區＋未分組，標頭線上/總數，線上優先)/名稱(A→Z)。排序切換控制(預設依狀態)＋可收合 `SectionHead`(▸/▾)皆持久化 localStorage；套用狀態/分組/群聊/封鎖區(不含訊息請求)。只做經典版(三欄維持 ADR-0079)。desktop 436 綠 | 已接受 |
| [0214](./0214-unified-contact-row-and-status-on-row.md) | **統一聯絡人列規格＋狀態訊息上列（情境切換副線）**：經典（依狀態分區）與三欄（最近排序）兩套列表的「列」規格分歧（副線、操作鈕、標籤、資料源）。抽單一共用 `ContactRow`（SSOT）＋純函式 `rowSecondary`：有未讀→末則預覽、否則正在聽→♪、否則狀態訊息（富狀態 renderStatus）、否則預覽、皆空留白。`SidebarEntry` 補 statusMessage/nowPlaying；`messagePreview` 抽到 deck-sidebar 共用；經典補 convos＋聯絡人標籤，三欄補 🚫🗑🧠。只統一列內容、不併容器排序/分區（ADR-0079 雙模式）。取捨：經典由單行變兩行。desktop 428 綠 | 已接受 |
| [0213](./0213-conversation-p2p-status-chip.md) | **對話標題列 P2P 直連品質晶片**：ADR-0210 靜音全域「P2P 失敗」後留下資訊缺口——P2P 失敗仍影響檔案內容（P2P-only 無 relay 退路）與通話，使用者送檔卡住卻無線索。加冷靜、脈絡化的常駐晶片（`⚡直連`綠／`⚪直連未建立`灰）。引擎：`webrtc.ts` `onConnectionState`（dc open→true、close/failed→false，以資料通道為權威訊號）→`types.ts` `onPeerConnection`→`relay-backend` 轉發。UI：`App` 以 `p2pConnected` Set 追蹤、換身分清空，只給 1:1 的 `ConversationWindow`；標題列通話鈕左側渲染，僅「有值×對方非離線」顯示。明載文字不受影響＝資訊非警報，補完 0210。engine 269＋desktop 416 綠 | 已接受 |
| [0212](./0212-cf-build-watch-paths-exclude-relay-bootstrap.md) | **CF 自動建置排除 `relay/bootstrap`（build watch paths）**：`relay-health.yml`（cron 每時）因 `health-history.json` 的 uptime 計數每次探測都變，實質每小時 commit（實測連續 9–10 筆全只動該檔）。若接回瀏覽器版 Worker 的 Git 並開自動建置，這些 bot commit 會白白整站重建（≈720/月＞免費層 500、且產物與上一小時位元級相同——清單走 Nostr 簽章事件、不進 worker 產物）＋無審核上線＋雜訊淹沒真失敗。對策：CF → Settings → Build → Build watch paths：Include `*`、Exclude `relay/bootstrap/*`（repo 根相對）。設定屬 CF 儀表板無法進 repo，於 `wrangler.jsonc` 註解承載＋本 ADR 紀錄；Git 目前未連線＝接回後生效 | 已接受 |
| [0211](./0211-unlock-screen-switch-identity.md) | **解鎖畫面加「用其他身分登入」入口**：釐清「依顯示名稱切換私鑰」現況——`resolveSignIn`（名稱選身分、密碼解那把 nsec）本就是桌面/瀏覽器共用邏輯，密碼只解鎖不選鍵。落差＝瀏覽器記住作用中身分後開機直落「那一個」身分的 UnlockScreen，畫面無「換身分」入口（桌面同樣，UnlockScreen 共用）。改：`UnlockScreen` 加 `onSwitch`（顯示「用其他身分登入」逃生口）；`App.switchIdentity`＝`clearActive`（軟登出、不刪身分、不重載）回 SignIn 名稱欄，打對名稱即命中並解鎖。維持「名稱選、密碼解」模型與零伺服器狀態；兩平台一致。順帶解掉「單一鎖定身分無法在解鎖前新建身分」的 UX 死角 | 已接受 |
| [0210](./0210-default-stun-and-silent-p2p-degrade.md) | **一般模式加預設 STUN（跨網路 P2P）＋靜音 P2P 失敗警告**：回報瀏覽器版跨網路初次對話跳「⚠️ P2P 連線失敗」（訊息仍送達）。根因＝一般模式 `buildRtcConfig` 無 STUN→只有 host 候選＝僅同區網可連；且失敗被當使用者錯誤（P2P 只是加值、訊息走 relay）。改：`DEFAULT_STUN`（Cloudflare）於非 forceTurn 帶入（企業 relay-only 不加，維持不外洩 IP）；`webrtc.ts` 失敗改 console.debug 不報錯；`_headers`／Caddyfile 的 connect-src 放行 `stun.cloudflare.com:3478`。取捨：P2P 固有揭露公網 IP。engine 266 綠 | 已接受 |
| [0209](./0209-website-webapp-entry-and-data-storage.md) | **官網新增網頁版入口＋各版本資料存放說明**：`App.tsx` 加 `WEBAPP_URL` 常數（cinderous.propfolk.com），首頁 hero 加「在瀏覽器開啟」跨 origin 連結（不 iframe，遵 ADR-0090/0147）；新增「你的資料存在哪裡」段（桌面＝OS 金鑰庫＋加密磁碟、網頁＝本地密碼包裹存瀏覽器 localStorage/IDB/OPFS＋托管站零使用者資料、行動＝本地優先；共通：中繼只暫存密文離線留言、多裝置同步才有加密快照）。雙語 `copy.ts`。typecheck/build 綠 | 已接受 |
| [0208](./0208-web-app-cloudflare-pages.md) | **瀏覽器版走 Cloudflare Pages：repo 內建 `_headers`＋`_redirects`**：對已把網域放 CF 者最省事（原生綁子網域、自動 HTTPS、免手動 CNAME）。`apps/desktop/public/_headers`（嚴格 CSP 真標頭，含 frame-ancestors——GitHub Pages meta 做不到；＋nosniff/no-referrer）＋`_redirects`（`/* /index.html 200` SPA fallback），Vite `public/` 自動複製進 dist。`connect-src` 用通用 `'self' wss:`（repo 不綁特定 relay）；要鎖死改成自己的 relay。與 ADR-0207 Zeabur/Docker 並存兩條路。信任邊界：CF 在 JS 交付路徑內 | 已接受 |
| [0207](./0207-web-app-containerized-deploy.md) | **瀏覽器版 web app 容器化部署（Docker＋Caddy，含 CSP）供 Zeabur/PaaS**：self-hosting-web-app 原只教 CF Pages/Netlify/Vercel、無容器目標（relay 有 Dockerfile、web app 沒有）。新增 `apps/desktop/Dockerfile`（node 建置→caddy 執行，build context=repo 根）＋`Caddyfile`（`auto_https off`、`:{$PORT}`、SPA fallback、**真標頭嚴格 CSP**——GitHub Pages 做不到 frame-ancestors）。CSP `connect-src` 由 `{$CSP_CONNECT:wss:}` 環境變數注入＝repo 保持 relay 無關、operator 可鎖死自己的 relay。金鑰邊界（ADR-0090/0147）不變；不改 app 邏輯 | 已接受 |
| [0206](./0206-identity-controls-in-titlebar.md) | **三欄模式：身分元件上移標題列（可拖曳、隨標題列動作）**：idbar 的切換器/＋/🔒/🗂（人 icon 併入切換器）改為一等標題列控制項，僅三欄＋Tauri 渲染。`ControlId` 增 identity/addid/unlockhidden/roster，身分控制缺漏補左帶尾（舊設定自動帶入、視窗控制遷移不變）；`titlebar.tsx` 加 `useRegisterIdentityControls`＋`IdentityControlsBundle`（比照 registerSettingsOpener），App 三欄+Tauri 註冊 bundle 且不畫 idbar；`TitleBar` 條件渲染（未註冊/unlock/roster 為 null 即略過）；拖曳編輯器補四拼塊。沿用雙帶+拖放+autoHide/風格＝零新機制。desktop 411 綠 | 已接受 |
| [0205](./0205-desktop-ui-autohide-pushdown-settings-fixed-height.md) | **桌面 UI 修正：自動隱藏標題列改「推開內容」＋設定彈窗固定高度**：(1) autoHide 揭示時標題列（fixed 覆蓋）原本蓋住內容→改為 `:has()` 偵測揭示時 `--viewport-h` 縮一條＋`.window-chrome__body` `margin-top:titlebar-h`，flex 自動吸收＝內容整體下移不溢出、過渡同步。(2) `.settings-modal` 加 `height:560px`（小視窗由既有 max-height 封頂）＝切分頁高度固定、內文卷軸調節，消除跳動。純 CSS，vite build 綠 | 已接受 |
| [0204](./0204-msi-uninstall-wipe.md) | **MSI（WiX）反安裝可選清空——屬性驅動**：ADR-0203 只給 NSIS；MSI 補上但改**屬性驅動**（`msiexec /x … WIPEDATA=1` 才清，預設保留）——互動框不適合企業靜默反安裝。`wix-uninstall-wipe.wxs`（經 `wix.fragmentPaths`＋`componentGroupRefs` 連結）定義 CustomAction `FileKey=Path`＋`--wipe-local`＋deferred＋`Impersonate=yes`，排 `Before=RemoveFiles`、條件 `REMOVE=ALL AND WIPEDATA=1`。重用 --wipe-local（Rust 零改）。ARP 直接反安裝不帶屬性＝不清；per-user 情境限制；需真實 msiexec 驗證 | 已接受 |
| [0203](./0203-uninstaller-optional-wipe.md) | **反安裝可選「一併清空」＋`--wipe-local` CLI＋明文身分索引**：NSIS 反安裝原本只移除程式、留下私鑰/資料。app 未跑讀不到登錄、keyring 無法枚舉→前端 `sync_identity_index` 寫明文 `identity-index.txt`（pubkey/namespace 皆公開）；`main()` 收 `--wipe-local`＝headless 讀索引刪金鑰庫＋store＋EBWebView；`installer-hooks.nsh` 的 `NSIS_HOOK_PREUNINSTALL` 以 MB_YESNO（預設否）選是則 ExecWait 主程式清空。重用 app keyring 避開 NSIS 刪 credential 脆弱路徑。僅 Windows；需真實反安裝驗證 | 已接受 |
| [0202](./0202-remove-identity-and-wipe-device.md) | **移除單一身分＋清空裝置（in-app，三端）**：桌面原本無法徹底移除身分。新增 Rust `wipe_identity`（刪金鑰庫 pubkey/db/rescue/db-next＋磁碟 store）與 `wipe_store_dir`；前端 `native/wipe.ts`（`clearBrowserNamespace` 純函式＋單測、`wipeDeviceLocal` 清 localStorage/IDB/OPFS）。設定「危險區域」：移除此身分（confirm danger）、清空裝置（**輸入片語 CLEAR 才執行**、提示先備救援登入碼）。金鑰庫無法枚舉→靠 nb.profiles 逐身分刪。不可逆，與軟登出（ADR-0201）分離。反安裝清空留 ADR-0203 | 已接受 |
| [0201](./0201-soft-logout-all-surfaces.md) | **軟登出（三端）**：桌面/瀏覽器原本沒有登出、行動端「登出」誤接成 `forgetActive`（破壞性刪身分）。新增純函式 `clearActive`（active→null、不刪任何 profile）；桌面 `logout()`＝confirm→clearActive→reload 回登入頁（有密碼者需再解鎖），經 `SettingsPanel.onLogout` 呈現；行動端 `logout()` 改為 `setScreen(unlock/signin)` 保留身分，破壞性 `forgetActive` 移到「移除此身分」入口。登出＝結束 session 保留資料，與 ADR-0202 清空裝置切開 | 已接受 |
| [0200](./0200-terminology-rescue-code-and-device-sync.md) | **使用者術語正名**：「備份碼」→**「救援登入碼」**（其密碼→「救援密碼」；用途本就是裝置遺失/重灌時登入回來，與既有 `rescue_*` 對齊）、「雲端備份/快照」→**「多裝置狀態同步」**（實為經 relay 的多裝置加密狀態同步，`settings_cloudHint` 順手修掉「用備份碼還原」的錯誤描述）。**只改 i18n 顯示值**（中英，鍵名/型別不變＝parity 自動維持）；**刻意不動**內部識別字（`cloudSync`/`makeBackupCode`…）、ADR-0070/0071、分頁名。無測試斷言舊字串 | 已接受 |
| [0199](./0199-unlock-hidden-entry-gating.md) | **「解鎖隱藏身分」🔒 入口收斂**：原本桌面永遠顯示（`isTauri()`），沒設過密碼的人點了只得「密碼不符」＝困惑。改閘在 `shouldOfferUnlockHidden`＝「本機有任一**已啟用密碼**的身分」（純函式、單測）。**刻意不**閘在「有隱藏身分」——否則 🔒 出現本身就洩漏「這裡藏了帳號」、反噬 ADR-0067 否認性；「有密碼」≠「有隱藏」故不可區分＝保住否認性。三方平衡（乾淨 UI／否認性／可發現性）| 已接受 |
| [0198](./0198-desktop-close-dialog-in-app.md) | **桌面關閉確認改用 app 風格對話框**（取代原生 rfd）：ADR-0197 的 `rfd` YesNo 是 OS 原生視窗，與無邊框自訂 UI 格格不入且無法本地化。改為 `CloseRequested`→`prevent_close`＋**`emit("app://close-requested")`** 交回前端，`App.tsx` `listen`→既有 `useDialog().confirm`（ADR-0139）彈 app 風格框；確認＝新命令 `quit_app`（`exit(0)`）、取消＝`hide_to_tray`（`hide()`）。新增 i18n `close_title/message/quit/tray`（中英）。移除 `rfd::MessageDialog`（`FileDialog` 仍用）。typecheck＋cargo check＋406 測試綠 | 已接受 |
| [0197](./0197-desktop-lifecycle-single-instance-tray-cache.md) | **桌面生命週期：單一實體＋關閉提示＋版本更新清快取**（修「更新後仍見舊前端」）：根因＝Tauri 編譯期把前端嵌進 exe，純前端改動不重編譯→留舊前端（清 WebView2 快取無效）。三項 Rust 改動（`tauri-plugin-single-instance` 第二次啟動聚焦既有窗；`CloseRequested`→縮 tray＋rfd YesNo 問是否直接結束；`main()` 於建 webview 前比對版本清 `EBWebView\Default\{Cache,Code Cache}`＝保留 Local Storage）**強制重編譯→重嵌新前端**（治本）＋快取雙保險。cargo check 綠 | 已接受 |
| [0196](./0196-flame-icon-redesign.md) | **圖示改火焰造型並同步桌面＋官網 favicon**：三層同心圓餘燼→**尖端向上的火焰**（外焰暖色漸層 `#e8531a→#ffab3d`＋亮內焰 `#ffd66b`，深藍圓角底）；桌面 `tauri icon` 由火焰來源（`icons/cinderous-ember.svg`）重生整套、官網 `index.html` favicon 同步為火焰 path（純色版）。行動端無 favicon、hero `CinderMark` 不在範圍。需重建＋重發 v0.0.1；website build 綠 | 已接受 |
| [0195](./0195-signin-relay-button-and-app-icon.md) | **登入 relay 切換改明確按鈕＋桌面圖示改真標誌**：relay 展開入口從右下角低調 📡 改為收合狀態列旁的明確「使用其他中繼站」文字鈕（`relay-change` testid 保留；預設仍不必填＝自動選座錨點）；桌面圖示以官網餘燼意象（深藍圓角＋暖橙/琥珀/亮黃三層，`icons/cinderous-ember.svg`）經 `tauri icon` 生成整套取代藍色佔位圖。需重建＋重發 v0.0.1；desktop 406 綠 | 已接受 |
| [0194](./0194-remove-demo-mode-hints.md) | **移除登入的「示範模式」提示文字**（未填 relay ＝自動選座預設錨點）：`ANCHOR_RELAYS` 已就緒後「留空＝示範模式」已誤導——中性化 `signIn_relayUrl`（去掉「留空使用示範模式」）、`signIn_relayDemo`/`settings_relayDemo`/`mobileSettings_relayDemo`（→「尚未連線中繼站/未設定中繼站」）；`SignIn` 的 `probing` 初值 seed（有錨點且無預設 relay 即 true）消除首個 render 閃「示範模式」。demo 後端保留但不再宣傳；未動 relay＝用預設錨點（ADR-0069）。desktop 406＋i18n 綠 | 已接受 |
| [0193](./0193-bilingual-outward-docs.md) | **對外說明文件中英雙語**：每份對外文件 `X.md` 增 `X.en.md`＋頂端語言切換 header（中↔英互指）；英文版互連指向對方 `.en.md`。納入 10 份（README、CONTRIBUTING、SECURITY、SELF-HOSTING、NODE-SUBMISSION、MAINTAINER-ACTIVATION、三份 self-hosting、User-Guide）；內部文件（PRD/ARCHITECTURE/ADR/ROADMAP/dev guide）維持單一中文。翻譯保留 code/路徑/NIP/ADR/連結、術語統一；斷鏈驗證全過 | 已接受 |
| [0192](./0192-website-fonts-multinode-selfhosting.md) | **官網字型統一＋多節點協作圖＋自架文件單一入口**：英文一律 **Manrope**（自架 woff2、Vite 指紋化、**不走 CDN** 以維持零第三方宣稱），中文走 `Noto Sans TC→PingFang TC→Microsoft JhengHei` 堆疊，單一 `--font` 套全站＋強制表單元素繼承；技術頁新增 `MultiNodeDiagram`（原創主題色 SVG：任一節點離線自動改走其他、簽章清單自動選座、P2P 直連）；新增 `docs/SELF-HOSTING.md` 單一入口比較四種 relay 部署＋web app 並連向詳細文件，`node_docs` 改指它。typecheck/build/test 綠、字型 base-aware 進 dist | 已接受 |
| [0191](./0191-rename-cinderous.md) | **專案更名 Cinder→Cinderous（含 npm scope）＋錨點子網域 whoami885→cinderous1**：`@cinder/*`→`@cinderous/*`（9 套件＋全 import＋workflow filter＋lockfile 重產）、產品顯示名大寫 `Cinder`→`Cinderous`、relay 子網域＋`GITHUB_URL`＋Pages `base /Cinderous/`＋git remote。**刻意保留**小寫內部識別字（keyring `app.cinder.desktop`／bundle id／Rust crate `cinder-desktop`／worker `cinder-relay`）避免金鑰失聯/身分變動。ADR 0001–0190 歷史不動；BMC 帳號 whoami885 修回。typecheck 9/9＋1341 測試全綠。**舊 whoami885 relay 已死→客戶端需重建** | 已接受 |
| [0190](./0190-relay-list-publish-auth-fix.md) | **修簽章清單帶內發佈的 NIP-42 AUTH 缺口**：首次實跑發現簽章清單（kind 10037）發佈全失敗（`⚠`）——因 relay `requireAuth`，`relay-core` 對 **EVENT 亦要求先 AUTH**，但 `health-check.ts` 的 `publishEvent` 直接送 EVENT 未認證 → 被回 `OK false`（探測 `conformance.ts` 早有 AUTH、發佈這支漏了）。改為**重用** `conformance.ts` 匯出的 `withWs`/`autoAuth`/`parse`，先臨時鑰 NIP-42 AUTH 再發佈（公共站不限事件作者）；契約已由 `relay-core.test.ts` 覆蓋，I/O 以真實 workflow 重跑驗證。typecheck＋88 測試綠 | 已接受 |
| [0189](./0189-second-anchor-relay.md) | **第二座錨點 relay＋簽章池收錄**：部署第二座 Cloudflare Worker relay 於**獨立帳號**（隔離帳號層故障、不重用 `whoami885`；預設 email 推導子網域已改中性 `jt0856` 後才對外）；`ANCHOR_RELAYS` 加入 `wss://cinder-relay.jt0856.workers.dev`→ 錨點 2 座（ADR-0039 最低韌性）；`relays.json` 收錄兩座為候選（每小時探測＋分級＋簽章發佈）。殘留：兩座皆 CF（全域故障仍同倒，第三座宜換平台）、中性子網域降低但未全消關聯；生效需設 `MAINTAINER_NSEC`＋重建客戶端 | 已接受 |
| [0188](./0188-maintainer-trust-root-activation.md) | **啟用維護者信任根**（填入 `MAINTAINER_PUBKEY`）：把休眠的簽章 relay 池（ADR-0039/0092）轉為運作——`bootstrap-config.ts` 填入**專用**維護者公鑰（`genkey:maintainer` 本機產、nsec 只落地＋離線備份、絕不進 code）；客戶端重建後訂閱 `kind 10037`＋`verifyRelayList` 驗簽採用。**非破壞**：relays.json 仍空/secret 未設時退回 `ANCHOR_RELAYS`。信任根風險＋輪替需重建客戶端，防護見 `MAINTAINER-ACTIVATION.md`。待辦：設 `MAINTAINER_NSEC` secret→收錄首座→重建客戶端→驗證 | 已接受 |
| [0187](./0187-website-vision-first-ia.md) | **官網資訊架構改版（願景優先）**：首頁改闡述**核心價值觀＋「取回通訊自主權」願景**（hero `eyebrow`＋願景段 `vision_*`＋核心價值四卡 `val_*` 以「對你的意義」框架）；新增 **技術原理頁** `pages/Tech.tsx`（`nav_tech`，吸收訊息傳遞圖＋四大技術支柱 `feat_*`＋協定基線卡 `tech_proto_*`：NIP-17/44/59/42/13＋WebRTC P2P）；**下載直連 `GITHUB_URL/releases`**（移除站內 `Download.tsx` 與 `download_*`，`View` `home\|download\|node`→`home\|tech\|node`）。沿用既有設計系統零新增 CSS；typecheck/build/test 6/6 綠 | 已接受 |
| [0186](./0186-website-project-page.md) | **官網改採 GitHub 專案頁 `/Cinder/`**（修訂 0185 位置選擇）：專案頁「每 repo 一個」＝不占帳號唯一 user-page 名額、不必改主 repo 名，符合「在專案下開、不要在根」；`vite.config` 設 `base: "/Cinder/"`，`Transparency.tsx` 的 `fetch("/funds.json")`→base-aware `import.meta.env.BASE_URL + "funds.json"`（尾斜線＝根站/專案頁/自訂網域三者皆正確，一勞永逸）；全站掃描僅此一處執行期絕對路徑會壞，其餘（index.html src、SVG `url(#id)`、外部 href、client-side 導覽）皆安全。build asset 前綴 `/Cinder/`＋website 6/6 綠 | 已接受 |
| [0185](./0185-website-github-pages-deploy.md) | **官網以 GitHub Pages 部署**（GitHub Actions＋根站）：新增 `.github/workflows/pages.yml`——push main 動到 `apps/website/**` 或其相依（i18n/theme/core）即 `pnpm --filter @cinder/website build`→`upload-pages-artifact`→`deploy-pages`（pnpm/Node 鏡像 ci.yml、**不加 pnpm version**、concurrency 防重疊）；站為純靜態＋**無 URL 路由**（`useState<View>`）＝無 SPA fallback 坑；採**根站**（base `/` 與 `fetch("/funds.json")` 零改動，需 repo 命名為 `<user>.github.io` 或自訂網域）；修 `GITHUB_URL` `Nostr-buddy`→`Cinder`。Pages Source＝Actions 為人工前置；專案頁 `/<repo>/` 走法（base＋fetch 兩改）已於註解標明 | 已接受 |
| [0184](./0184-mobile-aux-panel-review-fixes.md) | **行動端輔助面板審查修正**：**`auxImages`/`auxThreads` 改 `useMemo([messages])`**（原每次打字都重算整份訊息）；**行動端便條文案不再承諾算式**（新增 `note_placeholderM`/`note_hintM`，計算在輸入框、便條只筆記——與 ADR-0183 意圖一致）；便條分頁設閘改 `onNoteLoad && onNoteSave`；更正誤導註解；清桌面死 CSS（`.daux__calc*`）。加密/隱私/零廣播審查全過、無 CRITICAL。mobile 197＋desktop 406 綠 | 已接受 |
| [0183](./0183-mobile-conversation-aux-panel.md) | **行動端對話輔助面板**（桌面右欄的行動端版）：對話標頭一顆 📋 → 分頁面板（**媒體相簿／對話串／便條**），不搬三欄佈局；`thread-util`（replyCounts 等）上移共用引擎（消除桌面/行動重複，桌面改自 `@cinder/engine` 匯入）；便條每對話一張、**加密落盤**（身分 nsec 導出金鑰 `sealValue`，比桌面明文更嚴）；計算不併入便條（行動端計算已 inline 在輸入框，刻意不同於桌面）。engine 265＋mobile 197＋desktop 406 綠 | 已接受 |
| [0182](./0182-deck-right-note-tab.md) | **右欄「計算」分頁改為「便條」**：`CalcPanel`→`NotePanel`——每對話一張私人便條（`note-store.ts`，`nb.note.<id>`，純本機不上雲，多行 textarea）；**計算是其中一個功能**（ADR-0097 保留：便條最後一非空行若是算式即時算出、可插回對話）；`AuxTab "calc"→"note"`，移除無引用的 calc-only i18n 鍵。desktop 411 綠 | 已接受 |
| [0181](./0181-mobile-enterprise-residual-cleanup.md) | **行動端企業模式殘留清理**（ADR-0180 列的 3 項）：**儲存槽佇列 UI**（設定頁狀態/移除/重試）＋`completeSlot` done 時**釋放位元組**（記憶體不累積）；**企業成員鎖公司座**（`createRelayChat` 對 `org.enterprise` 不帶 `anchors`/`connectorFor`——鏡像桌面，不漫遊不外洩心跳；owner/個人照舊漫遊）；**接管/刪除二次確認**（`confirmAction`，刪除明講不可逆）。mobile 192 綠 | 已接受 |
| [0180](./0180-mobile-enterprise-review-fixes.md) | **行動端企業模式審查修正**（3 高）：**離職接管上線洩漏**（`start()` 首拍 beat 早於 setInvisible→引擎加 `initialInvisible` 建構即隱身，`takeoverOffboarded` 以 `forceInvisible` seed）；**配對企業身分連錯座**（idRelay 鏈漏 `bundle.relayUrl`＋prof stale→抽 `resolveIdRelay` 純函式補回優先序）；**隱身跨身分殘留**（`signInWith` 統一 `setInvisible(!!forceInvisible)`）；signInWith 位置參數重構為選項物件。engine 260＋mobile 190＋desktop 408 綠 | 已接受 |
| [0179](./0179-mobile-offboarding-takeover.md) | **行動端離職接管（企業主端，加密託管）＋儲存槽收檔桌面限定提示**：`org-escrow.ts` 託管清單以企業主 nsec 導出金鑰 `sealValue` **加密落盤**（`c1:` 密文、不含明文 nsec，比桌面明文更嚴，ADR-0112 不破）；`onOrgEscrow` 加密 upsert；離職＝託管中但不在名冊在世成員→名冊畫面「離職帳號接管」區：**接管查看**（以託管 nsec 登入指向公司座＋立即隱身查看歷史，`signInWith` overrideRelay）／**刪除託管**；企業主收儲存槽落盤**不做**（原生 FS）但手機介面明確文字提示。**A~D 全落地** | 已接受 |
| [0178](./0178-mobile-enterprise-owner-management.md) | **行動端企業模式・階段 D v1（企業主管理）**：`PairBundleOrg.orgInviteToken` 跨端流通（核准權杖持久/搬家）；`createCompany` 生成 `orgOwner`＋權杖身分（`signInWith` overrideOrg）→ 新 `RosterAdminScreen`（組織名/成員/歡迎詞/工時/保留天數/**邀請碼複製＋託管勾選**，`publishRoster` 契約鏡像桌面）；設定頁「組織名冊」入口（企業主限定）。**一家公司可全在手機組起**；離職接管（onOrgEscrow 持久＝紅線敏感）與群組/進階政策留 v2，收儲存槽落盤留桌面 | 已接受 |
| [0177](./0177-mobile-company-vault-deposit.md) | **行動端企業模式・階段 C（公司儲存槽員工端）**：桌面靠 `savedPath` 重讀原檔（限 Tauri），行動端 web 無持久路徑→新增 `slot-queue.ts` 佇列**直接持有位元組**（v1 session 內）；對話 🗄 鈕（企業成員限定）主動挑檔入佇列，`origin`＝對話名；背景效果＝企業主在線且有 pending→`depositFile` P2P 直送（不需讀檔）。**員工端完全獨立**（消費＋入職＋存槽齊備）；企業主接收/落盤仍桌面（原生 FS）；durable/佇列 UI 後續 | 已接受 |
| [0176](./0176-mobile-enterprise-invite-join.md) | **行動端企業模式・階段 B（邀請碼入職）**：`NsecSignInScreen` 顯示名稱欄貼邀請碼→「加入組織」面板（公司座 host＋escrow 明示同意）→ `joinOrg` **生成全新企業成員身分**（`inviteToOrg`）；`backend.ts` 放開 ADR-0173 唯讀＝轉發 `orgJoinToken`/`orgEscrow`（冪等，開機自動入職＋公司帳號託管）；`signInWith` per-identity relay 鎖公司座；有密碼＝`rememberInProfile(org)` 跨重啟持久。**員工端完全獨立** | 已接受 |
| [0175](./0175-mobile-enterprise-consumer-completion.md) | **行動端企業模式・階段 A（消費端補完）**：保留天數已由引擎內部套用（傳 orgAdminPubkey 即免費）；**下班自動靜音**（`shouldMuteOrgNotification` 上移共用引擎消除桌面/行動重複，桌面再匯出保持 import 不變；行動端 `onOrgInfo` 存工時+成員、`onMessage` 通知閘門加靜音判定）；**歡迎詞**變更時顯示一次。路線圖 A~D 的 A，接續 B 邀請碼入職 | 已接受 |
| [0174](./0174-mobile-enterprise-identity-persist.md) | **行動端配對企業身分跨重啟持久**：配對匯入可「記住此裝置」（`PairImportScreen` 加本地密碼欄，留空＝暫時 session）；`rememberInProfile` 把 org 精華寫進登錄 Profile（enterprise/orgOwner/adminPubkey/…）；`signInWith` 的企業精華改取 `bundle?.org ?? profileOrg(登錄 Profile)`＝重啟解鎖即以企業身分啟動（連公司座/採用名冊/頭銜設閘）。打通「桌面建立→配對帶來→後端運作→跨重啟持久」；nsec 仍只 Argon2id 包裹（ADR-0112 不變）；仍唯讀 | 已接受 |
| [0173](./0173-mobile-enterprise-backend-roster.md) | **行動端企業身分後端接線**（唯讀採用公司名冊）：`MobileBackendOptions.org` 透傳給 `createRelayChat`＝鏡像桌面——`org.adminPubkey`→`orgAdminPubkey`（後端訂閱並採用管理者名冊：同事自動進通訊錄＋allowlist＋政策＋組織資訊）、`org.orgOwner`→`orgOwner`；**只讀不寫**（不帶 orgJoinToken/orgEscrow，不從行動端重觸發入職/託管）；`onOrgInfo`→`selfEnterprise=true`（實際名冊採用確認會員身分，設閘雙來源）。durable 化與寫入流程留待下一批 | 已接受 |
| [0172](./0172-enterprise-identity-via-pairing.md) | **企業身分經配對搬家帶到行動端**（審查 #6）：`PairBundle.org`（enterprise/orgOwner/adminPubkey/orgJoinToken/orgEscrow，向後相容＋雙向淨化）讓工作身分從桌面搬到手機時保留企業脈絡；桌面 `runPairSource` 帶上作用中 profile 的 org；行動端以執行期 `selfEnterprise`（取自 `bundle.org`）設閘頭銜編輯器＝**企業/企業主身分才顯示**（與桌面一致），解掉 ADR-0170 遺留；後端連公司座/抓名冊與登錄持久化留待下一批 | 已接受 |
| [0171](./0171-status-message-broadcast-coalescing.md) | **自訂狀態文字廣播節流**（審查 #5）：引擎 `setStatus` 同步廣播是**刻意契約**（ADR-0129 catch-up＋測試依賴，不改）；改在 UI 層合併——`changeStatusMessage` 本機狀態/持久化即時、`setStatus` 廣播走 ~600ms debounce（停手才送目前文字一次、不外送半成品）；離散狀態變更仍立即並清待送文字計時器；卸載/登入/登出清計時器。桌面仍逐字（後續比照） | 已接受 |
| [0170](./0170-mobile-parity-batch-3-groups-title.md) | **行動端功能對齊批次三**（群組／頭銜）：**新增群組成員**（成員面板「＋新增成員」列出尚非成員的聯絡人逐一加入，僅管理者＋後端支援時顯示；群組無共用金鑰＝免 rekey）、**企業自報頭銜 chip**（`Contact.title` 於聯絡人列與 1:1 標頭以實心主色 chip 顯示、群組不顯示；設定頁草稿＋「更新」鈕呼叫 `setSelfTitle` 全量重播個人檔）；頭銜編輯暫未依企業身分設閘（行動端尚無該旗標，對所有真實 relay 身分顯示、預設空、Hint 說明會廣播）；SSR 斷言 chip 條件渲染 | 已接受 |
