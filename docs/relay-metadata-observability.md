# Relay 可觀測元資料：攻擊面與降低槓桿（紀錄文件）

> **性質**：這是一份**紀錄文件**，非決策。彙整「一個惡意 relay 部署者實際能觀測到哪些
> 元資料」以及「有哪些降低槓桿、各自的代價與衝突」，供未來決策參考。已採納的項目
> 另立 ADR；其餘為**暫緩／記錄**。
>
> **決策對照**：
> - **(d) 在線 jitter/隱身、(e) 心跳 P2P 卸載** → 已採納，見 `docs/adr/0088`。
> - **(j) 在線狀態內容封裝** → 已採納，見 `docs/adr/0129`（狀態文字/音樂）與 `docs/adr/0120`（typing/敲一下）。
> - **M7 訂閱洩漏聯絡人** → ADR-0129 之後在線狀態最大的殘餘洞（本文件新增記錄，暫緩）。
> - 其餘（M1 盲化收件匣、(k) 定址式在線信標、(g) Tor 傳輸、(h) 外層填充盤點、(a/b/c/f) 結構性方案）→ 本文件記錄，暫緩。
> - 前向保密（內容層 harvest-now-decrypt-later）屬另一議題，見 `docs/adr/0028`（維持）與 `0091`（MLS 暫緩）。

## 前提

Gift Wrap（NIP-17/59，ADR-0002）已藏掉**訊息內容**（NIP-44）與**寄件者身分**。本文件只談
**Gift Wrap 之後仍殘留**、relay 看得到的元資料。威脅模型（PRD §6）已將中繼營運者列為頭號
對手、並把「全球被動流量分析對手」列為 out-of-scope——本文件的降低槓桿以「抬高惡意 relay
的關聯成本」為目標，不承諾對抗國家級全域對手。

## 一、攻擊面：relay 今天實際看得到什麼

| # | relay 可觀測 | 現況為何藏不掉 | 嚴重度 |
| --- | --- | --- | --- |
| **M1** | **收件人 pubkey** | kind 1059 的 `p` tag＝收件人真實 pubkey；客戶端以 `#p` 拉自己的收件匣（`relay/src/relay-core.ts` inbox 邏輯）。**relay 知道「誰在收訊、何時收」** | 高（Gift Wrap 補不到的最大洞） |
| **M2** | **上線時序 + 真實 pubkey** | 心跳 kind 20000 以真實金鑰明簽廣播（非 Gift Wrap） | 中（時序仍在；**內容已由 ADR-0129 封裝**、jitter/隱身/P2P 卸載由 ADR-0088 處理） |
| **M7** | **在線訂閱洩漏聯絡人清單** | 收聯絡人在線狀態須訂閱 `{kinds:[20000], authors:[聯絡人清單]}`——這個 REQ 把你的**聯絡人集合**交給 relay | **高**（Gift Wrap 藏訊息層社交圖譜，但這裡直接洩漏聯絡人是誰） |
| **M3** | **連線 IP** | 對 relay 的 WebSocket 連線來源 | 中 |
| **M4** | **訊息大小 / 時間 / 頻率** | 事件經 relay 轉發／儲存 | 低～中 |
| **M5** | **同 IP 多 pubkey 的關聯** | 連線層可把多裝置／多身分連結 | 中（跨身分不可連結是 ADR-0055 的目標，但網路層仍可能） |
| **M6** | **AUTH 簽章證據**（開 `requireAuth` 時） | 客戶端送真實金鑰簽章的 AUTH 事件（`relay-core.ts` handleAuth） | 低～中（取捨見 ADR-0057） |

> **ADR-0129 之後的更新**：在線狀態的**內容**（狀態文字 `m`、正在聽的音樂 `np`、`s`）曾夾在
> 明文心跳裡（M2 的一部分），現已改為 **NIP-59 封裝、只在改變/對方上線時發**——relay 再也讀不到
> 你在聽什麼、狀態寫什麼。typing/敲一下同樣已封裝（ADR-0120）。**M2 現在只剩「時序＋真名」**。
> 但這也讓 **M7（訂閱洩漏聯絡人）** 浮上檯面成為在線狀態最大的殘餘洞——它與 M1 同源（見第三節）。

**核心洞察**：**M1 + M2 的時序關聯**——「X 在線的同時某收件匣在收訊」——讓惡意 relay（或被串
策展進清單的一組 relay）能**部分重建社交圖譜**，即使 Gift Wrap 已藏寄件者。**M7 更直接**：不必做
時序關聯，一個訂閱 REQ 就交出你的聯絡人集合。M1 與 M7 都最有價值也最難，因為它們撞本專案兩根柱子
（見第三節）。

## 二、降低槓桿一覽（含代價與衝突）

### M1 收件人 pubkey（最有價值、最難）

- **(a) 盲化輪替收件標籤**：不以真實 pubkey 當 `p` tag，改用寄收雙方共享秘密**按時間衍生的
  輪替標籤**；relay 只見一堆無法互相關聯、也連不到單一 pubkey 的隨機標籤。
  - 代價：收件人須同時訂閱「每聯絡人 × 每 epoch」多個衍生標籤（**訂閱膨脹**，撞 ADR-0006 容量模型）。
  - 衝突：**破壞「只憑 npub 就能加你／傳你」**——陌生人算不出你的輪替標籤（撞 ADR-0024 QR 加好友／
    加好友請求流程）。
  - 可能折衷：**已互加聯絡人走盲化、陌生首次接觸走公開 npub** 的混合式（待評估）。
- **(b) k-匿名分桶**：訊息依收件人 key 的粗前綴分桶，收件人拉整桶本地過濾，relay 只知「你是桶中 K 之一」。
  - 代價：**頻寬放大約 K 倍**，直接壓免費層。
- **(c) 誘餌流量**：對隨機收件人發假 Gift Wrap 模糊收訊圖。
  - 代價：同樣吃免費層額度。

### M2 上線時序 / 內容

- **(d) jitter + 隱身上線** → **已採納（ADR-0088）**。
- **(e) 心跳／在線狀態 P2P 卸載**（複用 ADR-0006 F5）→ **已採納（ADR-0088）**。
- **(j) 在線狀態內容封裝** → **已採納（ADR-0129）**：狀態文字/音樂/`s` 移出明文心跳，改 NIP-59
  封裝、只在改變/上線時發給「在線✕無P2P」的聯絡人。心跳降為**無內容存活信標**。typing/敲一下
  同屬此類，已由 ADR-0120 封裝。**這消除了 M2 的內容洩漏，但時序仍在。**
- **(f) 盲化在線信標**：連**時序**也藏（在線狀態以輪替金鑰廣播，同 (a) 家族）。藏得更徹底，但同樣有
  訂閱膨脹／加好友衝突 → **暫緩**。

### M7 在線訂閱洩漏聯絡人（ADR-0129 後浮上檯面）

- 與 M1 同源：只要客戶端用 `authors:[聯絡人]` 過濾器向 relay 要在線狀態，就等於報出聯絡人清單。
- **(k) 定址式在線信標**：改成每位聯絡人各發一則**封裝**的在線信標（`#p` 定址、外層臨時金鑰），
  收端以 `{#p:[我]}` 收——不再列 authors。**能同時修好 M2 時序歸屬與 M7**。
  - 代價：發佈數**乘以聯絡人數**——直接摧毀 ADR-0109 的心跳配額節省（心跳本來就是請求量的大宗）。
    ADR-0129 之所以可行，正是因為它只在**改變/上線**時封裝、且只給小集合；把**每一則存活信標**都
    這樣做則回到「N × 每 60 秒」的不可負擔。
  - 可能折衷：只對「在線✕無P2P」的小集合定址、且拉長信標間隔——待評估。→ **暫緩**。
- **(l) ADR-0123 的具名訂閱 ACL** 已擋掉「無 scope 的消防水管」，但**具名＝仍要列 authors**，
  所以它防的是大規模收割、**不是** M7；兩者正交。

### M3 連線 IP

- **(g) 選用 Tor／mixnet 傳輸**：對 relay 的 WebSocket 走 Tor，relay 只見出口節點 IP。
  - 代價：延遲、行動端電量 → 設為「高隱私模式」選項、預設關即可 → **暫緩、記錄**。
  - 註：既有 TURN 遮蔽的是「P2P 對端看到的 IP」，**不等於** relay 看到的；(g) 補的是不同洞。

### M4 流量分析

- **(h) 固定尺寸填充盤點**：NIP-44 本身已對長度做填充（分桶到近似 2 次方）；待**盤點 Gift Wrap
  外層是否亦對齊**。低成本 → **記錄、待盤點**。

### M6 AUTH

- **(i) AUTH 設為選用 / 一次性證明**，或以 (a) 盲化收件匣根本免在 filter 亮真實 pubkey。取捨與
  ADR-0057 相關 → **記錄**。

## 三、關鍵取捨（為何 M1 不輕啟）

真正藏住收件人（M1），與本專案兩個賣點**直接對立**：

1. **「只憑 npub 就能加好友／傳訊」** 需要**穩定、可被陌生人計算**的收件地址；盲化輪替標籤讓
   陌生人算不出。
2. **「永久免費」** 需要 relay 端能靠 `#p` **精準投遞**、不做全桶拉取；k-匿名／誘餌／全下載都以
   頻寬（＝免費層額度）換匿名集。

因此「藏收件人 = 犧牲其一」。這與前向保密（ADR-0091）暫緩同一種性質：**不是做不到，是要拿核心
價值去換**。任何 M1 方案在推進前，都應先回答「如何與 npub 加好友＋免費層共存」（混合式 (a) 是
目前看來最有機會的方向）。

## 四、狀態摘要

| 槓桿 | 針對 | 狀態 |
| --- | --- | --- |
| (d) jitter＋隱身 | M2 時序 | ✅ 採納（ADR-0088） |
| (e) 在線 P2P 卸載 | M2 時序 | ✅ 採納（ADR-0088） |
| (j) 在線狀態內容封裝 | M2 內容 | ✅ 採納（ADR-0129；typing/nudge 見 ADR-0120） |
| (h) 外層填充盤點 | M4 | 📋 待盤點 |
| (g) Tor 傳輸選項 | M3 | ⏸ 暫緩（記錄） |
| (a) 盲化輪替收件標籤 | M1 | ⏸ 暫緩（撞 npub 加好友／免費層；混合式待評估） |
| (b) k-匿名分桶 | M1 | ⏸ 暫緩（頻寬） |
| (c) 誘餌流量 | M1 | ⏸ 暫緩（頻寬） |
| (k) 定址式在線信標 | M2 時序＋M7 | ⏸ 暫緩（發佈數 × 聯絡人數，撞 ADR-0109 配額；折衷待評估） |
| (f) 盲化在線信標 | M2 時序 | ⏸ 暫緩（同 a 家族衝突） |
| (i) AUTH 選用／一次性 | M6 | ⏸ 暫緩（記錄） |
